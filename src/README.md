# src/

Reserved for future workflow *software* (e.g. the deferred work-queue runner — `work/proposals/_deferred/26-07-11.work-queue-tier.md`). The bootstrap itself is documentation + `bun` scripts and lives in `.claude/`.
