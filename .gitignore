# Secrets — never commit (BASELINE git-hygiene policy; /initialize check #7)
.env
.env.*
!.env.example
!.env.sample
!.env.template
*.pem
*.key
*.p12
*.pfx
id_rsa
id_dsa
id_ecdsa
id_ed25519
*.keystore

# Machine-local Claude state (never canonical)
.claude/settings.local.json
.claude/worktrees/

# Scratch / runtime
tmp/
node_modules/

# Local codebase graph index (codebase-memory-mcp — BASELINE index policy)
.codebase-memory/

# OS / editor cruft
.DS_Store
Thumbs.db
*~
*.swp
