---
difficulty: low
depends-on: []
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect
---

# Phase 1 — validation-and-enactment

## Objective

Open the mission's criterion-3 evidence ledger, file the proposal's enactment, and clear the bookkeeping debt — the mechanical settlements that phases 2–3 and the close ceremony build on. Mostly filing; the judgment content (what the reconciliation note must say) is enumerated below.

## Scope

**1. Criterion-3 evidence ledger.** Open a `## Validation evidence (criterion 3)` section in this campaign's log with three slots:

- **Downward nudge — already satisfied, cite it**: M2's phase-2 verdict-absence event ([`26-07-24.execute-conversion.md`](../../todos/_done/26-07-24.execute-conversion.md)) — the Manager parked without a verdict (bug cause 4 as predicted), the SendMessage nudge recovered it with context intact, "validated during its own construction." Quote the passage; do not re-run anything.
- **Upward pause-and-ask — slot opened, filled by phase 3**: phase 3 carries the Executor-held judgment item that forces a real pause-and-ask; this ledger records it when it happens.
- **Operator panel attestation *(operator-gated)***: one line from the operator attesting live-panel observation of this campaign's subagent Managers (the operator gave an informal attestation for M2 in dialog on 26-07-24 — this campaign records a formal one against a campaign it governs).

**2. Enactment filing.** Move `work/proposals/26-07-23.subagent-manager-boundary.md` → `work/proposals/_enacted/` per the proposals convention: `status: enacted (<date> — mission v1 M2 conversion; validated M2–M3)`, append the closer's session stamp to `sessions:`, and append a reconciliation note (house style, append-only, after the final section) recording: (a) premise 1 inverted on v2.1.218 — nesting default-on/deep, the spawn-depth var a restrictive cap; the design's env key survives re-rationalized as pin + cap (D2 re-scope, operator-confirmed 26-07-24); (b) premises 2–5 confirmed by M1 with the depth-2 live-streaming bonus; (c) the boundary validated in anger during M2's own campaign (verdict-absence nudge) — cause 4 of the premature-exit bug survived conversion exactly as the proposal's fallback reasoning anticipated. `git mv`, never copy — history stays.

**3. Bookkeeping.** `work/MISSION.md` roadmap row M2 → `done (26-07-24)` (status-table maintenance, not charter prose — the determination texts stay untouched). Verify M3's row flips to `in progress` at this campaign's open per convention.

**4. Archive spot-check (pre-close prep).** Run the `/archive` enumeration (`git grep -hoE '[a-z-]+@[a-z0-9-]+/[0-9a-f-]{36}' -- work '*.md'`), verify every stamped local handle has its `work/sessions/play/<uuid>.jsonl.gz`, backfill any gap via `bun .claude/bin/session-archive <uuid>`, and record the handle-count + status table in the work order. `LOST` handles escalate — that is the failure the close ceremony must not discover mid-`git mv`.

## Acceptance

- The campaign log carries the `## Validation evidence (criterion 3)` section with the downward-nudge citation quoted and the two remaining slots explicitly opened (upward: "pending phase 3"; attestation: marked operator-gated).
- The proposal file exists under `work/proposals/_enacted/` (moved by `git mv`, no copy left behind), frontmatter `status: enacted …` + closer stamp appended, reconciliation note present covering points (a)–(c), append-only diff.
- `work/MISSION.md` M2 row reads `done (26-07-24)`; no other MISSION.md text changed (diff scope check).
- Archive spot-check table in the work order: every stamped `play/` handle → `archived` or `backfilled`; zero `LOST`; `cd work/sessions && sha256sum -c MANIFEST` passes.
- Diff scope: the proposal move + note, MISSION.md roadmap row, campaign log, this milestone's docs, any backfilled `work/sessions/` files — nothing else.

## Reconnaissance amendments (26-07-24)

Appended at the campaign's Reconnaissance gate; operator-dispositioned, milestone-architect confirmed. These **govern** where they conflict with the text above.

1. **`zero LOST` → zero *new* LOST.** The enumeration yields 12 distinct UUIDs: 7 archived, 2 backfillable (`2f2dc1c5-64e6-4673-8e24-4309de22fb19` — M2 Executor; `a52d1822-b990-4172-8516-2c4002aed454` — M1 Executor). Three have no source transcript anywhere and no action recovers them: `86ade408-7a9b-4edd-991a-6b3592ce377a` and `1063f991-16dd-4fef-bc25-ba8c37c9dd2c` (stamps inside the 26-07-05 founding series — they predate the 26-07-11 adoption of archive-on-stamp; the CLI-retention rot the durable-resumability proposal itself predicted, now realized) and `3816fb23-acef-4f71-b79f-ce68441a6b21` (Orchestra relay, never ran with this repo as `cwd`). The criterion protected two things — the ceremony never discovers a hole mid-`git mv`, and stamps *this mission relies on* stay consultable; pre-existing losses sit outside both. Record all three in the spot-check table as `LOST (pre-existing)` with one-line causes, and surface them in the closing report's reader index: the archive's front door acknowledges its holes rather than silently narrowing the criterion.

2. **MISSION.md scope — both roadmap rows.** The acceptance line's "no other MISSION.md text changed" guarded *charter prose* (determinations, criteria), not the roadmap table. Read it as **"no MISSION.md changes outside the roadmap table."** Flip M2 → `done (26-07-24)` and M3 → `in progress`. The milestone status blockquote tracks too, per house convention (M1 and M2 both did) — M3's `MILESTONE.md` header is already flipped by the Executor at campaign open.

3. **Inbound-link repair — diff scope widened (operator-confirmed).** The `git mv` breaks two live markdown links: `work/MISSION.md:11` and `work/milestones/1.spike/MILESTONE.md:14`. Repoint both at the `_enacted/` path. This is pointer maintenance, not charter prose and not historical rewriting. The two further references in `work/todos/_done/` stay stale by the never-rewritten convention — do not "fix" them.

4. **`_enacted/` shape.** The directory currently holds only the dated series subdirectory `26-07-05.agentic-workflow/`. This proposal is not a series member, so it moves to `work/proposals/_enacted/26-07-23.subagent-manager-boundary.md` as the first flat file there. Nothing in the convention forbids it.

5. **House style, extracted from precedent** (`_enacted/26-07-05.agentic-workflow/26-07-06.durable-resumability.md`, `26-07-02.mission-tier.md`): frontmatter `status: enacted (<date> — <prose>)`; the closer's stamp is a new `sessions:` entry with the comment `# enactment filing`, role naming the act; the reconciliation note is a final-position `## Reconciliation note (<date>, <context> — <one-line gist>)` heading with **prose** body, appended after the current final section (`## Spawn plan (post-spike)`), never inserted mid-document.

6. **Campaign-log authorship.** The criterion-3 ledger content is **returned in this work order**, not written into `work/todos/26-07-24.execute-validation-and-fold.md` — the Executor solely authors that document and appends the section itself, stamped `> manager@play/<executor-session-id>`. Acceptance is read against the returned content plus the Executor's insertion.

## Depends on

Nothing upstream — first phase. Downstream premises it must leave true: **the enactment note is written before the doctrine rewrite** (phase 2's ADR cites the enacted proposal at its `_enacted/` path), and **the archive store is verified whole** before phase 3 stages a close ceremony that `git mv`s it.
