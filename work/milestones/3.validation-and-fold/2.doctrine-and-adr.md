---
difficulty: high
depends-on: [1]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect
---

# Phase 2 — doctrine-and-adr

## Objective

Make the workflow's architecture durable on the product side of the seam: rewrite the root dual-doctrine per D4 (product-on-`main`, instance-on-mission-branches) and author the new `.claude/ADR.md` — the terse decision register future agents read instead of excavating archived branches. Everything authored here folds to `main` unchanged; genericity is the acceptance test for every line.

## Scope

**1. Root `CLAUDE.md` rewrite (D4).** The rewrite surface, from planning research (26-07-24):

- The two-bullet product/instance doctrine paragraph → the new model: `main` is the pure project-generic template (product only: `.claude/` manifest, scaffold, canon READMEs, `src/README.md`, `.cbmignore`); the instance — the workflow's own development — lives on **mission branches**, each closed branch tagged (`<v>-closed`) and carrying its mission archive; design history is reachable through `.claude/ADR.md` and the tags, not through in-tree instance files.
- The footgun-index table (rows point at `work/footguns/*`, which archives with the branch) → replaced by a pointer to ADR § Standing hazards.
- Conventions section survives with the genericity rule intact; "BASELINE tracks the manifest" gains nothing new (ADR.md rides the same rule).

**2. Root `README.md` rewrite (D4).** The header's "both source of truth and live instance" sentence; the "Bootstrap a new project" section (its selective-copy warning presumes a non-purified `main` — post-fold the instruction is **clone `main`**, and the wholesale-copy trap is dead at the root, per D4's stated consequence); the "Developing the workflow" section → the continuation protocol (branch from `main` per mission, work under the workflow on the branch, close + fold per the ceremony); the "Provenance" section → points at the mission tags and ADR instead of in-tree paths (the founding consult handle moves to the ADR with its restore path noted).

**3. `.claude/ADR.md` — new, terse, BASELINE-manifested.** A decision register, not an essay — one compact entry per decision: number, date, one-line decision, one-to-three-line rationale, provenance pointer (`git show <tag>:<path>` form). Integrates:

- **The founding determinations** (from `work/todos/_done/agentic-workflow/1.determinations.md`, locked 26-07-11 + the 26-07-12 `/design` amendment): callback grammar; the role ladder; the command family; the portability seam (`.claude/` + BASELINE vs `work/PROJECT.md`); MCP posture; durability/archive-on-stamp; mission close; the `work/` layout.
- **Mission v1 (subagent-boundary, 26-07-23/24)**: D1 proposal-binds; D2 *as amended* — depth is configured state; the spawn-depth key is a **pin + cap** against a default-deep platform (the enabling premise inverted on v2.1.218; capability flipped twice in a year — never assume); D3 whole-tree archives (Manager below the durable tier, stamps anchor to the Executor session, `session-archive` sweeps `subagents/`); D4 template purification (this doctrine itself); D5 consult integrity. Plus the boundary conversion's operational rules worth one line each: pause-and-ask protocol, verdict-absence detection, parent-mode permission dominance, headless-as-documented-fallback.
- **§ Standing hazards** (absorbing the product footguns, operator-confirmed 26-07-24): the consult DENY-list is name-deny-only for the deferred/built-in tool class — re-enumerate and reconcile on every harness upgrade; consults are repo-bound (exit 4 on foreign handles); `.claude/bin/` JSDoc headers must never contain `*/` (globs break the bun parse).
- **§ Continuation protocol**: how the next workflow mission runs — branch from `main`, `/plan` the mission (charter recreates `work/MISSION.md`), author `work/PROJECT.md` at branch open (restore from the last mission tag: `git show v1.subagent-boundary-closed:work/PROJECT.md` as the exemplar), close + fold per the staged ceremony; the mission-tag ledger (v1 → tag, one line, append per mission).

Each mission entry cites its tag; the register is append-per-mission. **Genericity check**: the ADR documents the *product's* design rationale — it must read correctly inside any consuming repo (a consuming repo's agents may read it for "why is the workflow shaped this way"; only the continuation protocol is explicitly marked as for-the-template-repo).

**4. BASELINE reconciliation.** Manifest: `ADR.md` added (`### This file`-adjacent or its own line per house format). Scaffold-enumeration gaps found at planning close in the same pass: `work/sessions/README.md` added to the step-2 copy list; `work/missions/README.md` stub decision recorded (scaffold it or keep `/close`'s create-on-first-close — implementer follows what phase 3's purification manifest settles); `work/sessions/MANIFEST` and `work/PROJECT.md` explicitly noted as bootstrap-authored/lazy-created (so the template's reference-resolution gate sanctions their absence).

## Acceptance

- Root `CLAUDE.md` and `README.md`: no remaining passage presumes instance content on `main` — headlessly checkable: `grep -n 'todos/_done/agentic-workflow\|proposals/_enacted/26-07-05\|proposals/26-07-05\|work/footguns/\|work/sessions/' CLAUDE.md README.md` returns zero hits (the ADR and tags carry those pointers now); the bootstrap section says clone `main`; the genericity rule and BASELINE-manifest rule survive.
- `.claude/ADR.md` exists; covers the founding determinations, mission v1's decisions as amended, standing hazards (all three), and the continuation protocol; every mission entry carries a provenance pointer; length in the spirit of "terse" — a register, not a narrative (reviewer judgment; target well under 150 lines).
- `BASELINE.md` manifests `ADR.md`; the scaffold enumeration reflects the reconciled list; the same-commit rule held (manifest updated in the commit that adds ADR.md).
- Genericity review recorded in the work order: the ADR and rewritten root docs read correctly for a consuming repo; project-specific residue (machine names, operator paths) absent from ADR.md — `grep -n 'yarrow\|/home/' .claude/ADR.md` empty.
- Reference resolution passes for all edited files against the *current branch* tree (the bare-template resolution check is phase 3's rehearsal — root docs may still cite `work/MISSION.md` etc. as conventions, but only as creatable-by-convention paths, matching BASELINE's enumeration).
- Historical files untouched: the determinations record and founding corpus are sources read, never edited.

## Depends on

Phase 1 — the ADR cites the proposal at its `_enacted/` path and the evidence ledger's existence. Downstream premise it must leave true: **phase 3's purification manifest and rehearsal treat the rewritten root docs + ADR as the final `main`-bound text** — the fold carries them verbatim; no doctrine authoring remains in phase 3.
