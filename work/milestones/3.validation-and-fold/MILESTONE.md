---
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
---

# M3 — validation and fold: prove the boundary, purify the template

> Status: pending — **stub**; harden via `/plan work/milestones/3.validation-and-fold` after M2.

## Objective

Prove the converted boundary on real work with the operator watching, file the enactments, and prepare the D4 fold-back that leaves `main` a pure project-generic template.

## Scope (gist)

- **Validation campaign**: a real `/execute <milestone-or-order>` under the new boundary — candidate subject: implementing `/configure` (`todos/26-07-11.configure-command.md`), real enough to exercise specialists, nudges, and a pause-and-ask. Success criterion 3's both-directions evidence is recorded in the campaign log.
- **Enactment filings**: `26-07-23.subagent-manager-boundary.md` → `_enacted/` per the proposals convention; addenda + footgun entries from the campaign.
- **D4 doctrine rewrite**: root `CLAUDE.md` + `README.md` — the product/instance split becomes product-on-`main` / instance-on-mission-branches; bootstrap doc simplifies (clone of `main` is the copy).
- **Fold-back preparation**: the enumerated purification (what leaves `main`, what stays), branch tag carrying the archive, the fold commit series staged for the operator's `/close` ceremony — which this milestone prepares and never performs.

## Phases (gist)

(1) validation campaign, (2) enactment + doctrine rewrite, (3) fold preparation. `/plan` decides; the campaign's lessons may reshape (2)–(3).

## Acceptance (gist)

Campaign log carries the observability evidence; `main`-bound tree (staged) contains product only; `/close` can run as pure ceremony — judgment, report, tag — with no mechanical work left inside it.
