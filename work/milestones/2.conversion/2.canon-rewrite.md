---
difficulty: high
depends-on: [1]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect
---

# Phase 2 — canon-rewrite

## Objective

The doctrinal conversion: the Executor dispatches Managers as Agent-tool subagents; pause-and-ask replaces the die-with-`ESCALATE` + `--resume` ceremony for interpretation questions; the compensatory apparatus retires from executor/manager canon; the premature-exit bug's remedies land mechanism-neutral; the headless pattern demotes to a documented fallback with stated causes. Design authority: the proposal §§ 2–5 (D1 binds), amended by M1's verdicts and the consulted architect's assessment in the bug order.

## Scope

Six files, enumerated. Everything cited as platform behavior is M1-certified (verdicts: [`todos/_done/26-07-23.probe-battery.md`](../../todos/_done/26-07-23.probe-battery.md)).

**1. `executor/CLAUDE.md` — § The Manager boundary rewritten** (retitled; today it reads "a child session, not a subagent" — the premise M1 inverted):

- **Dispatch**: each phase Manager is an Agent-tool subagent — per-call `model: "opus"` (load-bearing, M1-certified at both depths; omitting it inherits the session model at up to 2× cost), the phase work doc as the self-contained brief, the same automatic-mode workflow governing it. Depth topology under the cap: Executor (root) → Manager (depth 1) → specialists (depth 2).
- **Observability**: the native panel replaces the jsonl periscope — live transcripts at both depths (M1-certified), task status, surfaced permission prompts. The programmatic parent receives the final result only; mid-flight state rides the work doc's `## Progress` ledger, and the bounded check-in habit survives as *reading that ledger*, never a transcript.
- **Call-and-response**: upward pause-and-ask (a Manager that hits novelty ends its turn with the question — context held, truly awaiting; the Executor deliberates, consults the Architect per the existing ladder, answers via SendMessage; the Manager resumes in place). Downward nudge (Executor → Manager SendMessage mid-campaign; M1-certified: delivery at the next tool boundary, honored as task direction; a completed subagent auto-resumes on message with context intact). Known limit, stated: a parent cannot address a grandchild — the Executor steers the Manager, the Manager steers its specialists.
- **Durability amendment (D3)**: the Manager leaves the durable tier; its stamps anchor to the hosting Executor session — `manager@<machine>/<executor-session-id>` (nearest-resumable-session principle, no new grammar). Transcripts persist at `<projectDir>/<executor-session-id>/subagents/agent-*.jsonl` (all depths flat, M1-certified), swept by `session-archive` (phase 1 fact); a past Manager stays reachable by resuming its Executor and continuing the agent by id (M1-certified across a genuine session-end).
- **Permission story**: parent mode dominates structurally (M1: no per-call lever exists for a stricter child) — the campaign's autonomy scope, decided at the Reconnaissance gate, maps to the **Executor session's own mode**, not per-child flags.
- **Headless fallback subsection (clearly marked)**: the `claude -p` child-session pattern survives as the deliberate fallback, demoted from default, its cause stated at the dispatch site (genuinely independent permission scoping, or beyond-cap depth). Bug causes 1–2 land here as fallback discipline: the dispatch invocation *is* the background task, never `&`-wrapped; never background-and-wait a long external process — detach via `setsid` with PID + log captured and Executor-staged finalization.
- **Role table row** updated (`child claude -p … session` → subagent dispatch).

**2. `executor/workflows/execute-milestone.md` — 7.1–7.3 + budgets:**

- 7.1: Agent-tool dispatch replaces the `claude -p`/stream-json/`tmp/accomplish` recipe; the init-event session-id capture is replaced by the returned agent id, and the phase work doc's `sessions:` entry anchors to the Executor session per the durability amendment.
- 7.2: task-notification cadence replaces tail/jq + mtime-stall — on wakeup/notification, read the `## Progress` ledger, distill the one-liner into the campaign log. The mtime-stall bullet retires (the panel + notifications replace hung-child detection; a genuinely wedged Manager surfaces as a stalled ledger, handled by the nudge).
- 7.3: **verdict-absence detection** (bug cause 4, written once, mechanism-neutral): on a Manager's clean return, check the work doc for `## Acceptance Verdict`; absence = early stop, distinct from re-plan → SendMessage nudge to continue to the verdict. The nudge-until-verdict loop **shares the outer pass budget** (default 2) — no new counter. PARTIAL/residual handling becomes SendMessage continuation (context intact, the point of the conversion); a fresh Manager remains the re-plan path. The ESCALATE branch narrows per the amendment below.

**3. `manager/workflows/execute-work-order-automatically.md` — escalation-as-pause + EXECUTION DISCIPLINE:**

- **Escalation-as-pause**: interpretation questions become pauses — end the turn with the question and await the answer in place. The terminal `ESCALATE` verdict is reserved for genuinely terminal states, enumerated: budget exhaustion, an unblessed order after the refinement bound, operator-gated stops. Wording must remain true for a headless-fallback Manager (where a pause is not answerable, the terminal verdict remains its only channel — say so).
- **EXECUTION DISCIPLINE subsection** (bug causes, mechanism-neutral where they are): no `pkill`/`killall` on name/path substrings — exact captured PIDs or port-derived PIDs (`ss -tlnp`) only (cause 3, universal: any session whose prompt embeds a path can self-match); foreground-block under the Bash ceiling vs `setsid`-detach with PID + log captured — never background-and-wait (cause 2); the **final-mile exception** at its narrowest: direct tool calls for mechanical remaining convergence steps only, under explicit Executor instruction, never self-invoked — a bounded exception to "don't implement directly," not general license.
- The progress-heartbeat rationale rewords mechanism-neutral (crash-safe state for *any* interrupted Manager, not "killed child").

**4. `manager/CLAUDE.md`**: dispatch-classification table row (phase build → subagent Manager; SendMessage continuation for residuals; fresh dispatch for re-plans) and the `tmp/accomplish/*.jsonl` cost-note (L100) — cost now rides the dispatch result surface.

**5. `commands/execute.md`**: the milestone-tier bullet ("one autonomous Manager child session per phase (`claude -p …`)") → subagent dispatch.

**6. Close the bug order** [`bugs/26-07-23.bug.headless-manager-premature-exit.md`](../../todos/bugs/26-07-23.bug.headless-manager-premature-exit.md) to `_done/` (operator-confirmed routing, 26-07-24): a closing section maps each of the four causes to the landed text (1–2 → fallback discipline; 3 → EXECUTION DISCIPLINE; 4 → verdict-absence detection), then file it.

## Acceptance

- The five canon files contain no unmarked reference to: `claude -p` Manager dispatch, `--output-format stream-json`, `tmp/accomplish`, the tail/jq check-in recipes, or the mtime-stall rule — every survivor sits inside the clearly-marked headless-fallback subsection. Headlessly verifiable: `grep -n 'tmp/accomplish\|stream-json\|mtime' .claude/agents/executor/CLAUDE.md .claude/agents/executor/workflows/execute-milestone.md .claude/agents/manager/CLAUDE.md .claude/agents/manager/workflows/execute-work-order-automatically.md .claude/commands/execute.md` returns hits only within the fallback subsection's line range (enumerate the range in the work order).
- `execute-milestone.md` 7.3 carries verdict-absence detection in mechanism-neutral wording, sharing the outer pass budget.
- The automatic-mode workflow enumerates the terminal-`ESCALATE` causes (budget, unblessed, operator-gated) and states the pause protocol; the EXECUTION DISCIPLINE subsection carries the three rules + the final-mile exception with its three qualifiers (mechanical remaining steps; explicit Executor instruction; never self-invoked).
- The durability amendment is stated: Manager stamps anchor as `manager@<machine>/<executor-session-id>`; the known depth-2 SendMessage limit is documented.
- The headless fallback names its trigger causes at the pattern's definition site.
- Bug order in `_done/` with the four-cause mapping; no other open todo references the retired apparatus as live doctrine.
- Reference-resolution check passes for every edited file (quality gate step 1 scoped to the diff).
- Genericity holds: no project-specific facts entered `.claude/` (the acceptance test of root `CLAUDE.md`).

## Depends on

Phase 1 — the rewritten docs cite the settings cap and the archive sweep as existing, tested facts. Downstream premise it must leave true: **phase 3 greps against final canon text** — this phase leaves the six files in their converted end-state, with the fallback subsection clearly delimited so the sweep's exclusion is mechanical.
