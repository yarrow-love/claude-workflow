---
type: chore
difficulty: low
depends-on: [2]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect (consult handle)
  - executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 # M2 campaign executor (seeded this order)
---

# M2 · Phase 3 — coherence-sweep (work order)

> Seeded by executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 from the phase spec [`work/milestones/2.conversion/3.coherence-sweep.md`](../milestones/2.conversion/3.coherence-sweep.md) + phase-3 reconnaissance + phase-2's recorded end-state (fallback range 28–49) and its deferred-residue handoff. **The phase doc stays the spec/record; this is the build surface.**

**Automatic mode.** [work-order workflow](../../.claude/agents/manager/workflows/execute-work-order.md) + [automatic-mode overrides](../../.claude/agents/manager/workflows/execute-work-order-automatically.md): plan → implement → review → resolve to Acceptance, inner budget 3, converging to a `## Acceptance Verdict` here. Documentation-only; no front-end (`work/PROJECT.md`) → no inspector. Consult the Architect for doctrinal questions: `bun .claude/bin/consult "architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2" "<question>"`.

## Objective

After phase 2's rewrite: no live-canon reference to the retired apparatus remains outside marked fallback text; sibling docs that *describe* the Manager mechanism agree with the new boundary; the deferred work-queue-tier proposal takes its reconciliation note; the full quality gate passes end-to-end. Mechanical by design.

**Phase 2 is landed (cite as fact).** The six canon files are in converted end-state. The **fallback subsection** is `.claude/agents/executor/CLAUDE.md` **lines 28–49** (`### Headless fallback — deliberate, non-default`) — the token-grep's only sanctioned survivor range. `work/README.md:90` is already corrected.

## Scope

### 1. Token sweep — the grep recipe (rerun at close, output pasted into the verdict)

**Pinned invocation (recon footgun — this env's `grep`/`ugrep` shell functions mis-split a multi-file arg list under zsh and return zero hits with exit 2, silently; do NOT trust a naive `grep "$FILES"`):** sweep **directories**, not an expanded file list —

```sh
grep -rn -- 'tmp/accomplish\|accomplish-live-feed\|stream-json\|mtime\|tail -c\|tail -f' \
  .claude work/README.md work/todos/README.md work/PROJECT.md work/footguns work/runbooks docs \
  CLAUDE.md README.md
# and, separately, Manager-dispatch-shaped claude -p:
grep -rn -- 'claude -p' .claude work/README.md work/todos/README.md work/PROJECT.md work/footguns work/runbooks docs CLAUDE.md README.md
```

**Classify every hit** (no silent skips) into one of these labels — **seven** (recon added the last two):
- `live-canon-clean` — already fixed by phase 2, now reads clean (re-verify).
- `fallback-subsection-exclude` — inside `executor/CLAUDE.md` 28–49.
- `consult-apparatus-exclude` — `.claude/bin/consult`, the callback grammar / `--fork-session` text in `work/README.md`, `work/footguns/consult.md`. A **surviving** mechanism; `--resume`/`claude -p` there are legitimate.
- `historical-exclude` — `work/todos/_done/`, `_cancelled/`, `work/proposals/` (all statuses, **including** the two loose top-level files `26-07-22.lan-intercom.md` and `26-07-23.subagent-manager-boundary.md`), `work/sessions/`, `work/missions/`. Never rewritten.
- `runbook-probe4-exclude` — `work/runbooks/probe-battery.md` `--resume` hits (probe-4 procedure, bare `claude --resume`).
- `MISSION.md-exclude` — `work/MISSION.md` is the fixed charter (its failure-criteria clause names the fallback deliberately). It is **out of scope** for edits; if a hit surfaces, classify it here — do not edit it.
- **`phase-3-own-fix`** — this phase's own residue targets (item 2 below), fixed here, not merely re-verified.

Acceptance = **zero unclassified hits.**

### 2. Semantic residue — the flagged items the token-grep will NOT catch (phase-2 handoff + recon)

These carry no banned token, so grep misses them — fix/adjudicate each explicitly:

- **`work/README.md:48`** (Executor role-ladder row): "one Manager **child-session** per phase" → mechanism-neutral wording that agrees with the converted boundary (a subagent Manager per phase; the role-ladder should not invoke the retired session-dispatch mechanism). **Fix.** Keep it work-system-generic and minimal.
- **`.claude/agents/architect/CLAUDE.md:39`** and **`.claude/agents/architect/workflows/plan-milestone.md:16`**: "the literal convergence target of a **headless Manager session**" → mechanism-neutral (an *autonomous* Manager session — the criterion is **headless-verifiability of the acceptance block**, not the dispatch mechanism; preserve that meaning + the operator-gated-items flag). **Fix.**
- **`.claude/agents/manager/CLAUDE.md:114`** ("findings travel to **child Managers** via documents") and **`.claude/agents/executor/workflows/execute-milestone.md:12`** ("so **child Managers** can consult the planning session"): "child Manager" is genealogically accurate under the new model (a subagent *is* a child of its Executor); phase-2 reviewer judged not-wrong. **Coherence-review** — keep unless the phrasing actively implies the retired *session* mechanism; if kept, note why in the verdict.
- **`work/todos/26-07-22.lan-intercom.md`** consult-site anchors drift — informational only (halted order; consult apparatus, not retired dispatch). **No edit.**

### 3. Reconciliation note

Append to [`work/proposals/_deferred/26-07-11.work-queue-tier.md`](../proposals/_deferred/26-07-11.work-queue-tier.md), **after its final section**, house style (five prior `## Reconciliation note (…)` precedents exist), append-only (diff shows pure addition, nothing else in the file moves):

`## Reconciliation note (26-07-24, subagent-manager-boundary conversion — "never a subagent" clause obsoleted)`

Content: the file's retirement-rationale clause (a) (L10–13, "it dispatches orchestrators as Agent-tool subagents, which the session-nesting lesson rules out … so it must be a `claude -p` child session, never a subagent") rested on the session-nesting lesson; M1 certified nested dispatch on the pinned CLI and M2 converted the boundary, so a future work-queue tier dispatches Managers as subagents per the current executor canon.

### 4. Alignment + full quality gate

`BASELINE.md` ↔ `/initialize` ↔ live tree agree on the phase-1 additions; then the full quality gate (`work/PROJECT.md` § All roles):
1. Reference-resolution across `.claude/` (every referenced path resolves; sanctioned lazy-creates excepted, incl. `tmp/accomplish` inside the fallback subsection).
2. Tooling smoke: `bun .claude/bin/session-archive` and `consult` no-args → usage + exit 1; `commit-guard` TTY contract; `cd work/sessions && sha256sum -c MANIFEST`.
3. `/initialize` end-to-end (incl. the new depth-cap check #4).

## Acceptance

*(carried verbatim from [the phase doc](../milestones/2.conversion/3.coherence-sweep.md))*

- The work order carries the grep recipe verbatim and its final run's output: **zero unclassified hits** — every hit either fixed in this phase or matched to a written exclusion class above.
- The two architect docs read mechanism-neutral while preserving the headless-verifiability criterion for acceptance blocks.
- The reconciliation note stands in `_deferred/26-07-11.work-queue-tier.md`, house style, append-only (diff shows pure addition).
- Quality gate passes in full: every `.claude/`-referenced path resolves; `bun .claude/bin/session-archive` and `consult` no-arg usage/exit-1; `commit-guard` TTY contract; `cd work/sessions && sha256sum -c MANIFEST`; `/initialize` audit green including the new depth-cap line.
- Diff scope: no edits under the historical paths (verifiable via `git diff --stat` against the exclusion list).

**Executor-added (from the phase-2 handoff):**
- `work/README.md:48` reads mechanism-neutral (no "child-session"); the four semantic-residue items above are each fixed or adjudicated-with-reason in the verdict.

## Depends on

Phase 2 (landed) — the sweep greps against final canon text with the fallback subsection delimited (28–49). Downstream premise it must leave true: **M3's validation campaign launches from coherent canon** — a fresh Executor session following only the docs dispatches a subagent Manager, and the milestone-level acceptance (`MILESTONE.md`) is checkable without this planning session's context.
