---
type: feature
difficulty: medium
depends-on: []
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect (consult handle)
  - executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 # M2 campaign executor (seeded this order)
---

# M2 · Phase 1 — mechanics (work order)

> Seeded by executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 from the phase spec [`work/milestones/2.conversion/1.mechanics.md`](../milestones/2.conversion/1.mechanics.md) + phase-1 reconnaissance + the confirmed gate decisions. **The phase doc stays the spec/record; this is the build surface.** Acceptance is carried verbatim below — it is the automatic-mode convergence target.

**Automatic mode.** Run the [work-order workflow](../../.claude/agents/manager/workflows/execute-work-order.md) with the [automatic-mode overrides](../../.claude/agents/manager/workflows/execute-work-order-automatically.md): plan → implement → review → resolve, looped to the Acceptance criteria under the inner pass budget (3), converging to a `## Acceptance Verdict` written to *this* file. No front-end surface exists in this repo (`work/PROJECT.md`) — the inspector is not dispatched. For a genuinely novel technical question, **consult the planning Architect** (`bun .claude/bin/consult "architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2" "<question>"`) rather than escalating cold.

## Objective

Land the mechanical substrate phase 2's canon rewrite will cite as fact: the D2-re-scoped depth-cap env key (settings + BASELINE + `/initialize` audit) and the `session-archive` subagent-transcript sweep, self-tested. **No canon prose moves in this phase** — `executor/`, `manager/`, and `commands/` docs are untouched (diff-scope acceptance below).

## Implementation guidance (from reconnaissance — verified against the live tree 26-07-24)

**1. The depth-cap key — `.claude/settings.json`.** Today it has exactly two top-level keys (`permissions`, `hooks`), valid JSON, 2-space indent, no trailing commas. Add a third top-level sibling:

```json
"env": { "CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH": "2" }
```

Rationale to encode (the D2 re-scope, operator-confirmed 26-07-24): the key is a **pin + cap**, not an enabler. M1 certified that on v2.1.218 nesting is default-ON and deep (≥3 levels) and the var *restricts* — at the cap a subagent is simply **not granted the Agent/Task tool** (structural, no error). Value `"2"` encodes the ladder exactly (root session → Manager → specialists); it pins behavior against platform default flips (twice in one year) and applies the blast-radius principle to dispatch.

**2. BASELINE + `/initialize`.** Two BASELINE spots (recon confirmed both matter — do both):
- **Manifest-section settings-skeleton clause** (`.claude/BASELINE.md` ~L27–29): add one clause naming the key + value + cap/pin rationale, in the clause-per-baseline-item house style, placed **before** the terminating "**Project decisions, not baseline**" carve-out sentence (the key is baseline-pinned, *not* a per-repo decision — do not put it in the carve-out).
- **§ Expectations "Settings" bullet** (~L56): this is what `/initialize` actually audits against (`initialize.md` L5). Extend it so the audit has an anchor for the new key (e.g. "…baseline permission entries present; the `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` env cap present and `"2"`").
- **`/initialize` audit** (`.claude/commands/initialize.md`): add a **new dedicated numbered check** (do not overload the "Hooks resolve" check — recon found no value-pin precedent; a new check is cleaner) in house pattern: state the target (key present in `.claude/settings.json` and `== "2"`), name the report on failure (missing/other → report with BASELINE's recipe). This is a **value-pin** check — do **not** write a behavioral "is nesting on?" probe (that M1-era framing is obsolete).

**3. The subagent sweep — `.claude/bin/session-archive` (86 lines).** Single `archive(transcriptPath, uuid)` primitive, called from two sites — **both must gain the sweep**: hook mode (~L59, `--from-hook` branch, gated by the root-uuid `stamped` git-grep) and CLI-arg mode (~L83, inside the `for (const arg of args)` loop). Mechanics recon surfaced:
- `archive()` **hardcodes** `rel = ${machine}/${uuid}.jsonl.gz` — it **cannot be reused verbatim** for subagent files. Choose: (a) refactor `archive()` to accept an explicit output-relative-path (one primitive, cleaner) or (b) a sibling helper duplicating the gzip + MANIFEST-upsert body with its own `rel`. **Planner's call** — either satisfies acceptance; prefer (a) if the diff stays contained.
- After archiving the main transcript for `<uuid>`, glob `~/.claude/projects/<slug>/<uuid>/subagents/agent-*.jsonl` **and** `agent-*.meta.json`. M1 + recon confirmed **flat layout**: depth-1 and depth-2 transcripts co-locate in this one dir keyed by the root session — one glob covers all depths. No glob lib is imported today (`node:fs` only) — use `readdirSync`+regex or `Bun.Glob`.
- Archive each **gzip'd** to `work/sessions/<machine>/<uuid>/subagents/`, mirroring the live layout, `.jsonl` → `.jsonl.gz`, `.meta.json` → `.meta.json.gz` (gzip is content-agnostic; keep the `.meta.json` stem). One MANIFEST line each — the existing **upsert-by-path-match + sort** extends unchanged (note: MANIFEST sorts by *full line* = by leading hash, so sweep lines interleave by hash; cosmetic, expected).
- **Exclude the sibling `tool-results/` dir** — it is a sibling of `subagents/` at the session-dir level, so globbing specifically inside `<uuid>/subagents/` excludes it by construction. Do **not** do a broad recursive walk under `<uuid>/`.
- A session with **no `subagents/` dir** must archive exactly as today — the sweep is **additive, never a failure path** (do not let an empty glob count as a failure or perturb hook-mode's silent exit-0 / CLI-mode's exit-code contract).
- **Footgun** ([`work/footguns/bin-scripts.md`](../footguns/bin-scripts.md)): **no `*/`-containing token in the JSDoc header** — describe globs in prose there; keep literal globs in code. Verify the header parses (`bun run` the script) after editing.

**4. Sweep self-test (run LAST — after all specialist dispatches, or the session's own `subagents/` fixture is undercounted).** This Manager session necessarily dispatches specialists, so its own `subagents/` dir is a live fixture. After implementing, run `bun .claude/bin/session-archive <this-manager-session-id>` and verify (a) `work/sessions/play/<uuid>/subagents/` contains `agent-*.jsonl.gz` (+ `.meta.json.gz`) for the specialists dispatched this session, (b) `cd work/sessions && sha256sum -c MANIFEST` passes, (c) `bun .claude/bin/session-archive` with no args still prints usage and exits 1 (smoke test). `.meta.json` sidecar fields present live: `agentType, description, toolUseId, spawnDepth, model`.

**5. Interactive-default check *(operator-gated — STAGE, do not attempt headlessly)*.** M1 certified default-ON only via `-p` subprocesses; the interactive no-var default is uncertified and must run in a **no-var environment** (once this phase lands the key, *this* repo always runs capped). Write a concrete operator recipe into this work order's `## Operator recipe` section: launch plain interactive `claude` from `$HOME` (or any dir whose settings do not set the key), dispatch a subagent that dispatches its own subagent, record whether depth-2 completes. Evidence, **not a gate** — either outcome closes M1's caveat and does **not** block phase close; a divergence from default-ON only tightens the BASELINE rationale wording.

## Acceptance

*(carried verbatim from [the phase doc](../milestones/2.conversion/1.mechanics.md) — the inner-loop convergence target)*

- `.claude/settings.json` contains the `env` block with `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH: "2"`, valid JSON, existing keys untouched.
- `BASELINE.md`'s settings-skeleton clause names the key with the cap/pin rationale; `.claude/commands/initialize.md` carries the audit line in house style; running `/initialize`'s settings checks against the live tree passes.
- `session-archive` sweep: running it against a session id that has live `subagents/` files produces `work/sessions/<machine>/<uuid>/subagents/agent-*.jsonl.gz` (and `.meta.json.gz`) with MANIFEST lines; `cd work/sessions && sha256sum -c MANIFEST` passes; `tool-results/` content is demonstrably not swept; a session without subagent files archives cleanly.
- Tooling smoke test passes: `bun .claude/bin/session-archive` with no args prints usage and exits 1.
- *(operator-gated)* The interactive no-var default observation is recorded in the work order, either outcome. **[Executor override: staged as an operator recipe below; non-blocking for phase close.]**
- Diff scope: only `.claude/settings.json`, `.claude/BASELINE.md`, `.claude/commands/initialize.md`, `.claude/bin/session-archive`, this milestone's docs, and the phase work order — no `executor/`/`manager/` canon edits.

## Depends on

Nothing upstream — first phase. Downstream premise it must leave true: **phase 2's rewritten canon cites the settings cap and the whole-tree archive sweep as existing, tested facts** — the docs land after the mechanics, never before.

## Operator recipe

**Interactive no-var default check — evidence, NOT a gate.** M1 certified default-ON only via `-p` subprocesses; this closes the one remaining caveat (the interactive default in a *no-var* environment). Once this phase lands the key, *this* repo always runs capped — so the check must run where the key is unset.

1. From `$HOME` (or any dir whose `.claude/settings.json` does **not** set `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH`), confirm the var is unset in the ambient env: `echo "${CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH:-unset}"` → should print `unset`.
2. Launch plain interactive `claude` there (no `-p`).
3. Ask it to dispatch a subagent (depth 1) that is itself instructed to dispatch its own subagent (depth 2) and have that depth-2 subagent report back a token string.
4. Record whether the **depth-2** subagent completed and returned its token — one line, either outcome, into the milestone record `work/milestones/2.conversion/1.mechanics.md` (this work order stays the build surface).

**Non-blocking.** Either outcome closes M1's interactive-default caveat and does **not** block phase close. A divergence from default-ON (depth-2 blocked with no var set) only tightens the BASELINE cap/pin rationale wording — it does not invalidate the pin, which caps regardless of the platform default.

---

## Plan

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

Four independent files, no shared edit surface → **two parallel implementers**, then a Manager-run self-test last (not an implementer phase).

- **Implementer 1 — config + docs cluster** (A settings.json, B BASELINE.md ×2, C initialize.md, C′ operator recipe). One hand keeps the cap/pin rationale coherent across the three surfaces that repeat it.
- **Implementer 2 — the code** (D `session-archive` sweep). Isolated; the only surface needing real logic + the JSDoc footgun discipline.

**Phase A** — add `"env": { "CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH": "2" }` as a third top-level sibling after `hooks`; validate JSON parse.
**Phase B** — (1) settings-skeleton clause before the "Project decisions, not baseline" carve-out; (2) extend § Expectations "Settings" bullet with the audit anchor.
**Phase C** — new dedicated numbered check (value-pin: key present and `== "2"`), renumber the tail; do not overload "Hooks resolve".
**Phase C′** — write the interactive-default-check operator recipe into this order's `## Operator recipe` (evidence, non-blocking; must not touch the milestone doc).
**Phase D** — refactor `archive(srcPath, rel)` to take an explicit rel + `mkdirSync(dirname(...))`; add a `sweepSubagents(uuid)` helper (`readdirSync` filtered by `/^agent-.*\.(jsonl|meta\.json)$/`, gzip each into `work/sessions/<machine>/<uuid>/subagents/<name>.gz`); wire at both call sites (hook L59, CLI L83); sweep never increments `failures`, absent `subagents/` returns early; JSDoc header prose-only (no `*/`), re-verify with `bun run`.

### Decision points

1. **`archive()` refactor vs sibling helper** — RECOMMENDED **refactor (a)**: explicit `rel` param + `mkdirSync(dirname(...))`; MANIFEST-upsert logic stays in one place, diff stays contained (the guidance's condition for preferring a). Rejected: sibling helper duplicating the body.
2. **Glob mechanism** — RECOMMENDED **`readdirSync` + regex**: stays in the file's `node:fs`-only idiom, no new dep; flat layout → one non-recursive read covers all depths. Rejected: `Bun.Glob`.
3. **Work split** — RECOMMENDED **2 parallel implementers** (disjoint files, no merge risk). Rejected: single serial.

Risk notes carried to implementers: JSDoc `*/` footgun (D), hook/CLI exit-code contracts unperturbed by sweep (D), `tool-results/` excluded structurally by globbing only inside `<uuid>/subagents/` (D), JSON validity (A), diff scope (all — no milestone-doc or `executor/`/`manager/` edits).

---

## Blessing

> architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # blessing

**BLESSED** (round 1; consult-fork of the standing milestone-architect handle, `--model fable`). The architect verified the plan against all four live build surfaces and confirmed: both design decisions (archive() refactor; `readdirSync`+regex glob) are sound and sit inside latitude the spec explicitly left; the sweep is correctly additive/never-a-failure and excludes `tool-results/` structurally; the BASELINE + `/initialize` double-anchor is complete (the § Expectations "Settings" bullet closes a gap the spec didn't enumerate); diff scope is clean with no canon/milestone-doc edits. **No human design decision required.**

Two non-blocking advisories carried to the implementer/self-test:
1. Self-testing against `7f0fbcc4-…` also upserts that session's main-transcript archive — harmless by design (last-write-wins; transcripts only grow) and desirable (makes the M1 probe tree the first whole-tree archive).
2. JSDoc hazard is specifically the 2-char sequence `*/`; `agent-*.jsonl` is safe, a leading-`*` path glob is not — verify with `bun run` per the footgun.

---

## Code Review

> reviewer@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

**Pass 1 — clean, converged. Zero findings (blocking or non-blocking).** Fresh reviewer verified every acceptance criterion statically plus the parse/no-args smoke checks:

- settings.json: `env` block correct, JSON parses, `permissions`/`hooks` untouched.
- BASELINE.md: settings-skeleton clause names the key + cap/pin rationale, placed before the carve-out; § Expectations "Settings" bullet carries the audit anchor.
- initialize.md: new dedicated check #4 "Spawn-depth cap" (value-pin `== "2"`, not a behavioral probe); tail renumbered 5-9, no gaps/dupes; Report section format unaffected.
- session-archive: `archive(srcPath, rel)` used byte-identically at both main-transcript call sites; `dirname`/`readdirSync` imports correct; `sweepSubagents` reads only `<uuid>/subagents/` (regex-filtered) → `tool-results/` structurally excluded (no recursive walk); additive/never-a-failure (early return on absent dir, never touches `failures`, both exit contracts unperturbed); JSDoc header clean (no stray `*/`); no-args prints usage + exits 1 under both `bun run` and direct invocation.
- Diff scope clean: only the four `.claude` files + the work order; milestone doc unchanged; no `executor/`/`manager/` canon edits.

Matches the blessed decision points (refactor-not-sibling; `readdirSync`+regex) and respects the JSDoc footgun. No resolution pass required.

---

## Progress

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

- 01:21 manager (automatic mode) entered; build surfaces + fixtures verified (primary self-test fixture `7f0fbcc4-2131-4fdd-8f57-714beedc5cb1` has live multi-depth `subagents/agent-*` files)
- 01:21 planner dispatched (step 5)
- 01:23 blessing: BLESSED (round 1, consult-fork of milestone architect, fable); no novel decisions
- 01:23 implementation started (steps 8-9): 2 parallel implementers dispatched (config+docs cluster; session-archive sweep)
- 01:25 config+docs cluster landed: settings.json env key (JSON parses), BASELINE ×2, initialize.md new check #4 (tail renumbered 5-9), operator recipe written
- 01:26 sweep code landed: archive(src,rel) refactor + sweepSubagents helper wired at both call sites; JSDoc clean; no-args exits 1
- 01:26 review pass 1 dispatched (fresh reviewer)
- 01:31 review pass 1: 0 findings — converged (no resolution pass)
- 01:35 self-test run (LAST, all specialists dispatched): 7f0fbcc4 → 7 subagent pairs archived; 76ba13c6 → tool-results exclusion proven (0 tool-results paths in store); 2929edd2 → no-subagents clean archive (exit 0, no subdir, then reverted); no-args exit 1; sha256sum -c MANIFEST exit 0
- 01:36 quality gate: reference-resolution clean (only sanctioned src/_archive/ absence); tooling smoke all pass; /initialize settings checks pass (check #4 == "2", hooks resolve, baseline integrity)
- 01:37 commits: a8c63c7 (env cap config+docs), 861d920 (sweep code + demonstrated store output)
- 01:37 Acceptance Verdict written → PASS

---

## Acceptance Verdict

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

**PASS.** All blocking acceptance criteria met; blessing clean (round 1, no novel decisions); review converged with zero findings on pass 1. Commits: `a8c63c7`, `861d920` (code + demonstrated store output), plus the `Document:` commit carrying this verdict.

Per-criterion status:

| # | Criterion | Status | Evidence |
| --- | --- | --- | --- |
| 1 | `settings.json` `env` block with `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH: "2"`, valid JSON, existing keys untouched | **PASS** | `JSON.parse` → `ok`; third top-level sibling after `hooks`; `permissions`/`hooks` byte-identical |
| 2 | BASELINE settings-skeleton clause names key + cap/pin rationale; `initialize.md` audit line in house style; `/initialize` settings checks pass | **PASS** | BASELINE: clause before the carve-out + § Expectations audit anchor (2 occurrences); `initialize.md` new dedicated check #4 (value-pin, tail renumbered 5-9); check #4 verify → value `"2"` PASS, hooks resolve, baseline integrity clean |
| 3 | `session-archive` sweep produces `<machine>/<uuid>/subagents/agent-*.jsonl.gz` (+`.meta.json.gz`) with MANIFEST lines; `sha256sum -c MANIFEST` passes; `tool-results/` not swept; no-subagents session archives cleanly | **PASS** | `7f0fbcc4` → 7 `.jsonl.gz` + 7 `.meta.json.gz` (15 MANIFEST lines incl. main); `sha256sum -c` exit 0, 0 FAILED; `76ba13c6` (has `tool-results/`) → only 2 subagent pairs archived, **0** `tool-results` paths in store; `2929edd2` (live main, no `subagents/`) → exit 0, main-only, no subdir |
| 4 | Tooling smoke: no-args prints usage, exits 1 | **PASS** | `session-archive`/`consult` no-args exit 1; `commit-guard` empty-stdin exit 0; all quality-gate smoke checks pass |
| 5 | *(operator-gated)* interactive no-var default observation recorded | **operator-gated** | Concrete recipe staged in `## Operator recipe`; non-blocking per Executor override — records into the milestone doc, either outcome |
| 6 | Diff scope: only the four `.claude` files + milestone docs + work order; no `executor/`/`manager/` canon edits | **PASS** | `git status` = the 4 `.claude` files + work order + sanctioned `work/sessions/` sweep output; milestone phase doc unchanged; no canon prose moved |

**Downstream premise left true:** the settings cap and the whole-tree archive sweep now exist as tested facts — phase 2's canon rewrite can cite them.

**Residuals (non-blocking):**
- The operator-gated interactive no-var default check (criterion 5) is staged, not run — it is headless-uncertifiable by design and does not block phase close. Recipe in `## Operator recipe`; result records into `work/milestones/2.conversion/1.mechanics.md`.
- Milestone reconciliation (MILESTONE.md phase-1 status; footgun ledger — no new footgun surfaced) is the parent Executor's to make, not this single-order Manager's.
