---
type: chore
difficulty: high
sessions:
  - executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # campaign executor
---

# Execute: M3 — validation and fold

## Objective

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Drive [`work/milestones/3.validation-and-fold/MILESTONE.md`](../milestones/3.validation-and-fold/MILESTONE.md) to acceptance: complete the mission's criterion-3 evidence, land the enactment filing and the D4 doctrine rewrite + `.claude/ADR.md`, and stage a rehearsed fold recipe so the operator's `/close` runs as pure ceremony.

**Phases and dependency order** — strictly serial, each `depends-on` its predecessor:

| # | Phase | difficulty | depends-on |
| --- | --- | --- | --- |
| 1 | [validation-and-enactment](../milestones/3.validation-and-fold/1.validation-and-enactment.md) | low | — |
| 2 | [doctrine-and-adr](../milestones/3.validation-and-fold/2.doctrine-and-adr.md) | high | 1 |
| 3 | [fold-preparation](../milestones/3.validation-and-fold/3.fold-preparation.md) | high | 2 |

No phase parallelism is available: phase 2 cites the proposal at its post-move `_enacted/` path, and phase 3's manifest and rehearsal run against phase 2's final `main`-bound text.

**Autonomy scope** *(confirmed at the gate, 26-07-24)*: **the fold executes.** Phases 1–3 and their acceptance are unchanged and M3 closes criteria-closed; then the ceremony runs — operator judgment + closing report + tag → fold per the rehearsed recipe → verification → push — with this Executor as its mechanical arm. The closing judgment and the criterion-3 attestation remain the operator's and are not transferable. See the `MILESTONE.md` re-scope note and the Decisions block below.

**Campaign as instrument** — this campaign runs under the converted boundary (subagent Managers, `model: "opus"` per call, operator watching the panels) and is itself the remaining validation evidence for mission criterion 3.

---

## Validation evidence (criterion 3)

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2
> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Opened by phase 1, composed by its Manager and inserted here by the Executor (the Manager never writes to this log). Upward leg fills at phase 3; the attestation is the operator's.

**Downward leg — the nudge. SATISFIED, 26-07-24 (M2 phase 2).** Nothing re-run; the event was already on the record. From [`_done/26-07-24.execute-conversion.md:98`](_done/26-07-24.execute-conversion.md), verbatim:

> **Verdict-absence event (dogfood signal for M3):** the Manager returned **without a verdict** — it parked on a background resolution implementer and ended its turn (bug cause 4, which the bug order predicted survives the conversion; the notification confirmed no live child). Files 1–6 landed grep-clean (I verified: banned tokens 5× in `executor/CLAUDE.md` 28–49 only, 0 elsewhere) but uncommitted, bug order unclosed, no gate/verdict. Applied the 7.3 case-(c) remedy: **SendMessage nudge** instructing direct final-mile completion (close bug order, gate, commit, verdict) — no further dispatch, no parking. The completed subagent **auto-resumed from transcript with context intact** (M1 probe-2 behavior confirmed in anger). The nudge mechanism this phase is landing thus validated *during its own construction*.

A downward nudge in the criterion's sense — Executor → Manager, mid-campaign, context intact on resumption — issued against a live campaign, not a probe. The summary-level echo at `:140` carries the same finding at campaign scale.

**Upward leg — pause-and-ask. SATISFIED, 26-07-24 (M3 phase 3).** The phase-3 Manager reached the point where the fold recipe needed its shape, **ended its turn with the question**, and held context. The Executor deliberated and answered by SendMessage; the Manager resumed in place from its transcript and continued to its verdict. The decision was the Executor's, never the Manager's.

**The question, as the Manager put it.** True merge (honest shared ancestry, mission blobs reachable from `main`'s history) versus squash-fold (one synthetic commit, slim history, no shared ancestry). It brought three things the Executor did not have:

- **Corrected facts.** The work order's numbers were stale — `main..HEAD` is **63 commits**, not 49; `work/sessions/play/` is **6,045,725 B across 71 blobs**, not 3.7 MB across 37; `.git` is **12 MB** with 924 loose objects, never gc'd. Phase 1's backfill roughly doubled both axes after the figures were written.
- **Confirmed decision 1, falsified.** `git merge-tree --write-tree 26-07-23 main` now exits **1** — a genuine two-region content conflict in `.claude/BASELINE.md`, created by *phase 2's own* BASELINE edits and absent at reconnaissance. The Executor verified this independently. **The pre-flight re-run amendment is what caught it** — written at the gate precisely because phase 2 would touch that file again. The amendment did its job.
- **Disciplined framing.** The Manager staged a guarded resolution, then explicitly refused to let the conflict steer the architectural choice ("choosing squash *because* of the conflict would let a solved mechanical cost steer an architectural choice"), and declined to invoke the D4-purity escalation trigger reflexively when it judged the fork had not been crossed — flagging the boundary rather than claiming it.

**The Executor's answer: true merge, `--no-ff`.** What decided it was remote topology neither party had, checked at answer time: `origin` carries **only `main`**; the mission branch has never been pushed; there are **no tags on the remote at all**. Since the continuation protocol requires the tag be pushed and never deleted, and a default `git clone` fetches all tags, pushing `v1.subagent-boundary-closed` puts all 63 commits' objects on origin under *either* shape. **Squash therefore buys a cosmetically cleaner `git log main` and not a lighter clone.** The trade reduced to honest ancestry plus main-reachable objects versus a tidier log, at identical clone weight — and merge fails safer, since a lost tag orphans nothing.

**The escalation trigger was considered and not fired, deliberately.** The Executor's deliberation did not turn on whether D4 means tree- or history-purity, because squash cannot deliver history purity here while the protocol demands a pushed tag. A charter question needs the operator when the answer would change the outcome; this one would not. Recorded explicitly so the record shows the fork was tested, not dodged.

**Why this is the criterion in its strong form.** A Manager holding a decision it was capable of making, ending its turn to ask, surviving with context intact, and resuming on an answer that arrived by SendMessage — with the answer materially different from what the Manager would have chosen alone, since the deciding fact lived outside its remit.

**Operator panel attestation — SATISFIED, 26-07-24.** Authored by the operator, unprompted and undrafted, during phase 3's live run. Recorded verbatim:

> Confirmed: depth-1 and depth-2 transcripts are streaming perfectly now. I have been witnessing this as we are currently using the system, and it's working great!

Given while the phase-3 Manager was live on the panel with its own specialists beneath it — observation of the running system, not a retrospective judgment of a record. It attests the load-bearing claim: the native panel carries live transcripts at **both** depths, which is the observability inversion the whole boundary conversion rests on and the thing the retired jsonl-periscope apparatus existed to fake.

**Provenance discipline, recorded because it matters more than the line.** One earlier operator sentence — *"The phase panels are working great now as far as I can tell"* — was **declined** as the attestation. It was a preface to witnessing rather than the witnessing, and the mission architect's ruling is that the attestation is an explicit operator-authored utterance, never inferred from adjacent approval. The Executor also declined to draft a candidate line, since a draft sitting in this slot is exactly what could later be misread as the operator's. The slot stayed open until the operator authored it themselves.

---

**Criterion 3 is complete.** All three legs satisfied in anger during a campaign the criterion governs: the downward nudge (M2 phase 2, cited; corroborated by a second independent instance in M3 phase 2), the upward pause-and-ask (M3 phase 3, above), and the operator's live-panel attestation. No leg was staged, simulated, or backfilled.

---

## Reconnaissance

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Three researchers, one per phase, read-only, dispatched in parallel 26-07-24. Consolidated below; per-phase readiness, then the deduplicated concern list, then the Decisions block.

**Per-phase readiness**

| Phase | Ready? | Blocking finding |
| --- | --- | --- |
| 1 | Ready with two spec corrections | Zero-LOST acceptance is unsatisfiable as written (three handles predate the archive convention); MISSION.md diff-scope line contradicts its own scope text |
| 2 | Ready | Source material located and enumerated; BASELINE gaps are four, not the two the spec names |
| 3 | Ready, premise corrected | `b332083` **does not conflict** — `git merge-tree` resolves clean; the recipe's "pre-resolved instruction" is unnecessary |

### Concerns, prioritized

**C1 — Three session handles are permanently LOST (phase 1, highest severity).** The stamp enumeration yields 24 handles / 12 distinct UUIDs. Seven are archived; two (`2f2dc1c5…`, `a52d1822…` — the M2 and M1 Executor sessions) are backfillable from live transcripts. Three have **no source transcript anywhere**: `86ade408-7a9b-4edd-991a-6b3592ce377a` and `1063f991-16dd-4fef-bc25-ba8c37c9dd2c` (stamps inside the 26-07-05 founding series, predating the 26-07-11 adoption of archive-on-stamp — precisely the CLI-retention rot the durable-resumability proposal named) and `3816fb23-acef-4f71-b79f-ce68441a6b21` (stamped in the premature-exit bug order, relayed from Orchestra; never ran with this repo as `cwd`). `sha256sum -c MANIFEST` passes (37 lines, zero failures). Phase 1's acceptance line "zero `LOST`" cannot be met by any action.

**C2 — Phase 1's MISSION.md diff scope contradicts itself.** Scope says flip M2 → done *and* verify M3 → in progress; acceptance says "M2 row reads `done (26-07-24)`; **no other MISSION.md text changed**." Both rows read `pending` today. Separately, `work/milestones/3.validation-and-fold/MILESTONE.md:9` carries a second copy of M3's status (`pending`) that falls out of sync the moment the roadmap row flips.

**C3 — The `git mv` breaks two live inbound links outside phase 1's stated diff scope.** `work/MISSION.md:11` (a markdown link to the proposal — and MISSION.md's charter prose is otherwise protected from edits) and `work/milestones/1.spike/MILESTONE.md:14` (M1's doc, not "this milestone's docs"). Two further references sit in `work/todos/_done/` and are historical-never-rewritten by convention — correctly left to go stale.

**C4 — `b332083` is not a conflict (phase 3 premise falsified, good news).** `git merge-tree --write-tree 26-07-23 main` exits 0 with three clean auto-merges (`BASELINE.md`, `commands/archive.md`, `commands/initialize.md`); the commit's touched lines are disjoint from the branch's 49 commits. `HEAD..main` contains only that one commit, so there is no other conflict surface. Caveat: phase 2 edits `BASELINE.md` again — re-run `merge-tree` at phase 3 execution, not just trust this snapshot.

**C5 — `work/PROJECT.md` is the rehearsal's most likely red.** It is referenced by **17 files** under `.claude/`, it strips in purification, and it is **not** on the sanctioned-absence list in its own quality gate. `work/sessions/MANIFEST` is a smaller instance of the same gap (referenced by `initialize.md`, which treats it conditionally, but the blunt grep does not). Compounding: the quality gate's definition *lives in* `work/PROJECT.md`, which the fold strips — so the rehearsal's sanctioned-absence list must be written into the runbook itself, not assumed resident on the purified tree.

**C6 — `/initialize` is *supposed* to fail on a purified tree.** Check 5 (keystone docs) requires `work/PROJECT.md`, which the bootstrap procedure authors at step 3, *after* `/initialize` first flags its absence. Phase 3's acceptance ("`/initialize` all passed") must mean "failed only at the named, sanctioned gaps" or it blocks on its own correctness.

**C7 — Two empty containers would vanish from git at purification.** `work/proposals/_enacted/` and `work/todos/_done/` have no `.gitkeep` (they are non-empty today, so none was needed). `_deferred/`, `_rejected/`, `_cancelled/`, `bugs/` already carry one. The purification commit must *add* two `.gitkeep` files, not merely `git rm` — a mechanical step absent from the phase-3 recipe.

**C8 — BASELINE's scaffold enumeration has four gaps, not two.** Step 2's brace expansion omits `sessions/` **entirely** (directory and README), and lists `missions/`, `notes/`, `runbooks/` bare — so `missions/README.md`, `notes/README.md`, `runbooks/README.md` are unenumerated too, though all three are tracked. `work/sessions/MANIFEST` and `work/PROJECT.md` appear nowhere in BASELINE. The machine-registry adaptation *is* stated (`BASELINE.md:38`) but says "replace the row(s) with the new project's machine(s)" — not a placeholder row.

**C9 — `.archive/README.md` is an unmanifested product orphan.** A 190-byte generic convention statement ("repo-local cold storage… not part of the bootstrap"), added in the founding commit `5adad6a`, never touched since, zero project facts. Reads as product — but it is absent from `.claude/BASELINE.md`'s manifest entirely.

**C10 — Two strip-list paths are unnamed by phase 3's prose.** `work/proposals/26-07-05.agentic-workflow/` (the non-underscore standing remainder — root `CLAUDE.md` names it as instance, but the strip enumeration names only `_enacted/26-07-05…` and `_deferred/`), and this campaign log itself (untracked today, a dated todo once committed).

**C11 — Live hooks would mutate the rehearsal tree.** `.claude/settings.json`'s `SessionEnd` archive hook writes a transcript into `work/sessions/` and the `Stop` commit-guard hook can block/prompt — both fire if a live Claude session runs inside the disposable worktree, corrupting the very file listing the rehearsal diffs against BASELINE.

**C12 — The acceptance grep flags legitimate generic prose.** `README.md:10` uses `work/sessions/` to describe the callback-grammar archive mechanism as *architecture*, not as an instance pointer. It must be rephrased (point at `work/README.md` § The callback grammar) purely to satisfy a literal-string check that cannot distinguish convention from instance.

**C13 — The ADR's continuation-protocol exemplar cites a tag that does not exist.** `git tag -l` is empty; `v1.subagent-boundary-closed` is created by the *operator's* `/close`, after M3. The ADR must phrase it prospectively, not as a runnable example.

**C14 — Campaign-log authorship.** Phase 1's scope instructs the Manager to "open a section in this campaign's log" — a document the Executor solely authors and concurrently edits. **Executor ruling:** the Manager returns its criterion-3 content in its work order; the Executor appends the section here, stamped `> manager@play/<executor-session-id>` per "the role names the act." No Manager writes to this file.

### Merge-vs-squash — facts assembled, decision deliberately withheld

Phase 3's Executor-held item is the fold's presentation on `main`. Recon assembled the facts, did not decide: `main..HEAD` is **49 commits**; `work/sessions/play/` is **3,734,389 bytes across 37 blobs** (largest ~799 KB); whole `.git` is 8.5 MB; and **no consuming-repo workflow depends on ancestry** — adoption is copy-based per BASELINE's bootstrap, never an upstream merge. The phase-3 Manager will pause-and-ask; the answer is the Executor's, and the exchange is the criterion-3 upward leg.

The milestone architect added the one residue those facts cannot settle: phase 3 was written reading D4's "instance content leaves `main`" as **tree**-purity, history carrying the blobs being fine. A **history**-purity reading of D4 materially favors squash — and that is a charter-interpretation fork, not a fact question. **If the deliberation turns on which purity D4 means, that single question escalates to the operator.** Either shape preserves the continuation protocol provided the tag is pushed and never deleted.

### Decisions confirmed (Reconnaissance gate, 26-07-24)

Operator-answered at the gate, then blessed by both architect handles where the answer expanded scope.

| # | Decision | Disposition |
| --- | --- | --- |
| 1 | Autonomy scope | **The fold executes** — operator authorized, both architects blessed, shape (b). See the `MILESTONE.md` re-scope note. Phases 1–3 and their acceptance are **unchanged**; M3 closes criteria-closed; then ceremony → fold → verification → push, Executor as mechanical arm. |
| 2 | Three unrecoverable handles (C1) | Recorded as `LOST (pre-existing)` with causes; acceptance amended to zero *new* LOST; surfaced in the closing report's reader index. |
| 3 | Broken inbound links (C3) | Both repaired to the `_enacted/` path; phase 1's diff scope widened to include `work/MISSION.md` and `work/milestones/1.spike/MILESTONE.md`. The two `_done/` references stay stale by convention. |
| 4 | Panel attestation (criterion 3) | Operator attests **during** the campaign, watching the phase panels. Must land **before** the ceremony opens. |
| 5 | MISSION.md diff scope (C2) | "No other MISSION.md text changed" guarded charter prose, not the roadmap table — both rows flip; milestone status blockquotes track. |
| 6–13 | C5–C13 | Executor rulings, recorded in the per-phase amendment sections: `.archive/README.md` keep + manifest; `.gitkeep` generalized to any emptied keep-column directory; rehearsal green condition written four-part; `/initialize` a manual walkthrough with an expected-outcomes table, no live session in the worktree; ADR tag citation prospective; `README.md:10` rephrased; all four BASELINE gaps closed under a set-equality invariant; unnamed strip paths written in. |

**Two protections are not transferable** (mission architect, hard line): the **closing judgment** — `accomplished | failed | superseded` rendered by the operator as an explicit utterance, recorded verbatim, never inferred from "proceed" or silence; the Executor drafts, the operator renders. And the **attestation** — operator-authored; if it does not arrive, criterion 3 is incomplete and the report says so. *An instrument cannot certify its own reading*; the archive must not teach future missions that campaigns may certify themselves. Any **history-rewriting operation on `main`** would need its own fresh authorization.

**Recorded honestly**: the downstream driver is softer than stated. Orchestra is already bootstrapped; what it awaits is the updated `.claude/` manifest, which adoption-by-re-copy can take from this branch today. The purified `main` makes that canonical and diff-friendly; it does not gate it. A red rehearsal may therefore delay the fold at no cost to Orchestra.

**Consults**: `architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d` (mission architect — scope-expansion blessing, the non-transferable protections, shape (b)); `architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2` (milestone architect — per-finding amendments, the set-equality invariant, the rehearsal's green condition, the D4-purity escalation trigger).

---

## Phase Ledger

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

### Phase 1 — validation-and-enactment · **PASS**, ultimate acceptance holds

Manager subagent `a9c279b8d00afc6a0` (`model: opus`), work order [`26-07-24.validation-and-enactment.md`](26-07-24.validation-and-enactment.md). One dispatch, zero follow-ups — outer pass budget untouched. Commits `142b70e`, `eb52dc8`, `fce3a3c`, `3a86445`.

**Landed.** Proposal `git mv`d to `work/proposals/_enacted/` (true rename, 100% similarity, no copy at the old path — the first flat file there), `status: enacted (26-07-24 — mission v1 M2 conversion; validated M2–M3)`, closer's stamp, three-paragraph prose reconciliation note in final position covering the premise-1 inversion, premises 2–5, and the in-anger validation. `MISSION.md` both roadmap rows plus the line-11 link; `1.spike/MILESTONE.md` line-14 link. 34 backfilled session files, MANIFEST a pure upsert, 71/71 OK.

**Ultimate acceptance.** Both downstream premises this phase owed are true: the proposal sits at its `_enacted/` path for phase 2's ADR to cite, and the archive store is verified whole before phase 3 stages a ceremony that `git mv`s it. Advance.

**The one override of supplied text, accepted.** Scope item (c) told the Manager to write that cause 4 survived conversion "exactly as the proposal's fallback reasoning anticipated." The reviewer traced it and found it unsupported — the proposal never discusses verdict-absence or a Manager parking on a completed background child; its fallback reasoning concerns the spike failing. The prediction is the **bug order's** (`_done/26-07-23.bug.headless-manager-premature-exit.md`, frontmatter `:8`, assessment `:66`); the remedy is the proposal's § design item 3. Credit split accordingly. The Manager treated it as mechanical rather than escalating, on the grounds that it corrects an attribution in a permanent record toward what the record supports without touching any decision — **correct call**, and the error was mine: I carried that phrasing from the phase doc into the work order without tracing it. A related fix: the note's "vindicated a third time" was counting a step that never happened, since one prior instance was the very claim M1 falsified — arithmetic dropped, point kept.

**Scope-boundary call, upheld.** The Manager fixed `MISSION.md:11`'s link *label* as well as its target. A label displaying a path that no longer exists is the same defect the repair was authorized to fix. "The adopted proposal" on that line stays — charter prose, historically accurate at chartering.

**Three findings that route forward, not residuals:**

1. **The handle count is 26/13, not recon's 24/12.** The delta is this campaign's own `executor@`/`manager@2bd6ea6d…` stamps, written after recon ran. The 13th UUID is this live Executor session — not a fourth loss; archive-on-stamp collects it at `SessionEnd`. **The ceremony must expect 26/13 and re-run the sweep post-close** to confirm it landed. Carried into phase 3's pre-flight archive-sweep delta.
2. **`/initialize` check 2 fails on two pre-existing host-environment gaps** — `chrome-devtools` MCP configured but not connecting (`-32000: Connection closed`), and `codebase-memory-mcp` not installed. Neither touches `.claude/` or repo state. **Phase 3's expected-outcomes table must list these as environment noise** on top of the sanctioned pre-bootstrap failures, or the rehearsal reads them as reds it caused.
3. **Two bugs filed, both routed rather than deferred.** `bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md` — `work/PROJECT.md:9` omits `src/_archive/`, which `BASELINE.md:38` mandates gates sanction; a **fourth member of C5's list**, found after C5 was written, and since `PROJECT.md` is stripped by the fold it routes to **phase 3's runbook**. `bugs/26-07-24.bug.initialize-command-genericity-leak.md` — `.claude/commands/initialize.md:13` carries project facts from a *different* project ("ephemeris data, catalogs, vendored clones"); a genericity violation in the product by this repo's own acceptance test, which survived M2's coherence sweep because that sweep hunted dispatch tokens, not project facts. Routes to **phase 2**, already editing product docs.

### Phase 2 — doctrine-and-adr · **PASS**, ultimate acceptance holds

Manager subagent (`model: opus`), work order [`26-07-24.doctrine-and-adr.md`](26-07-24.doctrine-and-adr.md). One dispatch **plus two Executor follow-ups** — a verdict-absence nudge and an information relay; outer pass budget 2/2 consumed, nothing left before escalation had a third been needed. Commits `668e78e`, `cba0711`, `d914146`, `6ef32bb`, `05211b2`.

**Verified by the Executor, not taken on report**: tree clean; `668e78e` contains exactly `ADR.md` + `BASELINE.md` (the same-commit rule held); the acceptance grep returns **0** on both root docs; `grep -n 'yarrow\|/home/' .claude/ADR.md` empty; ADR is **73 lines** against a 150 ceiling.

**Landed.** `.claude/ADR.md` — hybrid of the three house formats, with the two-audience boundary structural rather than annotated: a single trailing `## Template repository only` section holding the continuation protocol, mission ledger, and the founding consult handle. Reviewer checked leakage in both directions, found none. Root `CLAUDE.md`/`README.md` on D4 doctrine. BASELINE reconciled — **seven** scaffold gaps closed under the set-equality invariant, not the four recon found: the four known READMEs plus `.archive/README.md` plus `docs/README.md` and `docs/research/README.md`. The last two matter beyond bookkeeping: `/research` writes into `docs/research/`, so their absence was a latent reference-gate failure in *every* bootstrap.

**Ultimate acceptance.** Phase 3's premise holds: the rewritten root docs and the ADR are final `main`-bound text, and no doctrine authoring remains in phase 3 — with one bounded exception I am adding deliberately (below).

**An unplanned save.** Mid-implementation the Manager found the reference gate extracts the ADR's `git show <tag>:<path>` provenance strings as *live tree references* — three of them are purification strips that never exist on `main`. **The rehearsal would have gone red**, and the cause would have looked like a phase 3 defect. It escalated, the architect ruled decision 3's logic generalizes, and it was fixed syntactically (consume the expression) rather than by per-path exemptions, which rot. This is the reconnaissance C5 concern arriving from a direction nobody predicted.

**The sweep's negative result, which is the point of asking for it.** All 24 tracked files under `.claude/` covered. One sibling leak beyond `initialize.md`: `close.md:17` hardcoded `git grep -h '@play/'` — this repo's machine name inside a recipe that must span every machine in the registry. Also `~/dev/claude-workflow` removed from `BASELINE.md:3`, the one file copied byte-identical into every clone. **Beyond those: nothing of that shape anywhere in the product.** Every `work/…` reference is a convention pointer, not an instance artifact; the `bin/` scripts derive machine and repo root dynamically. The product docs are clean for the fold — which is exactly what phase 3 needed established before staging a purification.

**The reviewer finding the Manager reclassified, and I uphold.** Eight product files still instruct "reconcile the CLAUDE.md index," a convention this phase removed from *this* template's root doc. The Manager declined to treat it as blocking and filed [`bugs/26-07-24.bug.footgun-index-convention-drift.md`](bugs/26-07-24.bug.footgun-index-convention-drift.md) instead. Correct: BASELINE step 3 makes root `CLAUDE.md` consuming-repo-authored, so those eight instructions remain **generically true** — a consuming repo may well index its ledgers there. What changed is only that the template's own exemplar no longer demonstrates it. Whether a purified template should model that convention at all is a design question touching eight agent policy docs; making unchartered doctrine edits at phase end to settle it would have been the worse error. Carries to close triage.

**A standing hazard discovered in anger — routed to phase 3 as a bounded exception.** Parallel dispatch: the disjoint file sets held *between specialists* and broke at the **manager/specialist boundary**. Operating under my own final-mile instruction, the Manager edited `work/PROJECT.md` directly while a continued phase-C implementer was still live on that file. It surfaced only because the implementer's `Edit` hit a stale `old_string` — a silent lost update was possible, and it ended well through the specialist's judgment (it verified and diffed rather than overwriting), not through design. The rule the Manager drew is right and worth preserving: **dispatching an agent onto a file transfers ownership until it reports, and a final-mile exception does not implicitly revoke that** — either wait, or cancel the continuation explicitly. My own nudge phrasing invited it, so the hazard is partly mine. This belongs in ADR § Standing hazards, whose whole purpose is hazards that outlive a mission; left in the campaign log it archives with the branch and is lost. Phase 3 appends the one entry — the single sanctioned exception to "no doctrine authoring in phase 3," justified because the hazard was discovered *after* phase 2 closed.

**Second in-anger instance of the nudge mechanism.** Phase 2 stopped short of its verdict exactly as M2's phase 2 did — parked on a completed child, no question stated. Same remedy, same clean auto-resume from transcript with context intact, in a different campaign from the one that landed the mechanism. Corroboration for the criterion-3 downward leg, which was already satisfied.

### Phase 3 — fold-preparation · **PASS**, ultimate acceptance holds

Manager subagent `aa06ec46dc3d559c3` (`model: opus`), work order [`26-07-24.fold-preparation.md`](_done/26-07-24.fold-preparation.md). One dispatch + two Executor follow-ups (the pause-and-ask answer, one verdict-absence nudge) — outer pass budget 2/2. Commits `295749e`, `dd4ac33`, `c741d9d`, `f4edcce`, `7b4c4ff`, `9253d69`.

**Verified by the Executor, not taken on report**: tree clean; `main` still at `b332083`; `git tag -l` empty; `git worktree list` a single entry; `work/runbooks/fold-and-close.md` at 723 lines.

**Landed.** Purification manifest — 191 tracked paths, 62 KEEP / 129 STRIP, zero unclassified *by construction*; set-equality with BASELINE's 18-path `work/` enumeration confirmed both directions. Rehearsal green **twice** (`IDENTICAL — 18` hard-green, `IDENTICAL — 64` whole-tree, 7 sanctioned unresolved, 4/4 tooling, `/initialize` green-or-expected-red), worktree removed. The full 723-line runbook with both helper scripts verbatim, the expected-outcomes table, and the sanctioned-absence list inline. The one sanctioned ADR doctrine edit (4 lines, generic, gate-clean). Sanctioned-absence bug closed; a divergence bug filed and carried.

**Ultimate acceptance.** The ceremony's premise holds: zero mechanical work remains inside it — judgment, report, tag, staged fold. Criterion 3 is complete. Nothing was folded outside the rehearsal.

**The finding worth more than the artifact.** Rehearsal run 1 **passed every visible check** while carrying a defect that would have put `work/missions/v1.subagent-boundary/**` into a purified `main`: the keep list derived the `work/` scaffold by grepping `README.md` under `work/`, which matches the mission archive's own READMEs. The archive is `git mv`'d in by the ceremony, **upstream of the fold** — so it existed in no tree the rehearsal could run against. No amount of running the rehearsal catches it; it was caught by reasoning about the ceremony-time tree and only then proven by simulating the archive move. The generalization, now written into the runbook:

> **Derive keep-column membership from the specification, never from the tree.** A tree-derived pattern answers "what looks like scaffold *here, now*"; a specification-derived list answers "what scaffold *is*" — and only the second is stable under a tree you cannot predict.

Symmetrically, the strip list is derived and never transcribed, because it must absorb what arrives late. The confirmation run was the empirical check: tracked paths moved 188 → 191 and strip 126 → 129 with **keep unchanged**, exactly as predicted.

**Two corrections the Manager made against itself, both left visible.** It first recorded the review pass as never returning, having read a stalled output-file size as a stalled agent — *file-size polling is not a liveness check*. The review had in fact completed with seven non-blocking findings. It corrected rather than overwrote, on the reasoning that a verdict which under-claims and then corrects is recoverable while one that over-claims is not. Separately, its own reviewer independently reproduced the extended merge proof in a throwaway worktree and proved the ADR entry gate-neutral (extraction set byte-identical at 6 paths across `295749e^`/`295749e`) — recomputation rather than trust, twice over.

**Two review findings that mattered for the ceremony specifically.** A prose/result mismatch in an assertion would have given an operator reinventing the grep a **false red on a gate whose rule is "any deviation stops the ceremony"** — the literal anchored command is now written in. And confirmed decisions 1 and 7 sat under a header claiming they govern, with no marker that two of eight had been superseded 150 lines below; a reader acting on decision 1 as written would have halted a ceremony engineered to hit exactly one staged conflict. Markers added additively, so the never-rewrite convention holds.

**Fourth in-anger instance of the file-ownership hazard.** The reviewer hit it while this phase was codifying it — the work order grew 412 → 445 lines underneath it because the Manager was editing concurrently. The reviewer read only and said so. That the hazard fired against the very phase writing it into the ADR is the strongest argument that the entry earns its place.

---

## Summary

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

**M3 closes criteria-closed, 26-07-24.** Three phases, all PASS, all ultimate-acceptance-clean. Four Executor follow-ups across the campaign (one continuation, three verdict-absence nudges); no phase exhausted its budget; nothing escalated to the operator that the confirmed decisions did not already cover.

**What landed.** Criterion 3 complete in all three legs. The proposal enacted at its `_enacted/` path with a reconciliation note recording the premise-1 inversion. `.claude/ADR.md` — the decision register, 73 lines plus the standing-hazard entry — with the root doctrine rewritten to D4 and BASELINE reconciled under a set-equality invariant. A 191-path purification manifest, a fold recipe rehearsed green twice in disposable worktrees, and a 723-line close-and-fold runbook carrying both helper scripts byte-verbatim.

**What the campaign proved beyond its own deliverables.** It ran under the boundary it was validating, and the boundary held — but the interesting evidence is where it strained:

- **Verdict-absence is a reproducible property, not an anecdote.** Three stops across two campaigns (M2 phase 2, M3 phase 2, M3 phase 3), one consistent trigger: *a Manager dispatches a child near the end of its work and treats the dispatch as completion*. The phase-3 Manager added the sharpest detail — it had genuinely correct reasons to wait (the runbook was another agent's file, and editing it would have violated the ownership rule that phase was codifying), and **the correctness of the reason to wait made the parking feel like discipline rather than a stop**. The nudge remedy worked all three times: clean auto-resume from transcript, context intact. Deliberately **not** written into the ADR — the phase-3 doctrine edit was bounded to one entry and I will not widen it after close. It routes to a future mission as a prepared disposition.
- **File ownership across the manager/specialist boundary fired four times**, the fourth against the very phase writing it into the ADR. That entry earns its place empirically, not by argument.
- **Rehearsal has a structural blind spot.** Run 1 went green on every visible check while carrying a defect that would have smuggled the mission archive into a purified `main`. The lesson generalized into the runbook: derive keep-column membership from the specification, never from the tree.
- **Agents corrected their own records rather than overwriting them** — the review-did-not-return correction, the cause-4 attribution, the "vindicated a third time" miscount. Each left visible.

**Where I was wrong, for the record.** I carried the cause-4 attribution into phase 1's order without tracing it, and phase 1's reviewer caught it. My phase-3 order's facts were stale (49 commits / 3.7 MB against a live 63 / 6.0 MB) because phase 1's own backfill had doubled them underneath me. My final-mile nudge phrasing in phase 2 invited the file-ownership collision it then had to record. None changed an outcome; all three were caught downstream, which is the system working, but they were mine.

**Deferred by design.** Residue R1–R6 (six references in keep-column files to stripped or instance-specific material, invisible to a gate that greps `.claude/` only); the `sessions:`-not-derivable-from-body-stamps convention gap contradicting `work/README.md:91`; the footgun-index convention drift across eight agent policy docs; the BASELINE postdate-list divergence. All four are design calls routed to a template-genericity pass on a future mission, with prepared dispositions in the runbook's triage section.

---

## Operator Handoff

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

M3 is closed. **The ceremony is staged and contains no mechanical work.** Everything below is either yours by design or mine to execute on your word.

**Yours, and not transferable:**

1. **The closing judgment** — `accomplished | failed | superseded`, as an explicit sentence recorded verbatim. I draft the closing report; you amend and render. It is not inferred from "proceed" or from silence. *(An instrument cannot certify its own reading; this campaign is criterion 3's instrument.)*
2. ~~The criterion-3 attestation~~ — **done**, recorded verbatim at `833377e`.

**Mine, on your word — the runbook (`work/runbooks/fold-and-close.md`) is the authority:**

3. Pre-flight: archive sweep, store integrity, and the merge-surface assertion — **counts are derived, not hardcoded**; a *higher* handle count is expected (the ceremony stamps its own close-out), a *lower* one is the anomaly that stops the ceremony.
4. Triage with prepared dispositions; archive `git mv` into `work/missions/v1.subagent-boundary/`; ledger row; tag `v1.subagent-boundary-closed`.
5. The fold: `git merge --no-ff 26-07-23` with the staged single-conflict `BASELINE.md` resolution (guarded — **any deviation from exactly that one conflict stops the ceremony**), then the enumerated purification commit, then the template gate.
6. Push. **Note:** `origin/main` is 4 commits behind local `main`, so the push publishes that backlog too. The mission branch and tag are unpushed today; both go up, and the tag must never be deleted — it is the continuation protocol's anchor.

**Two things to know before you render the verdict:**

- **The fold buys less for Orchestra than it may appear.** Orchestra is already bootstrapped; what it awaits is the updated `.claude/` manifest, which adoption-by-re-copy can take from this branch today. The purified `main` makes that canonical and diff-friendly — it does not gate it. Stated at the gate, restated here, so the record shows the urgency was preference rather than dependency.
- **Three session handles are permanently `LOST (pre-existing)`** and will appear in the pre-flight sweep. They predate archive-on-stamp or fall outside it. They are **not fold damage**; no future reader should chase them.
