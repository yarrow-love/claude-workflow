---
type: refactor
difficulty: high
depends-on: [1]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect (consult handle)
  - executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 # M2 campaign executor (seeded this order)
---

# M2 · Phase 2 — canon-rewrite (work order)

> Seeded by executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 from the phase spec [`work/milestones/2.conversion/2.canon-rewrite.md`](../milestones/2.conversion/2.canon-rewrite.md) + phase-2 reconnaissance + confirmed gate decisions. **The phase doc stays the spec/record; this is the build surface.** Acceptance carried verbatim below.

**Automatic mode.** Run the [work-order workflow](../../.claude/agents/manager/workflows/execute-work-order.md) with the [automatic-mode overrides](../../.claude/agents/manager/workflows/execute-work-order-automatically.md): plan → implement → review → resolve to the Acceptance criteria under the inner pass budget (3), converging to a `## Acceptance Verdict` in *this* file. No front-end surface (`work/PROJECT.md`) — no inspector. This is a **documentation-only** phase (no code); the quality gate reduces to **reference-resolution across every edited file** + the grep-cleanliness check below. Consult the planning Architect for novel doctrinal questions: `bun .claude/bin/consult "architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2" "<question>"`.

## Objective

The doctrinal conversion: the Executor dispatches Managers as Agent-tool subagents; pause-and-ask replaces the die-with-`ESCALATE`+`--resume` ceremony for interpretation questions; the compensatory apparatus retires from executor/manager canon; the premature-exit bug's remedies land mechanism-neutral; the headless pattern demotes to a documented fallback with stated causes. **Design authority:** the proposal §§ 2–5 (D1 binds), amended by M1's verdicts ([`todos/_done/26-07-23.probe-battery.md`](../todos/_done/26-07-23.probe-battery.md)) and the consulted architect's assessment in the bug order.

**Phase 1 is landed and tested (cite as fact):** `.claude/settings.json` carries `env.CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH: "2"` (the cap); `.claude/bin/session-archive` sweeps `<uuid>/subagents/agent-*.jsonl(.meta.json)` gzip'd to `work/sessions/<machine>/<uuid>/subagents/`, all depths flat, MANIFEST-verified. The D3 durability paths and depth-topology sentences may state these as existing.

## Files — enumerated (recon line numbers, verified valid; phase 1 did not touch any canon file)

**1. `.claude/agents/executor/CLAUDE.md` — § "The Manager boundary" (L20–72), rewritten + retitled** (drop "a child session, not a subagent" — the premise M1 inverted):
- **Dispatch**: each phase Manager is an Agent-tool subagent — per-call `model: "opus"` (load-bearing, M1-certified at both depths; omitting it inherits the session model at up to 2× cost), the phase work doc as the self-contained brief, the same automatic-mode workflow governing it. Depth topology under the cap: Executor (root) → Manager (depth 1) → specialists (depth 2).
- **Observability**: native panel (live transcripts at both depths, M1-certified; task status; surfaced permission prompts) replaces the jsonl periscope. Programmatic parent receives the final result only; mid-flight state rides the work doc's `## Progress` ledger; the bounded check-in habit survives as *reading that ledger*, never a transcript.
- **Call-and-response**: upward **pause-and-ask** (a Manager that hits novelty ends its turn with the question — context held, truly awaiting; the Executor deliberates, consults the Architect per the existing ladder, answers via SendMessage; the Manager resumes in place). Downward **nudge** (Executor → Manager SendMessage mid-campaign; M1-certified: delivery at the next tool boundary, honored as task direction; a completed subagent auto-resumes on message with context intact). **Known limit, stated:** a parent cannot address a grandchild — Executor steers Manager, Manager steers its specialists.
- **Durability (D3)**: the Manager leaves the durable tier; its stamps anchor to the hosting Executor session — `manager@<machine>/<executor-session-id>` (nearest-resumable-session principle, no new grammar). Transcripts persist at `<projectDir>/<executor-session-id>/subagents/agent-*.jsonl` (all depths flat, M1-certified), swept by `session-archive` (phase-1 fact); a past Manager stays reachable by resuming its Executor and continuing the agent by id (M1-certified across a genuine session-end).
- **Permission story**: parent mode dominates structurally (M1: no per-call lever for a stricter child) — the campaign's autonomy scope (set at the Reconnaissance gate) maps to the **Executor session's own mode**, not per-child flags.
- **Headless fallback subsection (clearly marked — see delimiter decision below)**: the `claude -p` child-session pattern survives as the deliberate fallback, demoted from default, its trigger causes stated at the dispatch site (genuinely independent permission scoping, or beyond-cap depth). Bug causes 1–2 land here as fallback discipline: the dispatch invocation *is* the background task, never `&`-wrapped; never background-and-wait a long external process — detach via `setsid` with PID+log captured and Executor-staged finalization.
- **Role-table row** (L70): `child claude -p … session` → subagent dispatch.

**2. `.claude/agents/executor/workflows/execute-milestone.md` — 7.1–7.3 + budgets:**
- **7.1** (L14): Agent-tool dispatch replaces the `claude -p`/stream-json/`tmp/accomplish` recipe; init-event session-id capture → the returned agent id; the phase work doc's `sessions:` entry anchors to the Executor session (D3).
- **7.2** (L15): **task-notification cadence** replaces tail/jq + mtime-stall — on wakeup/notification, read the `## Progress` ledger, distill the one-liner into the campaign log. The mtime-stall bullet (budgets, L26) retires (panel + notifications replace hung-child detection; a wedged Manager surfaces as a stalled ledger, handled by the nudge).
- **7.3** (L16–20): **verdict-absence detection** (bug cause 4, written once, mechanism-neutral): on a Manager's clean return, check the work doc for `## Acceptance Verdict`; absence = early stop (distinct from re-plan) → SendMessage **nudge** to continue to the verdict. The nudge-until-verdict loop **shares the outer pass budget** (default 2) — no new counter. PARTIAL/residual handling becomes SendMessage **continuation** (context intact); a fresh subagent Manager remains the re-plan path. **Step-boundary (Executor decision, bake it):** pause-and-ask is a **7.3 branch**, not 7.2 — a paused Manager *returns* (ends its turn awaiting the answer), so on return distinguish three cases: (a) verdict present → evaluate ultimate acceptance; (b) no verdict + a pending question → deliberate / architect-consult / answer via SendMessage (pause-and-ask); (c) no verdict + no question → verdict-absence early stop → nudge. The ESCALATE branch narrows per file 3.
- **Cost bullet** (budgets, L28): currently "cost rides in the terminal `result` event — the last line of its `.jsonl` log" — reword mechanism-neutral (cost rides the dispatch result surface), reconciled identically with file 4's cost-note.

**3. `.claude/agents/manager/workflows/execute-work-order-automatically.md` — escalation-as-pause + EXECUTION DISCIPLINE:**
- **Escalation-as-pause** (the Step-8 override, ~L14, currently "return control to the caller"): interpretation questions become **pauses** — end the turn with the question and await the answer in place. The terminal `ESCALATE` verdict (defined generically ~L25) narrows to genuinely terminal states, **enumerated**: budget exhaustion, an unblessed order after the refinement bound, operator-gated stops. **Both-mechanism wording (recon Gap B):** this file has no delimited fallback subsection and none of the banned literals — handle both dispatch mechanisms with **inline conditional prose** (for a headless-fallback Manager a pause is not answerable, so the terminal verdict remains its only channel — say so); the reviewer verifies both-mechanism correctness (not grep-checked).
- **EXECUTION DISCIPLINE subsection (net-new; recon confirmed absent):** no `pkill`/`killall` on name/path substrings — exact captured PIDs or port-derived PIDs (`ss -tlnp`) only (cause 3, **universal**: any session whose prompt embeds a path can self-match); foreground-block under the Bash ceiling vs `setsid`-detach with PID+log captured — never background-and-wait (cause 2); the **final-mile exception** at its narrowest: direct tool calls for mechanical remaining convergence steps only, under explicit Executor instruction, never self-invoked (a bounded exception to "don't implement directly," not general license).
- **Progress-heartbeat rationale** (~L17): reword mechanism-neutral — crash-safe state for *any* interrupted Manager, not "killed child."

**4. `.claude/agents/manager/CLAUDE.md`:** dispatch-classification table row (L115: phase build → subagent Manager; SendMessage **continuation** for residuals; fresh dispatch for re-plans) and the cost-note (L100, `tmp/accomplish/*.jsonl`) — cost rides the dispatch result surface. **Also (recon Gap C, bake it):** the "don't implement directly" constraint (~L165) gains a one-clause cross-reference to file 3's final-mile exception (automatic-mode only) so the two files don't read as contradictory.

**5. `.claude/commands/execute.md`:** the milestone-tier bullet (L3, "one autonomous Manager child session per phase (`claude -p …`)") → subagent dispatch.

**6. `work/README.md` L90 (7th file — operator-confirmed at the gate 26-07-24):** the `sessions:` frontmatter clause reads "durable tiers only (architect / executor / **manager**: the sessions someone can actually `--resume`)." Under D3 a Manager subagent is **not** independently `--resume`-able — correct the clause so manager entries anchor to the hosting Executor session-id (nearest-resumable-session; reachable by resuming the Executor and continuing the agent by id), while architect/executor stay independently resumable. Delicate, load-bearing generic doc — keep it project-generic; minimal surgical edit; reviewer checks it. (This file is NOT under `.claude/`, so it is exempt from the `.claude/`-genericity acceptance line but must stay work-system-generic.)

**7. Close the bug order** [`bugs/26-07-23.bug.headless-manager-premature-exit.md`](../todos/bugs/26-07-23.bug.headless-manager-premature-exit.md) → `_done/` (operator-confirmed routing): add `resolution:` frontmatter one-liner + `status: open → done` + a `## Resolution (26-07-24)` section mapping each of the four causes to the **landed text** (1–2 → executor fallback discipline; 3 → EXECUTION DISCIPLINE; 4 → verdict-absence detection), then `git mv` to `_done/`. **Land this LAST** — it cites the other six files' final wording.

## Executor decisions to bake (do not re-litigate)

- **Fallback-subsection delimiter:** in `executor/CLAUDE.md`, gather every surviving apparatus reference under a **single** clearly-named subsection heading — use `### Headless fallback — deliberate, non-default` (a stable, distinctive heading). Record that subsection's **exact final line range** in this work order's `## Acceptance Verdict` so phase 3's grep exclusion is mechanical. Any surviving `tmp/accomplish`/`stream-json`/`mtime`/`claude -p`/tail-recipe token in the five canon files must live **inside** that range.
- **Terminology lock (use consistently across all files):** **nudge** = a SendMessage that steers/continues an existing Manager to finish (verdict-absence, mid-flight steering, "don't stop until the verdict"); **continuation** = a SendMessage re-dispatching a Manager for an incremental residual pass with context intact (vs a **fresh subagent** for a re-plan). Both are SendMessage to the Executor-anchored agent; the distinction is intent.
- **Draft shared prose once:** the dispatch description + model-override rationale appear in both file 1 and file 2 — write once, reuse verbatim (acceptance greps both in one invocation; drift fails it).
- **Genericity guardrail:** no project-specific facts enter `.claude/` (the acceptance test of root `CLAUDE.md`). Platform mechanics (Agent tool, SendMessage, per-call `model`) are already how `.claude/` speaks — fine.

## Acceptance

*(carried verbatim from [the phase doc](../milestones/2.conversion/2.canon-rewrite.md), plus the two Executor-added criteria marked ★)*

- The five canon files contain no unmarked reference to: `claude -p` Manager dispatch, `--output-format stream-json`, `tmp/accomplish`, the tail/jq check-in recipes, or the mtime-stall rule — every survivor sits inside the clearly-marked headless-fallback subsection. Headlessly verifiable: `grep -n 'tmp/accomplish\|stream-json\|mtime' .claude/agents/executor/CLAUDE.md .claude/agents/executor/workflows/execute-milestone.md .claude/agents/manager/CLAUDE.md .claude/agents/manager/workflows/execute-work-order-automatically.md .claude/commands/execute.md` returns hits only within the fallback subsection's line range (enumerate the range in the work order).
- `execute-milestone.md` 7.3 carries verdict-absence detection in mechanism-neutral wording, sharing the outer pass budget.
- The automatic-mode workflow enumerates the terminal-`ESCALATE` causes (budget, unblessed, operator-gated) and states the pause protocol; the EXECUTION DISCIPLINE subsection carries the three rules + the final-mile exception with its three qualifiers (mechanical remaining steps; explicit Executor instruction; never self-invoked).
- The durability amendment is stated: Manager stamps anchor as `manager@<machine>/<executor-session-id>`; the known depth-2 SendMessage limit is documented.
- The headless fallback names its trigger causes at the pattern's definition site.
- Bug order in `_done/` with the four-cause mapping; no other open todo references the retired apparatus as live doctrine.
- Reference-resolution check passes for every edited file (quality gate step 1 scoped to the diff).
- Genericity holds: no project-specific facts entered `.claude/` (the acceptance test of root `CLAUDE.md`).
- ★ **`work/README.md` L90** no longer lists a Manager among independently `--resume`-able sessions; it states the Executor-anchored durability for Manager stamps. (7th file — folded in per the gate decision.)
- ★ **The fallback-subsection line range is recorded** in this work order's verdict (phase-3 exclusion input), and the `manager/CLAUDE.md` "don't implement directly" ↔ final-mile-exception cross-reference is present.

## Progress

- 00:00 planner dispatched (step 5) — fable, difficulty high, full 7-file rewrite
- 00:01 plan written (step 6); submitting for blessing (step 7)
- 00:02 blessing: conditional-BLESSED, round 1 — 2 interpretation directives applied, no re-consult required; 4 decision points adjudicated ACCEPTED
- 00:03 implementer dispatched (steps 8–9) — single sequential, all 7 files
- 00:04 files 1–6 landed; grep-clean (5 banned-token hits all in fallback subsection L28–49; shared line 1:1; cost phrase in both); review pass 1 dispatched (step 10, fresh reviewer)
- 00:05 review pass 1: 2 blocking (terminology-lock leftovers) + 3 mechanical polish, all auto-resolvable; 0 design escalations. Resolving via continued implementer (step 11)
- 00:06 resolution landed + verified (grep-clean, no re-dispatch leftovers, cost phrase intact, cross-ref present); Executor confirmed complete
- 00:07 bug order closed + git mv to _done/ (four-cause→landed-text map); gate PASS (reference-resolution: only sanctioned tmp/accomplish in fallback subsection; grep-clean 28–49; README L90 corrected)
- 00:08 quality gate passed; committing deliverables (final-mile exception: direct tool calls, explicit Executor instruction)

---

## Plan

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

**Strategy: one implementer, all seven files, strictly sequential.** The files share a verbatim prose block (1+2, grep-checked identical), a terminology lock (nudge / continuation / fresh subagent), a shared cost phrase (2+4), a cross-reference pair (3↔4), and a closer citing the six files' final wording. Parallel implementers would drift. Edit order: 1 → 2 → 3 → 4 → 5 → 6 → verify → 7 (bug close + `git mv`). Reviewer (fresh) after files 1–6 land; fallback line-range computed only after review settles file 1. Commits: (1) `Update:` files 1–6; (2) `Document:` bug close + `git mv`.

### Deliverable 1 — verbatim shared-prose line (byte-identical in file 1 Dispatch bullet + file 2 step 7.1)

> Each phase Manager is an Agent-tool subagent — `subagent_type: manager`, the phase work doc as the self-contained brief, the same work-order automatic-mode workflow governing it. Per-call `model: "opus"` is load-bearing: omitted, the dispatch inherits the hosting session's model (up to 2× Opus cost), and the Manager role is coordination, which Opus handles; the specialists' model tiers ride on their own frontmatter pins, independent of the Manager's model.

(Blessing directive 2 applied: specialist-pin enumeration dropped — pins live in agent frontmatter; duplicating them into canon creates a drift surface.)

### Deliverable 2 — the `### Headless fallback — deliberate, non-default` subsection (last subsection of file 1's boundary section, before `## The two loops`)

Gathers **every** surviving apparatus token (`tmp/accomplish`, `stream-json`, `mtime`, the `claude -p` recipe, tail/jq). States trigger causes at the top (independent permission scoping OR beyond-cap depth). Carries bug causes 1–2 as fallback discipline (dispatch invocation IS the background task, never `&`-wrapped; never background-and-wait — `setsid`-detach with PID+log, Executor-staged finalization). Includes flags (`--model opus`, `acceptEdits`/`bypassPermissions`, no `--bare`), handback/`--resume` mechanics, introspection tail recipes, the mtime-stall rule (this path only), and channel limits (a headless child cannot pause-and-ask/receive a nudge — terminal verdict is its only channel; its stamp is `manager@<machine>/<child-session-id>`, self-resumable). Full verbatim text captured for the implementer. Line range recorded in the verdict post-review (phase-3 exclusion input).

### Per-file specs (condensed — implementer gets the full detail)

- **File 1 `executor/CLAUDE.md`**: retitle §Manager-boundary → "an Agent-tool subagent"; replace L20–41 body with 5 bullets (Dispatch [shared line + routing + depth topology] / Observability / Call-and-response [pause-and-ask, nudge, grandchild limit] / Durability D3 / Permission story) + the fallback subsection. Role-table row L70 → Agent-tool dispatch. **Consequential minimal-touch retouches** (Decision 1): L3, L8, L10, L14, L46, L63, L76 — purge "child session" premise & fix the D3-contradicting stamp/`--resume` language; terminology lock (advance/continue/escalate).
- **File 2 `execute-milestone.md`**: 7.1 = shared line + Executor-anchored callback + agent-id note (+ headless-fallback pointer on stated trigger); 7.2 = panel/notification cadence, read `## Progress`, nudge a stalled ledger; 7.3 = **three-case return discipline** (a: verdict→ultimate acceptance/continuation/fresh/ESCALATE; b: no-verdict+question→pause-and-ask; c: no-verdict+no-question→verdict-absence nudge), shares outer budget; budgets L25 reworded, **L27 mtime-stall bullet deleted**; cost bullet L29 → "cost rides the dispatch result surface"; L5 retouch.
- **File 3 `execute-work-order-automatically.md`**: Step-8 override → **pause-on-novelty** (keep consult ladder verbatim; change only the two outcome clauses to end-turn-and-await; append inline both-mechanism clause — headless fallback can't pause, ESCALATE is its channel). Terminal-`ESCALATE` enumeration (budget/stall, unblessed-after-bound, operator-gated). New `## Execution discipline` section: exact-PIDs-only (universal), foreground-or-`setsid`-detach, never-background-and-wait, final-mile exception (3 qualifiers). Progress-heartbeat reword mechanism-neutral.
- **File 4 `manager/CLAUDE.md`**: cost-note → "cost rides the dispatch result surface"; dispatch-table row (subagent Manager; SendMessage continuation for residuals; fresh subagent for re-plans); "don't implement directly" constraint gains one-clause cross-ref to file 3's final-mile exception.
- **File 5 `commands/execute.md`** L3: milestone-tier bullet → "autonomous Manager subagent per phase (Agent-tool dispatch, per-call `model: "opus"`)".
- **File 6 `work/README.md` L90** (Blessing directive 1 applied — state the *principle*, not the subagent case): surgical parenthetical replacement only — the durable tiers are the sessions someone can actually `--resume`; by the **nearest-resumable-session principle** a *subagent* Manager's entry anchors to its hosting Executor session (`manager@<machine>/<executor-session-id>`, reachable by resuming that Executor + continuing the agent by id), while a *headless-fallback* Manager **is** its own resumable session and stamps its own id. Wording must not outlaw the fallback stamp. Rest of line untouched; stays work-system-generic. The ★ acceptance criterion reads accordingly: L90 no longer lists a Manager among *unconditionally* independently-resumable sessions. Reviewer checks L90 and the fallback subsection tell the same durability story.
- **File 7 bug order**: `status: done`, `resolution:` one-liner, `## Resolution (26-07-24)` four-cause→landed-text map, closer stamp; `git mv` to `_done/`. Lands LAST.

### Decision points (all recommendations accepted unless the operator/architect objects)

1. **Consequential same-file retouches beyond the enumerated anchors** (file 1 L3/L8/L10/L14/L46/L63/L76; file 2 L5) — *fix now, minimal-touch* **(rec)** vs defer to phase 3. Deferring leaves file-internal D3 contradictions (L76 stamps a child session-id) that would fail this phase's review.
2. **Manager-side consult-before-pause retained** (file 3 keeps its own architect-consult ladder; pause replaces only record-and-stop) **(rec)** vs lift all consulting to the Executor.
3. **File 6 held to the L90 parenthetical; `work/README.md:48` role-ladder row deferred to phase 3 as recorded residue** **(rec)** vs fix both now — honors "minimal surgical edit."
4. **Execution discipline as three rules** (never-background-and-wait split from foreground-vs-detach) **(rec)** — matches acceptance's literal "three rules + final-mile exception."

Baked gate decisions (delimiter heading, terminology lock, shared prose, 7.3 boundary, Gap B inline, Gap C cross-ref, budget sharing, file-6 inclusion) honored as-is, not reopened.

### Deferred residue (record in verdict → phase 3)

`work/README.md:48` role-ladder row "one Manager child-session per phase"; `architect/CLAUDE.md:39` + `plan-milestone.md:16` "headless Manager session" (already phase-3-owned); `lan-intercom.md` consult-site anchors will drift (halted order, informational).

---

## Blessing

> architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # blessing

**Verdict: conditional-BLESSED** (round 1) — the milestone architect (consult-fork) judged the plan faithful in strategy (single sequential implementer is right), in the 7.3 three-case return discipline, and in the fallback subsection's content. Two defects, both within its interpretation authority — **no operator escalation**; apply and proceed, no second round.

- **Directive 1 — File 6 L90 / Deliverable 2 coherence.** The absolute "anchors to its hosting Executor session" wording contradicts the surviving headless-fallback stamp (`manager@<machine>/<child-session-id>`, self-resumable). Reword L90 to state the **nearest-resumable-session principle**: a subagent Manager anchors to its Executor; a headless-fallback child stamps its own id. Read the ★ criterion as "no longer lists a Manager among *unconditionally* independently-resumable sessions." Reviewer checks L90 and the fallback subsection tell the same story. — **Applied** (file-6 spec + Deliverable 1 area updated).
- **Directive 2 — Deliverable 1.** Drop the specialist-pin enumeration `(planner → fable, …)` — pins live in agent frontmatter; duplicating into canon is a drift surface. Revert to "the specialists' model tiers ride on their own frontmatter pins, independent of the Manager's model." — **Applied**.

**Decision points 1–4: all ACCEPTED** as recommended. Non-directive notes: folding "stall" into the terminal-`ESCALATE` enumeration is consistent with intent (a recurring blocking finding is genuinely terminal for the inner loop; the three named causes remain satisfied); the three-case 7.3 discipline is mechanically sound (M1's certified auto-resume-on-message validates case (b)'s SendMessage answer); no genericity leak found.

Automatic mode: no operator sign-off pause; no novel design decision surfaced (architect adjudicated all forks). Proceeding to implementation on the blessed-and-refined plan.

---

## Depends on

Phase 1 (landed) — the rewritten docs cite the settings cap and archive sweep as existing, tested facts. Downstream premise it must leave true: **phase 3 greps against final canon text** — leave the six files in converted end-state with the fallback subsection clearly delimited (heading + recorded range) so the sweep's exclusion is mechanical.

---

## Acceptance Verdict

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

**PASS.** All Acceptance criteria met; one deferred residue recorded for phase 3 (planned, not a gap). Documentation-only phase — no code, no front-end surface (inspector correctly skipped). Convergence: plan → blessing (conditional-BLESSED round 1, 2 interpretation directives applied) → implement (single sequential) → review pass 1 (5 findings, all mechanical, 0 design escalations) → resolve → gate. Inner pass budget (3): **1 review pass** spent.

### ★ Fallback-subsection line range (phase-3 grep-exclusion input)

**`.claude/agents/executor/CLAUDE.md` lines 28–49** — `### Headless fallback — deliberate, non-default` (starts L28; next `## ` heading `## The two loops` at L50). Every surviving apparatus token (`tmp/accomplish`, `stream-json`, `mtime`, the `claude -p` recipe, tail/jq) lives at L35, L39, L40, L46, L47 — all inside this range; **zero** banned-token hits in the other four canon files. Range stable as of commit `8d1d2ac`; recompute if `executor/CLAUDE.md` changes above L49 before phase 3 greps.

### Per-criterion

| Criterion | Verdict |
| --- | --- |
| Five canon files: no unmarked apparatus reference; every survivor inside the fallback subsection (grep-verifiable) | **PASS** — 5 hits, all in L28–49; 0 elsewhere |
| `execute-milestone.md` 7.3 verdict-absence detection, mechanism-neutral, shares outer pass budget | **PASS** — three-case return discipline (a verdict / b pause-and-ask / c verdict-absence nudge); "shares the outer pass budget — no separate counter" |
| Automatic-mode: terminal-`ESCALATE` causes enumerated (budget/stall, unblessed-after-bound, operator-gated) + pause protocol stated; EXECUTION DISCIPLINE = 3 rules + final-mile exception (3 qualifiers) | **PASS** — new `## Execution discipline` section; final-mile qualifiers: mechanical remaining steps / explicit Executor instruction / never self-invoked |
| Durability amendment stated (`manager@<machine>/<executor-session-id>`); depth-2 SendMessage grandchild limit documented | **PASS** — executor/CLAUDE.md Durability + Call-and-response bullets |
| Headless fallback names trigger causes at definition site | **PASS** — independent permission scoping OR beyond-cap depth, stated under the heading |
| Bug order in `_done/` with four-cause map; no open todo references retired apparatus as live doctrine | **PASS** — `_done/` with `## Resolution (26-07-24)`; only this work order (spec text) matches the apparatus grep, not as live doctrine |
| Reference-resolution passes per edited file | **PASS** — sole unresolved ref is `tmp/accomplish` (sanctioned `tmp/*` lazy-create per PROJECT.md, inside the fallback subsection) |
| Genericity: no project-specific facts in `.claude/` (blessing D2: no specialist-pin enumeration in shared line) | **PASS** — platform mechanics only; pin enumeration dropped |
| ★ `work/README.md` L90 no longer lists a Manager as unconditionally `--resume`-able; states Executor-anchored durability | **PASS** — nearest-resumable-session principle; both cases (subagent anchors to Executor; headless-fallback self-stamps), coherent with the fallback subsection |
| ★ Fallback line range recorded + manager/CLAUDE.md "don't implement directly" ↔ final-mile cross-reference present | **PASS** — range 28–49 above; cross-ref at manager/CLAUDE.md:165 |

### Blessing directives (both applied, verified)
- **D1** — file 6 L90 states the *principle*, not the subagent case; L90 and the fallback subsection tell the same durability story (reviewer confirmed word-for-word match).
- **D2** — specialist-pin enumeration dropped from the shared prose line (pins stay in agent frontmatter).

### Commits
- `8d1d2ac` **Update:** six canon/doc files (files 1–5 `.claude/` + `work/README.md`) + the bug-order `git mv` rename.
- `a077733` **Document:** bug-order close content (status→done, `resolution:`, `## Resolution (26-07-24)` four-cause map).
- (this work-order doc — plan/blessing/progress/verdict — committed next as `Document:`)

Note: the `git mv` rename grouped into `8d1d2ac` rather than the `Document:` commit (harmless — the file is at `_done/`, its close content is in `a077733`).

### Deferred residue → phase 3 (recorded, not a gap)
- `work/README.md:48` role-ladder row "one Manager child-session per phase" — mechanism-neutral reword (Decision 3; minimal-surgical-edit gate decision holds file 6 to the L90 parenthetical).
- `architect/CLAUDE.md:39` + `plan-milestone.md:16` "headless Manager session" — already phase-3-owned.
- Two genealogically-fine "child Manager(s)" phrasings (`manager/CLAUDE.md` dispatch-table narrative; `execute-milestone.md` step 6) — reviewer judged not-wrong; fold into phase 3's sweep.
- `work/todos/26-07-22.lan-intercom.md` consult-site line anchors will drift after this landed (halted order; informational, no edit due).
