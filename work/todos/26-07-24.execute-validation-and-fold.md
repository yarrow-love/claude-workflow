---
type: chore
difficulty: high
sessions:
  - executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # campaign executor
---

# Execute: M3 — validation and fold

## Objective

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Drive [`work/milestones/3.validation-and-fold/MILESTONE.md`](../milestones/3.validation-and-fold/MILESTONE.md) to acceptance: complete the mission's criterion-3 evidence, land the enactment filing and the D4 doctrine rewrite + `.claude/ADR.md`, and stage a rehearsed fold recipe so the operator's `/close` runs as pure ceremony.

**Phases and dependency order** — strictly serial, each `depends-on` its predecessor:

| # | Phase | difficulty | depends-on |
| --- | --- | --- | --- |
| 1 | [validation-and-enactment](../milestones/3.validation-and-fold/1.validation-and-enactment.md) | low | — |
| 2 | [doctrine-and-adr](../milestones/3.validation-and-fold/2.doctrine-and-adr.md) | high | 1 |
| 3 | [fold-preparation](../milestones/3.validation-and-fold/3.fold-preparation.md) | high | 2 |

No phase parallelism is available: phase 2 cites the proposal at its post-move `_enacted/` path, and phase 3's manifest and rehearsal run against phase 2's final `main`-bound text.

**Autonomy scope**: *pending Reconnaissance checkpoint.*

**Campaign as instrument** — this campaign runs under the converted boundary (subagent Managers, `model: "opus"` per call, operator watching the panels) and is itself the remaining validation evidence for mission criterion 3.

---

## Validation evidence (criterion 3)

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2
> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Opened by phase 1, composed by its Manager and inserted here by the Executor (the Manager never writes to this log). Upward leg fills at phase 3; the attestation is the operator's.

**Downward leg — the nudge. SATISFIED, 26-07-24 (M2 phase 2).** Nothing re-run; the event was already on the record. From [`_done/26-07-24.execute-conversion.md:98`](_done/26-07-24.execute-conversion.md), verbatim:

> **Verdict-absence event (dogfood signal for M3):** the Manager returned **without a verdict** — it parked on a background resolution implementer and ended its turn (bug cause 4, which the bug order predicted survives the conversion; the notification confirmed no live child). Files 1–6 landed grep-clean (I verified: banned tokens 5× in `executor/CLAUDE.md` 28–49 only, 0 elsewhere) but uncommitted, bug order unclosed, no gate/verdict. Applied the 7.3 case-(c) remedy: **SendMessage nudge** instructing direct final-mile completion (close bug order, gate, commit, verdict) — no further dispatch, no parking. The completed subagent **auto-resumed from transcript with context intact** (M1 probe-2 behavior confirmed in anger). The nudge mechanism this phase is landing thus validated *during its own construction*.

A downward nudge in the criterion's sense — Executor → Manager, mid-campaign, context intact on resumption — issued against a live campaign, not a probe. The summary-level echo at `:140` carries the same finding at campaign scale.

**Upward leg — pause-and-ask. PENDING PHASE 3.** Slot deliberately empty. Phase 3 carries the Executor-held merge-vs-squash judgment, which its Manager reaches by pausing and asking rather than deciding; that exchange is the upward leg and lands here when it happens. If the deliberation turns on which purity D4 means, the question escalates past the Executor to the operator — in which case this slot records the full ladder, a Manager → Executor → operator escalation being a stronger instance of the criterion than Manager → Executor alone.

**Operator panel attestation — OPERATOR-GATED. SLOT OPEN.** Not drafted, by design: no line appears here that could later be misread as the attestation itself. The operator confirmed at the gate they will attest during this campaign from live observation of its phase panels, and it must land before the ceremony opens. *An instrument cannot certify its own reading* — this line is the operator's to author, and if it does not arrive, criterion 3 is incomplete and the closing report says so.

---

## Reconnaissance

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Three researchers, one per phase, read-only, dispatched in parallel 26-07-24. Consolidated below; per-phase readiness, then the deduplicated concern list, then the Decisions block.

**Per-phase readiness**

| Phase | Ready? | Blocking finding |
| --- | --- | --- |
| 1 | Ready with two spec corrections | Zero-LOST acceptance is unsatisfiable as written (three handles predate the archive convention); MISSION.md diff-scope line contradicts its own scope text |
| 2 | Ready | Source material located and enumerated; BASELINE gaps are four, not the two the spec names |
| 3 | Ready, premise corrected | `b332083` **does not conflict** — `git merge-tree` resolves clean; the recipe's "pre-resolved instruction" is unnecessary |

### Concerns, prioritized

**C1 — Three session handles are permanently LOST (phase 1, highest severity).** The stamp enumeration yields 24 handles / 12 distinct UUIDs. Seven are archived; two (`2f2dc1c5…`, `a52d1822…` — the M2 and M1 Executor sessions) are backfillable from live transcripts. Three have **no source transcript anywhere**: `86ade408-7a9b-4edd-991a-6b3592ce377a` and `1063f991-16dd-4fef-bc25-ba8c37c9dd2c` (stamps inside the 26-07-05 founding series, predating the 26-07-11 adoption of archive-on-stamp — precisely the CLI-retention rot the durable-resumability proposal named) and `3816fb23-acef-4f71-b79f-ce68441a6b21` (stamped in the premature-exit bug order, relayed from Orchestra; never ran with this repo as `cwd`). `sha256sum -c MANIFEST` passes (37 lines, zero failures). Phase 1's acceptance line "zero `LOST`" cannot be met by any action.

**C2 — Phase 1's MISSION.md diff scope contradicts itself.** Scope says flip M2 → done *and* verify M3 → in progress; acceptance says "M2 row reads `done (26-07-24)`; **no other MISSION.md text changed**." Both rows read `pending` today. Separately, `work/milestones/3.validation-and-fold/MILESTONE.md:9` carries a second copy of M3's status (`pending`) that falls out of sync the moment the roadmap row flips.

**C3 — The `git mv` breaks two live inbound links outside phase 1's stated diff scope.** `work/MISSION.md:11` (a markdown link to the proposal — and MISSION.md's charter prose is otherwise protected from edits) and `work/milestones/1.spike/MILESTONE.md:14` (M1's doc, not "this milestone's docs"). Two further references sit in `work/todos/_done/` and are historical-never-rewritten by convention — correctly left to go stale.

**C4 — `b332083` is not a conflict (phase 3 premise falsified, good news).** `git merge-tree --write-tree 26-07-23 main` exits 0 with three clean auto-merges (`BASELINE.md`, `commands/archive.md`, `commands/initialize.md`); the commit's touched lines are disjoint from the branch's 49 commits. `HEAD..main` contains only that one commit, so there is no other conflict surface. Caveat: phase 2 edits `BASELINE.md` again — re-run `merge-tree` at phase 3 execution, not just trust this snapshot.

**C5 — `work/PROJECT.md` is the rehearsal's most likely red.** It is referenced by **17 files** under `.claude/`, it strips in purification, and it is **not** on the sanctioned-absence list in its own quality gate. `work/sessions/MANIFEST` is a smaller instance of the same gap (referenced by `initialize.md`, which treats it conditionally, but the blunt grep does not). Compounding: the quality gate's definition *lives in* `work/PROJECT.md`, which the fold strips — so the rehearsal's sanctioned-absence list must be written into the runbook itself, not assumed resident on the purified tree.

**C6 — `/initialize` is *supposed* to fail on a purified tree.** Check 5 (keystone docs) requires `work/PROJECT.md`, which the bootstrap procedure authors at step 3, *after* `/initialize` first flags its absence. Phase 3's acceptance ("`/initialize` all passed") must mean "failed only at the named, sanctioned gaps" or it blocks on its own correctness.

**C7 — Two empty containers would vanish from git at purification.** `work/proposals/_enacted/` and `work/todos/_done/` have no `.gitkeep` (they are non-empty today, so none was needed). `_deferred/`, `_rejected/`, `_cancelled/`, `bugs/` already carry one. The purification commit must *add* two `.gitkeep` files, not merely `git rm` — a mechanical step absent from the phase-3 recipe.

**C8 — BASELINE's scaffold enumeration has four gaps, not two.** Step 2's brace expansion omits `sessions/` **entirely** (directory and README), and lists `missions/`, `notes/`, `runbooks/` bare — so `missions/README.md`, `notes/README.md`, `runbooks/README.md` are unenumerated too, though all three are tracked. `work/sessions/MANIFEST` and `work/PROJECT.md` appear nowhere in BASELINE. The machine-registry adaptation *is* stated (`BASELINE.md:38`) but says "replace the row(s) with the new project's machine(s)" — not a placeholder row.

**C9 — `.archive/README.md` is an unmanifested product orphan.** A 190-byte generic convention statement ("repo-local cold storage… not part of the bootstrap"), added in the founding commit `5adad6a`, never touched since, zero project facts. Reads as product — but it is absent from `.claude/BASELINE.md`'s manifest entirely.

**C10 — Two strip-list paths are unnamed by phase 3's prose.** `work/proposals/26-07-05.agentic-workflow/` (the non-underscore standing remainder — root `CLAUDE.md` names it as instance, but the strip enumeration names only `_enacted/26-07-05…` and `_deferred/`), and this campaign log itself (untracked today, a dated todo once committed).

**C11 — Live hooks would mutate the rehearsal tree.** `.claude/settings.json`'s `SessionEnd` archive hook writes a transcript into `work/sessions/` and the `Stop` commit-guard hook can block/prompt — both fire if a live Claude session runs inside the disposable worktree, corrupting the very file listing the rehearsal diffs against BASELINE.

**C12 — The acceptance grep flags legitimate generic prose.** `README.md:10` uses `work/sessions/` to describe the callback-grammar archive mechanism as *architecture*, not as an instance pointer. It must be rephrased (point at `work/README.md` § The callback grammar) purely to satisfy a literal-string check that cannot distinguish convention from instance.

**C13 — The ADR's continuation-protocol exemplar cites a tag that does not exist.** `git tag -l` is empty; `v1.subagent-boundary-closed` is created by the *operator's* `/close`, after M3. The ADR must phrase it prospectively, not as a runnable example.

**C14 — Campaign-log authorship.** Phase 1's scope instructs the Manager to "open a section in this campaign's log" — a document the Executor solely authors and concurrently edits. **Executor ruling:** the Manager returns its criterion-3 content in its work order; the Executor appends the section here, stamped `> manager@play/<executor-session-id>` per "the role names the act." No Manager writes to this file.

### Merge-vs-squash — facts assembled, decision deliberately withheld

Phase 3's Executor-held item is the fold's presentation on `main`. Recon assembled the facts, did not decide: `main..HEAD` is **49 commits**; `work/sessions/play/` is **3,734,389 bytes across 37 blobs** (largest ~799 KB); whole `.git` is 8.5 MB; and **no consuming-repo workflow depends on ancestry** — adoption is copy-based per BASELINE's bootstrap, never an upstream merge. The phase-3 Manager will pause-and-ask; the answer is the Executor's, and the exchange is the criterion-3 upward leg.

The milestone architect added the one residue those facts cannot settle: phase 3 was written reading D4's "instance content leaves `main`" as **tree**-purity, history carrying the blobs being fine. A **history**-purity reading of D4 materially favors squash — and that is a charter-interpretation fork, not a fact question. **If the deliberation turns on which purity D4 means, that single question escalates to the operator.** Either shape preserves the continuation protocol provided the tag is pushed and never deleted.

### Decisions confirmed (Reconnaissance gate, 26-07-24)

Operator-answered at the gate, then blessed by both architect handles where the answer expanded scope.

| # | Decision | Disposition |
| --- | --- | --- |
| 1 | Autonomy scope | **The fold executes** — operator authorized, both architects blessed, shape (b). See the `MILESTONE.md` re-scope note. Phases 1–3 and their acceptance are **unchanged**; M3 closes criteria-closed; then ceremony → fold → verification → push, Executor as mechanical arm. |
| 2 | Three unrecoverable handles (C1) | Recorded as `LOST (pre-existing)` with causes; acceptance amended to zero *new* LOST; surfaced in the closing report's reader index. |
| 3 | Broken inbound links (C3) | Both repaired to the `_enacted/` path; phase 1's diff scope widened to include `work/MISSION.md` and `work/milestones/1.spike/MILESTONE.md`. The two `_done/` references stay stale by convention. |
| 4 | Panel attestation (criterion 3) | Operator attests **during** the campaign, watching the phase panels. Must land **before** the ceremony opens. |
| 5 | MISSION.md diff scope (C2) | "No other MISSION.md text changed" guarded charter prose, not the roadmap table — both rows flip; milestone status blockquotes track. |
| 6–13 | C5–C13 | Executor rulings, recorded in the per-phase amendment sections: `.archive/README.md` keep + manifest; `.gitkeep` generalized to any emptied keep-column directory; rehearsal green condition written four-part; `/initialize` a manual walkthrough with an expected-outcomes table, no live session in the worktree; ADR tag citation prospective; `README.md:10` rephrased; all four BASELINE gaps closed under a set-equality invariant; unnamed strip paths written in. |

**Two protections are not transferable** (mission architect, hard line): the **closing judgment** — `accomplished | failed | superseded` rendered by the operator as an explicit utterance, recorded verbatim, never inferred from "proceed" or silence; the Executor drafts, the operator renders. And the **attestation** — operator-authored; if it does not arrive, criterion 3 is incomplete and the report says so. *An instrument cannot certify its own reading*; the archive must not teach future missions that campaigns may certify themselves. Any **history-rewriting operation on `main`** would need its own fresh authorization.

**Recorded honestly**: the downstream driver is softer than stated. Orchestra is already bootstrapped; what it awaits is the updated `.claude/` manifest, which adoption-by-re-copy can take from this branch today. The purified `main` makes that canonical and diff-friendly; it does not gate it. A red rehearsal may therefore delay the fold at no cost to Orchestra.

**Consults**: `architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d` (mission architect — scope-expansion blessing, the non-transferable protections, shape (b)); `architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2` (milestone architect — per-finding amendments, the set-equality invariant, the rehearsal's green condition, the D4-purity escalation trigger).

---

## Phase Ledger

> executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

### Phase 1 — validation-and-enactment · **PASS**, ultimate acceptance holds

Manager subagent `a9c279b8d00afc6a0` (`model: opus`), work order [`26-07-24.validation-and-enactment.md`](26-07-24.validation-and-enactment.md). One dispatch, zero follow-ups — outer pass budget untouched. Commits `142b70e`, `eb52dc8`, `fce3a3c`, `3a86445`.

**Landed.** Proposal `git mv`d to `work/proposals/_enacted/` (true rename, 100% similarity, no copy at the old path — the first flat file there), `status: enacted (26-07-24 — mission v1 M2 conversion; validated M2–M3)`, closer's stamp, three-paragraph prose reconciliation note in final position covering the premise-1 inversion, premises 2–5, and the in-anger validation. `MISSION.md` both roadmap rows plus the line-11 link; `1.spike/MILESTONE.md` line-14 link. 34 backfilled session files, MANIFEST a pure upsert, 71/71 OK.

**Ultimate acceptance.** Both downstream premises this phase owed are true: the proposal sits at its `_enacted/` path for phase 2's ADR to cite, and the archive store is verified whole before phase 3 stages a ceremony that `git mv`s it. Advance.

**The one override of supplied text, accepted.** Scope item (c) told the Manager to write that cause 4 survived conversion "exactly as the proposal's fallback reasoning anticipated." The reviewer traced it and found it unsupported — the proposal never discusses verdict-absence or a Manager parking on a completed background child; its fallback reasoning concerns the spike failing. The prediction is the **bug order's** (`_done/26-07-23.bug.headless-manager-premature-exit.md`, frontmatter `:8`, assessment `:66`); the remedy is the proposal's § design item 3. Credit split accordingly. The Manager treated it as mechanical rather than escalating, on the grounds that it corrects an attribution in a permanent record toward what the record supports without touching any decision — **correct call**, and the error was mine: I carried that phrasing from the phase doc into the work order without tracing it. A related fix: the note's "vindicated a third time" was counting a step that never happened, since one prior instance was the very claim M1 falsified — arithmetic dropped, point kept.

**Scope-boundary call, upheld.** The Manager fixed `MISSION.md:11`'s link *label* as well as its target. A label displaying a path that no longer exists is the same defect the repair was authorized to fix. "The adopted proposal" on that line stays — charter prose, historically accurate at chartering.

**Three findings that route forward, not residuals:**

1. **The handle count is 26/13, not recon's 24/12.** The delta is this campaign's own `executor@`/`manager@2bd6ea6d…` stamps, written after recon ran. The 13th UUID is this live Executor session — not a fourth loss; archive-on-stamp collects it at `SessionEnd`. **The ceremony must expect 26/13 and re-run the sweep post-close** to confirm it landed. Carried into phase 3's pre-flight archive-sweep delta.
2. **`/initialize` check 2 fails on two pre-existing host-environment gaps** — `chrome-devtools` MCP configured but not connecting (`-32000: Connection closed`), and `codebase-memory-mcp` not installed. Neither touches `.claude/` or repo state. **Phase 3's expected-outcomes table must list these as environment noise** on top of the sanctioned pre-bootstrap failures, or the rehearsal reads them as reds it caused.
3. **Two bugs filed, both routed rather than deferred.** `bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md` — `work/PROJECT.md:9` omits `src/_archive/`, which `BASELINE.md:38` mandates gates sanction; a **fourth member of C5's list**, found after C5 was written, and since `PROJECT.md` is stripped by the fold it routes to **phase 3's runbook**. `bugs/26-07-24.bug.initialize-command-genericity-leak.md` — `.claude/commands/initialize.md:13` carries project facts from a *different* project ("ephemeris data, catalogs, vendored clones"); a genericity violation in the product by this repo's own acceptance test, which survived M2's coherence sweep because that sweep hunted dispatch tokens, not project facts. Routes to **phase 2**, already editing product docs.
