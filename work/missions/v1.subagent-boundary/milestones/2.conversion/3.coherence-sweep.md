---
difficulty: low
depends-on: [2]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect
---

# Phase 3 — coherence-sweep

## Objective

Sweep the residue: after phase 2's rewrite, no live-canon reference to the retired apparatus remains outside marked fallback text; sibling docs that *describe* the Manager mechanism agree with the new boundary; the deferred work-queue-tier proposal takes its reconciliation note; the full quality gate passes end-to-end. Mechanical by design — the planning inventory below enumerates the known targets; the grep recipe catches what it missed.

## Scope

**1. Known residue (from the planning inventory, 26-07-24):**

- `.claude/agents/architect/CLAUDE.md` (Constraints) and `.claude/agents/architect/workflows/plan-milestone.md` (phase-decomposition discipline): "the literal convergence target of a **headless Manager session**" → mechanism-neutral wording (an *autonomous* Manager session — the criterion is headless-verifiability of acceptance, not the dispatch mechanism; keep that meaning).
- Any residue phase 2 deliberately deferred (its work order will say so) — pick it up here.

**2. The grep recipe (written into the work order, rerun at close):** sweep live canon for `tmp/accomplish`, `accomplish-live-feed`, `stream-json`, `mtime`, tail-recipe fragments (`tail -c`, `tail -f`), and Manager-dispatch-shaped `claude -p` hits. Live canon = `.claude/`, root `CLAUDE.md`/`README.md`, `work/README.md`, `work/todos/README.md`, `work/PROJECT.md`, `work/footguns/`, `work/runbooks/`, `docs/`. Written exclusions (each hit classified, not silently skipped):

- **Historical paths** — `work/todos/_done/`, `_cancelled/`, `work/proposals/` (all statuses), `work/sessions/`, `work/missions/`: never rewritten (21 files carry apparatus references by design).
- **The consult apparatus** — `.claude/bin/consult`, the callback grammar in `work/README.md`, `work/footguns/consult.md`: `--resume --fork-session` is a distinct, surviving mechanism.
- **The marked fallback subsection** in the executor canon (phase 2's delimited range).
- **`work/MISSION.md`** — the charter is the mission's fixed record (its failure-criteria clause names the fallback deliberately); annotate nothing.
- `work/runbooks/probe-battery.md` — its `--resume` hits are probe-4 procedure, not the retired apparatus (verified at planning).

**3. Reconciliation note.** Append to [`work/proposals/_deferred/26-07-11.work-queue-tier.md`](../../proposals/_deferred/26-07-11.work-queue-tier.md), after its final section, house style: `## Reconciliation note (<date>, subagent-manager-boundary conversion — "never a subagent" clause obsoleted)` — the retirement rationale's clause (a) rested on the session-nesting lesson; M1 certified nested dispatch on the pinned CLI and M2 converted the boundary; a future work-queue tier dispatches Managers per the current executor canon. Append-only; nothing else in the file moves.

**4. Alignment + gate.** `BASELINE.md` ↔ `/initialize` ↔ live tree agree on the phase-1 additions; then the full quality gate (`work/PROJECT.md`): reference resolution across `.claude/`, tooling smoke test, `/initialize` end-to-end, `sha256sum -c MANIFEST`.

## Acceptance

- The work order carries the grep recipe verbatim and its final run's output: **zero unclassified hits** — every hit either fixed in this phase or matched to a written exclusion class above.
- The two architect docs read mechanism-neutral while preserving the headless-verifiability criterion for acceptance blocks.
- The reconciliation note stands in `_deferred/26-07-11.work-queue-tier.md`, house style, append-only (diff shows pure addition).
- Quality gate passes in full: every `.claude/`-referenced path resolves; `bun .claude/bin/session-archive` and `consult` no-arg usage/exit-1; `commit-guard` TTY contract; `cd work/sessions && sha256sum -c MANIFEST`; `/initialize` audit green including the new depth-cap line.
- Diff scope: no edits under the historical paths (verifiable via `git diff --stat` against the exclusion list).

## Depends on

Phase 2 — the sweep greps against final canon text with the fallback subsection delimited. Downstream premise it must leave true: **M3's validation campaign launches from coherent canon** — a fresh Executor session following only the docs dispatches a subagent Manager, and the milestone-level acceptance (`MILESTONE.md`) is checkable without reference to this planning session's context.

## Addendum

**Closed 26-07-24 — PASS** (executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19; Manager subagent, `model: opus`, one review pass, architect-blessed). Build surface + verdict: [`work/todos/_done/26-07-24.conversion-3-coherence-sweep.md`](../../todos/_done/26-07-24.conversion-3-coherence-sweep.md). Commits `81e14a6`, `00f8a97`, `f2b03ab`.

- **Sweep:** grep rerun (pinned invocation) → **zero unclassified hits** — every apparatus survivor is `fallback-subsection-exclude` (`executor/CLAUDE.md` 28–49) or `consult-apparatus-exclude`. The three semantic-residue targets (no banned token) were fixed by direct edit + reviewer-verified.
- **Landed:** `work/README.md:48`, `architect/CLAUDE.md:39`, `plan-milestone.md:16` → "autonomous Manager session" / "one Manager subagent" (headless-*verifiability* meaning preserved); reconciliation note appended to `_deferred/26-07-11.work-queue-tier.md` (pure addition); both "child Manager(s)" phrasings kept (genealogically accurate). Diff scope clean — no historical-path edits beyond the sanctioned `_deferred/` append.
- **Quality gate green:** reference-resolution, tooling smoke, MANIFEST 25/25, `/initialize` incl. depth-cap check #4.
- **Non-blocking residual:** the instance `.gitignore` lacks an explicit secret pattern (`/initialize` check #7; pre-existing, out of scope) — surfaced to the operator in the campaign Operator Handoff.
