---
status: done
type: refactor
difficulty: low
priority: high
filed: 26-07-11
aligns: agentic-workflow
scope: [.claude]
depends-on: [1]
sessions:
  - architect@play/a9ddd6b3-7457-451c-85bd-a79a91b05e08 # series architect
---

# Sweep — rename roles, repoint paths, retire dead definitions

## Objective

The mechanical reconciliation of `.claude/` with the determinations: role renames, the old-flat-layout → `work/` path sweep, retirement of the scheduler and the broken hook. Mechanical but broad — the acceptance greps are the real spec. Prose semantics of workflows are **not** revised here (that is order 4); this order changes names and paths only.

## Steps

1. **Role renames** (files, frontmatter `name:`, and every in-`.claude/` reference):
   - `.claude/agents/orchestrator/` → `.claude/agents/manager/`; frontmatter `name: manager`; every "orchestrator"/"Orchestrator" token in `.claude/` prose → "manager"/"Manager" (including the extensive usage in `executor/CLAUDE.md` and the command files).
   - `.claude/agents/resolver.md` → `.claude/agents/integrator.md`; `name: integrator`; sweep references (most live in scheduler/implement-todo texts being retired or merged later — repoint the survivors: `implement-todo.md` step 12, `triage` references if any).
   - **Do not touch `work/proposals/` or any imported historical document** — historical vocabulary (and old `orchestrator@play/…` stamps) stays valid via the rename mapping in `work/README.md`.
2. **Path sweep** — in all 15+ affected `.claude/` files: `todos/` → `work/todos/`; `docs/milestones/` → `work/milestones/`; `docs/footguns/` → `work/footguns/` (create the directory with a stub `README.md` — one line, per the mission-tier footgun protocol — so references resolve); `docs/research/` stays (docs = knowledge); project-supplement references `.claude/agents/PROJECT.md` → `work/PROJECT.md` (file lands in order 3 — transient dangling reference within the series is acceptable); confirm `tmp/` is gitignored (the Executor's `tmp/accomplish/` stream logs).
3. **Retire the scheduler** — move `.claude/agents/scheduler.md` to `work/proposals/_deferred/26-07-11.work-queue-tier.md` wrapped with a header noting: (a) why retired — it dispatches Managers as Agent-tool subagents, which the Executor doc's session-nesting lesson rules out, and it references a dispatch service and Linear integration that don't exist here; (b) what to salvage when the work-queue tier is designed — the plan-evaluation protocol, the scope-overlap matrix, migration serialization, priority ordering. Delete `.claude/commands/schedule.md`.
4. **Fix the `SessionStart` hook** — `.claude/hooks/git-sync-check.ts` does not exist; remove the hook entry from `.claude/settings.json` (order 5 introduces the SessionEnd archive hook; a fresh git-sync-check can return with the bootstrap baseline if wanted — note it in the BASELINE manifest candidates, order 3).
5. **Registry sanity** — after renames, confirm the harness agent list shows `manager` and `integrator` (and no `orchestrator`/`resolver`/`scheduler`) in a fresh session.

## Acceptance

- `grep -ri 'orchestrator\|resolver' .claude/` → zero hits (case-insensitive), except any deliberate historical-mapping mention.
- `grep -rE '(^|[^/])todos/|docs/milestones|docs/footguns|agents/PROJECT\.md' .claude/` → zero hits; every path reference in `.claude/` resolves to a real location (the sole sanctioned exception: `work/PROJECT.md` pending order 3).
- `.claude/agents/scheduler.md` and `.claude/commands/schedule.md` gone; the deferred proposal exists with the retirement rationale.
- `claude` session starts with no hook error; `settings.json` contains no reference to nonexistent files.

---

## Summary

> architect@play/a9ddd6b3-7457-451c-85bd-a79a91b05e08

Executed 26-07-11. Renames: `agents/orchestrator/` → `agents/manager/`, `agents/resolver.md` → `agents/integrator.md` (git mv), with a full word-boundary content sweep (orchestrator→manager, resolver→integrator, old flat paths → `work/…`, supplement → `work/PROJECT.md`) across every `.claude/` markdown file — the harness registry now shows `manager`/`integrator` (confirmed live mid-session via the reloaded skill descriptions). Scheduler retired to `work/proposals/_deferred/26-07-11.work-queue-tier.md` with the retirement rationale + salvage list; `/schedule` deleted. Broken `SessionStart` hook removed from `settings.json`. Beyond plan: three dead lasers references fixed (`docs/git-workflow.md` cross-ref in integrator, `3.music-nrt` exemplar in plan-milestone → `work/milestones/README.md`, stale example in answer.md); stub READMEs created for `work/footguns/` and `work/milestones/` so swept references resolve; `work/todos/{bugs,_cancelled}/` + `docs/research/` scaffolded. Acceptance greps: role sweep clean; dangling-path audit clean except the four sanctioned lazy-creates (`tmp/*` runtime, `BACKLOG.md`, `work/PROJECT.md` → order 3).
