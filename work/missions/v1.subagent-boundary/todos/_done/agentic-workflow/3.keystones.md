---
status: done
type: chore
difficulty: medium
priority: high
filed: 26-07-11
aligns: agentic-workflow
scope: [work-system, .claude, tooling]
depends-on: [2]
sessions:
  - architect@play/a9ddd6b3-7457-451c-85bd-a79a91b05e08 # series architect
---

# Keystones — project supplement, root CLAUDE.md, baseline manifest, MCPs

## Objective

Author the three documents every agent session depends on but which the import left missing, and configure the always-useful MCP servers. After this order, the generic `.claude/` system has a real project to read about.

## Steps

1. **Author `work/PROJECT.md`** — the project supplement the (post-sweep) agent docs reference. Requires investigation of this repo, not template-filling:
   - **All roles**: the quality gate (inspect `package.json` scripts / toolchain — determine the actual typecheck/lint/test commands for this repo and record them as the gate); repo architecture pointer (root `CLAUDE.md`); the session-callback convention pointer (`work/README.md`).
   - **Per-role notes**: front-end surface (the chart UI — identify the dev-server command, target URL); runtime-evidence locations for the investigator; Context7 library list (Swiss Ephemeris / sweph bindings, FlexSearch, and whatever else `src/` imports — enumerate from `package.json`); documenter doc-surface priority order and never-touch list; inspector target URL + test posture (no auth provider yet — say so).
   - Keep it honest: where a fact doesn't exist yet (no deployed environment, no auth), state that explicitly rather than importing lasers-shaped assumptions.
2. **Author root `CLAUDE.md`** — a stub honest to current state: one-paragraph project description (sidereal astrology charting; `src/`; the docs/ reference corpus), pointer to `work/README.md` for the work system, pointer to `work/PROJECT.md` for operational facts, empty footgun index pointing at `work/footguns/`. The full architecture write-up belongs to the re-architecture mission, not this order.
3. **Author `.claude/BASELINE.md`** — the portability manifest: every file in `.claude/` belonging to the generic bootstrap (agents, commands, workflows, the settings skeleton and which settings keys are baseline vs project), the copy procedure ("bootstrap a new repo" = copy exactly this list + scaffold `work/`), and the expectation list `/initialize` audits against (user-scope MCPs: context7, playwright, chrome-devtools; `work/PROJECT.md` present; …). Anything in `.claude/` not listed is project-specific by definition.
4. **Configure browser MCPs at user scope** — `claude mcp add --scope user` for Playwright MCP (`@playwright/mcp`) and Chrome DevTools MCP (`chrome-devtools-mcp`); verify with `claude mcp list`. Record the exact commands in `BASELINE.md` as the recipe (they are the `/configure` seed). The inspector/designer agent docs stay generic; the project supplement (step 1) carries this repo's target URL.

## Acceptance

- `work/PROJECT.md` answers every question the agent docs ask of it (grep the agent docs for "supplement"/"PROJECT.md" mentions and check each against the authored sections) — with honest "not yet" entries where the project has no answer.
- Root `CLAUDE.md` exists; a fresh session auto-loads it without contradiction against reality.
- `.claude/BASELINE.md` enumerates the bootstrap; every `.claude/` file is classifiable as baseline or project-specific by the manifest.
- `claude mcp list` shows context7, playwright, chrome-devtools at user scope; an inspector dispatch can actually reach a browser.

---

## Summary

> architect@play/a9ddd6b3-7457-451c-85bd-a79a91b05e08

Executed 26-07-11. Repo investigation found the honest state: browser-native static app (no package.json, no build, no test/lint tooling), active surface `src/experiments/v0.1.1/`, vendored libs incl. `sweph-wasm@2.6.9`. `work/PROJECT.md` written to that reality — the quality gate is explicitly informal (app loads + renders without console errors; git hygiene) until the re-architecture mission establishes tooling; front-end serve recipe (`bunx serve`, WASM needs HTTP), no-auth/no-deploy stated plainly; Context7 library table with the vendored-source-is-ground-truth rule. Root `CLAUDE.md` authored as a deliberate pre-consolidation stub (points at code + reference notes rather than describing an unsettled architecture); empty footgun index table. `.claude/BASELINE.md` manifest written, including the bootstrap procedure and the `/initialize` expectations (MCP add-recipes recorded as the `/configure` seed). Playwright + Chrome DevTools MCPs added at user scope and verified Connected via `claude mcp list`. Deviation: none. Note: BASELINE.md lists the *post-order-4/5* command/agent family (architect/, execute.md, bin/) — intentionally forward-written; `/initialize` (order 5) audits against the landed state.
