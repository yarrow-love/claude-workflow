---
type: bug
status: done
scope: [product]
resolution: Parenthetical genericized to `work/PROJECT.md`; sweep found and fixed one sibling (`close.md:17`).
sessions:
  - manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # filer (M3 phase 1 manager)
---

# `/initialize` check #7 carries project facts from another project

Filed 26-07-24, surfaced by an M3 phase-1 quality-gate run of the `initialize` audit. Not investigated — symptom and location only.

## Symptom

`.claude/commands/initialize.md:13` (check #7, Git hygiene) reads:

> 7. **Git hygiene** — `.gitignore` covers: secrets patterns (`.env*`, keys), large-format assets **(this repo: ephemeris data, catalogs, vendored clones)**, `tmp/`, `.codebase-memory/` …

The parenthetical is a project fact — and not even *this* project's. "Ephemeris data, catalogs, vendored clones" describes an astronomy repo; this repo carries no such assets. The phrase "this repo" inside a bootstrap file is self-contradicting: the file ships to every consuming repo.

This violates the standard root `CLAUDE.md` sets for the product: *"every file under `.claude/` must stay project-generic: no project facts, no machine-specific paths, no assumptions a fresh repo wouldn't satisfy. The seam for project specifics is `work/PROJECT.md` in the consuming repo."* Genericity is the stated acceptance test for `.claude/` changes, so this is a defect in the product, not a cosmetic nit.

## Where

- `.claude/commands/initialize.md:13` — the offending parenthetical.

## Note for whoever fixes it

The fix is presumably to make the asset class generic (e.g. "large binary/vendored assets, per `work/PROJECT.md`") rather than to delete the check. Worth a sweep for sibling leaks in the same file and its neighbors while there — this one survived the M2 coherence sweep because that sweep hunted retired-dispatch tokens, not project facts.

## Resolution (26-07-24)

Fixed in M3 phase D of `work/todos/26-07-24.doctrine-and-adr.md` (Scope item 5).

- **`.claude/commands/initialize.md:13`** — the parenthetical now reads *large binary or vendored assets (the project's classes per `work/PROJECT.md`)*, routing the asset class to the consuming repo's seam. The check itself is intact: secrets patterns, `tmp/`, `.codebase-memory/`, and the `git status` clause all survive; only the project fact was removed.
- **`.claude/commands/close.md:17`** (sibling) — the requested sweep was run across all of `.claude/` and surfaced exactly one other project-fact leak: the session-store sweep recipe hardcoded `git grep -h '@play/'`, `play` being this repo's own machine-registry name. Replaced with the placeholder form used by every other callback-grammar reference in the product — `git grep -h '@<machine>/' -- '*.md'` — plus a clause noting the sweep spans every machine in the registry, not just this host.

No third *recipe* leak exists. The ephemeris/catalogs/vendored-clones grep is empty, and `grep -rn '@play/' .claude/` returns only `.claude/ADR.md` § Founding provenance — a concrete consult handle for a real archived session, not a hardcoded example, and the surrounding prose states outright that the handle is machine-qualified and therefore quarantined there rather than in the register body. Left as authored (a different category, and a sibling's file in the same phase); noted so a later genericity sweep doesn't re-flag it blind.
