---
type: chore
difficulty: high
status: done
scope: [work, .claude]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect — consult handle
  - executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # campaign executor
  - manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # phase-3 manager (subagent, anchored to the hosting Executor)
---

# M3 phase 3 — fold preparation

Seeded from [`work/milestones/3.validation-and-fold/3.fold-preparation.md`](../milestones/3.validation-and-fold/3.fold-preparation.md) by the campaign Executor. The phase doc stays the spec/record; **this** document is the build surface. Do **not** write to the campaign log [`26-07-24.execute-validation-and-fold.md`](26-07-24.execute-validation-and-fold.md) — it is the Executor's alone.

## Objective

Stage the fold so the close runs as pure ceremony: a file-level purification manifest, a fold recipe **rehearsed to green in a disposable worktree**, and a consolidated close runbook with prepared triage dispositions. This phase also carries the campaign's Executor-held judgment item — the real upward pause-and-ask that completes criterion 3.

Phases 1 and 2 are closed. The rewritten root docs, `.claude/ADR.md`, and the reconciled `BASELINE.md` are **final `main`-bound text** — the fold carries them verbatim and no doctrine authoring remains here, save the one bounded exception in decision 6 below.

## Scope

**1. The purification manifest** — a file-enumerated keep/strip ledger for the `main`-bound tree, re-verified against the **live** tree (not planning's snapshot; the tree has moved).

- **Keeps (product)**: `.claude/` whole (incl. `ADR.md`, `bin/commit-guard`, expanded `consult`); rewritten root `CLAUDE.md` + `README.md`; `.gitignore`, `.cbmignore`; `.archive/README.md`; `src/README.md`; `docs/README.md` + `docs/research/README.md`; `work/` **scaffold exactly per BASELINE's reconciled step-2 enumeration** — `work/README.md` (machine registry reset per BASELINE's stated adaptation), the canon directory READMEs, and the empty containers.
- **Strips (instance — including `main`'s own residue; main is not pure today)**: `work/MISSION.md`, `work/PROJECT.md`, all dated todos + `_done/` content (founding series included), all dated proposals + `_enacted/26-07-05.agentic-workflow/` + `_deferred/` content, **`work/proposals/26-07-05.agentic-workflow/`** (the non-underscore standing remainder — named explicitly per amendment 5), `work/milestones/*` (all three), `work/missions/v1…/` (the ceremony's own archive — it lives on the branch), `work/footguns/{bin-scripts,consult}.md` (absorbed by ADR § Standing hazards), `work/runbooks/{probe-battery,fold-and-close}.md`, `work/sessions/MANIFEST` + `play/`, `work/design/` content beyond scaffold, `work/notes/` content beyond its README, and **this campaign's own orders** (`26-07-24.execute-validation-and-fold.md`, `26-07-24.validation-and-enactment.md`, `26-07-24.doctrine-and-adr.md`, this file).
- **Zero unclassified paths** is the bar: `git ls-tree -r --name-only HEAD` fully covered.

**2. The fold recipe** — exact command series, executable top to bottom: fetch-fresh state assertions (`git status` clean, tag present), the merge, the purification commit (enumerated `git rm` paths — never `git add -A`), the template quality gate on the result, and the push. Every step idempotent or safely re-runnable; every failure mode named with its recovery.

**3. The rehearsal (the acceptance instrument).** In a disposable worktree (`git worktree add <scratch> main`), execute the recipe end-to-end against a throwaway branch: merge, purify, run the full template gate there. Record the gate output in this work order; remove the worktree. The purified `work/` tree must match BASELINE's reconciled enumeration **exactly** (`diff` the file listing against the manifest's keep column). **A red rehearsal blocks the phase — fix the manifest/recipe, not the gate.**

**4. The close runbook** — `work/runbooks/fold-and-close.md`. The operator chose the **full** artifact: written for a reader with no campaign context, because it archives as this workflow's first close-and-fold recipe and every future mission inherits it. Consolidate: pre-flight (`/archive` sweep green, phase 1's spot-check re-run); the ceremony as written (closing report drafted in dialog; triage with **prepared dispositions**, below); the archive `git mv` into `work/missions/v1.subagent-boundary/`; the ledger row; the tag `v1.subagent-boundary-closed`; then the fold recipe; then post-fold verification on `main`. The runbook is instance content — it archives with the mission it closes.

**Prepared triage dispositions** (write these in): `26-07-11.configure-command` → archive open-carried, re-seed on a future mission branch; `26-07-22.lan-intercom` → archives halted, Orchestra valve untouched; `bugs/26-07-24.bug.footgun-index-convention-drift.md` → open design question across eight agent policy docs, carries to a future mission (see decision 5); footguns → absorbed-by-ADR noted; runbooks → retire with mission; `docs/research/` → nothing to triage.

## Acceptance

Carried verbatim from the phase doc, then amended below — **the amendments govern where they conflict**.

- The purification manifest stands in the work order: every tracked path on the branch classified keep/strip, `.archive` dispositioned, zero unclassified paths (`git ls-tree -r --name-only 26-07-23` fully covered).
- **The rehearsal ran green**: worktree fold executed end-to-end; the purified tree's `work/` listing matches BASELINE's reconciled enumeration exactly; reference resolution, tooling smoke, and `/initialize` all passed in the rehearsal tree; output recorded; worktree removed (`git worktree list` clean).
- **The upward pause-and-ask happened and is recorded**: the criterion-3 ledger carries the Manager's question, the Executor's SendMessage answer, and the resumed continuation — the merge-vs-squash decision made by the Executor, not the Manager.
- *(operator-gated)* The operator's panel attestation line is recorded in the ledger (any phase of this campaign qualifies).
- `work/runbooks/fold-and-close.md` exists with all runbook elements above; a reader with no campaign context could execute it top to bottom.
- No fold execution occurred outside the rehearsal worktree: `main` untouched (`git log main` unchanged), no tag created, mission archive not moved.

## Confirmed decisions (Reconnaissance gate + phases 1–2, 26-07-24)

These **govern** the acceptance list above.

1. **Amended 26-07-24 — see § Amendment to Confirmed decision 1. The factual premise below is dead; do not act on it unqualified.** **`b332083` is NOT a conflict — the planning premise was falsified.** `git merge-tree --write-tree 26-07-23 main` exits 0 with three clean auto-merges; `HEAD..main` is that one commit alone, so no other surface exists. Drop the "pre-resolved instruction." Replace with a **pre-flight assertion**: re-run `git merge-tree --write-tree` immediately before the fold — phase 2 edited `BASELINE.md` again, so any earlier verification is stale — and **any conflict stops the ceremony for reassessment, never an ad-hoc resolution inside it.**

2. **Empty containers need `.gitkeep` *added*, not just contents removed.** `work/proposals/_enacted/` and `work/todos/_done/` are non-empty today so they carry no keeper; after the strip they would vanish from git entirely. Generalize rather than hand-enumerating: **any keep-column directory left with zero tracked files after the strip gains a `.gitkeep` in the purification commit.** Check `_deferred/` too.

3. **The rehearsal's green condition, precisely** — it was never all-nine-green:
   - **Hard green** — the `work/` file listing matches BASELINE's reconciled enumeration **exactly**; tooling smoke a full pass.
   - **Reference resolution** — every `.claude/`-referenced path resolves *or* sits on the reconciled sanctioned-absence list. Phase 2 landed that list in **both** `work/PROJECT.md` § All roles step 1 and BASELINE (`src/_archive/`, `work/PROJECT.md`, `work/sessions/MANIFEST` — **BASELINE's three of the gate's seven**; the full list is in the runbook § 14), **and** amended the grep to consume `git show <tag>:<path>` provenance expressions syntactically — without which three ADR pointers would have been checked as live tree refs and the rehearsal would have gone red for a phase 2 cause. If it still reds on `PROJECT.md`, that is a **phase 2 acceptance failure** — escalate to the Executor, do not widen the list here.
   - **`/initialize`** — each check green *or* **expected-red per a written expected-outcomes table for a pre-bootstrap-step-3 tree**. Check 5 (keystone docs) is definitionally expected-red: BASELINE step 3 authors `work/PROJECT.md` *after* the audit flags it. Put the table in the runbook — it doubles as the verification sheet for every future consumer's bootstrap.
   - **Two known environment failures at check 2 are noise, not yours**: `chrome-devtools` MCP configured but not connecting (`-32000: Connection closed`), and `codebase-memory-mcp` not installed. Both pre-existing and host-side. List them in the expected-outcomes table so a future reader doesn't misread them as fold damage.
   - The gate's *definition* living in `work/PROJECT.md` (which the fold strips) is **fine**: the rehearsal runs this branch's gate against the candidate tree; the template carries no gate of its own — the consuming repo authors one at step 3. But the runbook must carry the sanctioned-absence list **inline**, since the file that defines it does not survive the fold.

4. **No live Claude session inside the rehearsal worktree.** `.claude/settings.json`'s `SessionEnd` archive hook and `Stop` commit-guard hook would fire there, writing into `work/sessions/` and corrupting the very listing the rehearsal diffs. The rehearsal is command-driven (git / grep / bun); `/initialize` is a **manual walkthrough of its nine checks**. If `bin/session-archive` is smoke-tested in the worktree, **no-arg usage check only** — never pass it a real session id there.

5. **Close the routed bug; carry the other.** [`bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md`](bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md) is still open awaiting *this* phase's half — the runbook's inline sanctioned-absence list. Land that, then close the order to `_done/`. [`bugs/26-07-24.bug.footgun-index-convention-drift.md`](bugs/26-07-24.bug.footgun-index-convention-drift.md) stays open by design — a design question across eight agent policy docs, not phase 3's to settle; give it a prepared triage disposition in the runbook.

6. **The one sanctioned doctrine edit — append a standing hazard to `.claude/ADR.md`.** Phase 2 discovered it in anger, after its own close: parallel dispatch held between specialists but **broke at the manager/specialist boundary** — the Manager edited `work/PROJECT.md` directly under a final-mile instruction while a continued implementer was still live on that file. It surfaced only because the implementer's `Edit` hit a stale `old_string`; a silent lost update was possible. The rule: **dispatching an agent onto a file transfers ownership of it until that agent reports, and a final-mile exception does not implicitly revoke that** — either wait for the report, or cancel the continuation explicitly. One entry, house format (heading-per-hazard), matching the register's terseness. This belongs in the ADR because § Standing hazards exists for hazards that outlive a mission; left in the campaign log it archives with the branch and is lost. **This is the only doctrine authoring sanctioned in phase 3** — do not extend it.

7. **Amended 26-07-24 — see § Amendment to Confirmed decision 7. The count is 27, not 26; a higher number is expected, not an anomaly.** **The ceremony expects 26 handles / 13 UUIDs**, not recon's 24/12 — the delta is this campaign's own stamps. The 13th is the live Executor session, collected at `SessionEnd`. The runbook's pre-flight must say so and must **re-run the sweep post-close** to confirm it landed. Three handles are permanently `LOST (pre-existing)` and are expected; they are not a pre-flight failure.

8. **Verify the literal tag string before push.** The ADR's mission-entry provenance pointers cite `v1.subagent-boundary-closed` verbatim. The runbook's post-fold verification checks the tag actually created matches those strings exactly.

## The Executor-held item — pause and ask, do not decide

**The fold's presentation on `main`**: true merge (`git merge 26-07-23` + purification commit; honest shared ancestry, the mission's blobs reachable from `main`'s history) versus squash-fold (one synthetic commit; slim history, no shared ancestry with mission branches).

**You must not decide this.** Reach the point where the recipe needs it, then **end your turn with the question**, stating the trade-off as you see it from the manifest work. The Executor answers by SendMessage and you resume in place. This exchange is the campaign's criterion-3 upward leg and is a deliverable in its own right — a decision made *by you* would destroy the evidence the milestone exists to produce.

**Superseded 26-07-24 — the numeric figures below are stale; see § Research Findings 3, whose own commit count has since drifted again. Re-derive at ceremony time; never quote.** Facts as gathered at planning: `main..HEAD` is **49 commits**; `work/sessions/play/` is **3,734,389 bytes across 37 blobs** (largest ~799 KB); whole `.git` is 8.5 MB; **no consuming-repo workflow depends on ancestry** — adoption is copy-based per BASELINE's bootstrap, never an upstream merge. Either shape preserves the continuation protocol provided the tag is pushed and never deleted (under squash the tag alone pins the objects; under merge they are also `main`-reachable).

Planning's non-binding lean was true merge — ancestry worth more than slimness in a docs-scale repo. The milestone architect flagged the one residue facts cannot settle: phase 3 was written reading D4's "instance content leaves `main`" as **tree**-purity, history carrying the blobs being fine. A **history**-purity reading materially favors squash, and that is a charter-interpretation fork. If your question turns on which purity D4 means, say so explicitly — the Executor escalates that one to the operator.

## Notes for the run

- **Downstream premise this phase must leave true**: the ceremony finds **zero mechanical work** — judgment, report, tag, staged fold, done — and the criterion-3 ledger is complete at milestone close.
- The consult handle for planning intent is `architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2`, via `bun .claude/bin/consult "<handle>" "<question>"`.
- **File-ownership discipline** (decision 6 is about exactly this): if you dispatch a specialist onto a file, that file is theirs until they report. Do not edit it yourself in the meantime, even under a final-mile instruction.
- Quality gate per `work/PROJECT.md` § All roles. Commits: enumerate paths, never `git add -A`.

---

## Research Findings

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Two read-only researchers (parallel, 26-07-24) plus a planner verification pass, all re-derived against the **live** tree. Three findings change what this phase builds.

### 1. Confirmed decision 1 is falsified — the merge conflicts for real

`git merge-tree --write-tree 26-07-23 main` now **exits 1** with a content conflict in `.claude/BASELINE.md`; only `commands/archive.md` and `commands/initialize.md` auto-merge clean. Merge-base `fe63922`. Recon's clean-exit snapshot was taken before phase 2 rewrote `BASELINE.md` — exactly the staleness decision 1 predicted, with the opposite outcome.

Two conflicting regions, and in **both** the branch text subsumes `main`'s `b332083` intent (that commit's whole purpose was to drop instance-history paths from the portable set and annotate `src/_archive/`):

| Region | `main` (`b332083`) | branch `26-07-23` |
| --- | --- | --- |
| "Design rationale:" line | → "the project's design record under `work/`" | → `.claude/ADR.md` + provenance-pointer prose. Conflicts only because the branch **also** edited the adjacent "Canonical home" line, overlapping the hunks |
| Bootstrap step 2 | appended "a convention path created on first retirement — reference gates must sanction its absence" to `src/_archive/` | rewrote step 2 wholesale: reconciled enumeration, the set-equality invariant, and an explicit "Paths that post-date the scaffold" block naming `src/_archive/` |
| "Retention" line | dropped `(see agentic-workflow/5)` | **identical deletion** — auto-merges, no conflict |

Taking the branch side wholesale therefore loses nothing from `b332083`. That is a claim to be **asserted, not trusted** — see the plan's staged resolution.

### 2. Instance content lives *inside* four keep-column files — `git rm` cannot reach it

The planner's verification pass found what a path-level manifest structurally cannot: keep-column files whose **text** points at stripped paths. A clone of the purified `main` would ship dead links on `work/README.md`'s first screen, and BASELINE's "the READMEs are project-generic" claim would be false on day one.

| File | Instance residue |
| --- | --- |
| `work/README.md` | `:5` determinations-record + design-corpus links; `:54` work-queue-tier pointer; `:103` cross-LAN proposal pointer; plus the machine registry (already sanctioned by BASELINE) |
| `work/milestones/README.md` | the whole file is the v1 roadmap index, not the generic stub. **`main`'s own copy is already the stub** (founding commit `5adad6a`) — restoration is mechanical |
| `work/proposals/README.md` | `:17` founding-series pointer |
| `work/runbooks/README.md` | `:16` "Held here" row for `probe-battery.md` (and `fold-and-close.md` once authored) |

`work/footguns/README.md`, `work/notes/README.md`, `work/todos/README.md` and the remaining scaffold READMEs scan clean.

### 3. Stale facts corrected

| Fact | Work order | Live |
| --- | --- | --- |
| `main..HEAD` | 49 commits | **61** |
| `work/sessions/play/` | 3,734,389 B / 37 blobs | **6,045,725 B / 71 blobs** (phase 1 backfilled 34) |
| `.git` | 8.5 MB | **12 MB**, 924 loose objects, no gc ever run |
| ADR tag-string occurrences | — | **10 strings on 9 lines** (line 34 carries two) |
| `origin/main` | — | **4 commits behind** local `main` — the fold's push publishes that backlog too |

Manifest inventory: **188 tracked paths**, 62 KEEP / 126 STRIP, zero unclassified. `.archive/` has exactly one tracked path (`README.md`) — generic, KEEP, confirming the pre-decision. `work/todos/_done/` and `work/proposals/_enacted/` carry no `.gitkeep` (decision 2's exact case); `_cancelled/`, `bugs/`, `_deferred/`, `_rejected/` already do; no other keep directory goes to zero.

**Divergence, filed not fixed**: `.claude/BASELINE.md`'s "Paths that post-date the scaffold" names 3 entries; `work/PROJECT.md:9`'s gate list names 7. A consumer authoring its gate from BASELINE goes red on `tmp/*`, `work/MISSION.md`, `work/todos/BACKLOG.md`, `work/design/` contents. BASELINE is final `main`-bound text and doctrine authoring is barred here → bug filed, carries forward.

### Architect consult — decision 1's reopening

Consulted `architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2` rather than escalating cold, per the Planner Callback boundary. Verdict, as interpretation of the plan's own intent:

- **Stage the pre-verified resolution and rehearse it to green.** Decision 1's "any conflict stops the ceremony" guards the *operator's execution window* against improvisation; a resolution analyzed at staging, written into the recipe, rehearsed, and assertion-guarded is what that stop condition exists to force work **into**. Staging *is* the reassessment.
- **The stop condition survives, narrowed**: any conflict at ceremony pre-flight **beyond** the one the recipe carries a verified resolution for — i.e. drift after staging — still stops the ceremony.
- **Resolve by taking the branch side wholesale**, never a hand-authored merged text: that would be a third `BASELINE` variant existing on neither branch — novel content entering `main` without review or blessing.
- **Two executable assertions** turn the subsumption claim from taste into a check.
- **Contingent escalation**: if either assertion fails, or any `b332083` content outside the two analyzed regions is missing from the branch text, "which text does `main` get" becomes a genuine design decision → escalate, do not patch.
- Decision 1 takes a **dated amendment recording this consult**, not a silent rewrite.

---

## Implementation Plan

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Five phases around one mandatory pause. **Every file has exactly one owner per phase** — the discipline decision 6 exists to codify, applied to the phase that codifies it.

```
Phase 1 (manager: manifest + staged resolution)  ─┐
                                                  ├─► PAUSE (merge vs squash) ─► 3 ─► 4 ─► 5
Phase 2 (implementer: ADR hazard entry)  ─────────┘
```

**Phase 1 — manifest, staged resolution, amendments** *(pre-pause; manager-owned: this work order + a new bug filing)*. Dated amendment to decision 1 recording the consult. The manifest as a work-order section in **three** classes, not two: KEEP verbatim, **KEEP-WITH-ADAPTATION** (finding 2's four files, each with its exact mechanical edit), STRIP — plus directory-level rules for volatile trees (`work/sessions/play/` grows as this campaign's own sessions archive) and **prospective paths classified before they exist** (this order, the runbook, the bug moves), so the manifest accounts for its own artifacts. The two `.gitkeep` adds. A runnable zero-unclassified assertion. The staged `BASELINE.md` resolution with both assertions and the completeness check. File the post-date-list divergence bug.

**Phase 2 — the ADR standing-hazard entry** *(pre-pause, parallel; implementer, exclusive on `.claude/ADR.md`)*. One `###` entry in § Standing hazards, house format, terse: dispatching an agent onto a file transfers ownership until it reports; a final-mile instruction does not implicitly revoke that. Records the observable — a stale `old_string` is the *lucky* failure, the silent lost update the real one. No new tree-path references, so the reference gate's extraction set is unchanged. The only doctrine edit in the phase.

**— THE PAUSE —** merge vs squash. Everything above is shape-independent; everything below depends on the answer.

**Phase 3 — recipe + rehearsal** *(post-pause; implementer, no tracked files — drafts in scratchpad, executes in a disposable worktree)*. Author the recipe for the chosen shape, then execute end-to-end in `git worktree add … main` and iterate to green. Manifest faults are **reported to me**, not self-fixed — never the gate. Deliver the recipe, gate outputs, red-run causes, and a residue observation list. Remove the worktree.

**Phase 4 — the runbook** *(post-pause; implementer, exclusive on `work/runbooks/fold-and-close.md` + `work/runbooks/README.md`)*. Transcribes the **proven** recipe — the runbook never diverges from what was rehearsed, which is why it sits post-pause rather than splitting authorship across the boundary. Full artifact, `probe-battery.md` house shape, all elements per § Scope item 4, the `/initialize` expected-outcomes table, and the 7-entry sanctioned-absence list inline.

**Phase 5 — closure, commits, confirmation rehearsal** *(post-pause; manager + continued phase-3 implementer)*. Close the sanctioned-absence bug to `_done/`. Commit everything with enumerated paths. Then a **second rehearsal from the true branch tip** — the only run whose strip list includes this phase's own artifacts, and therefore the run whose output the acceptance records.

### Decision points

The merge-vs-squash item is **excluded by design** — it is the Executor's, and recommending it would destroy the evidence this milestone exists to produce.

| # | Decision | Rejected alternative | Consequence | |
| --- | --- | --- | --- | --- |
| 1 | Carry the pre-verified branch-side-wholesale `BASELINE.md` resolution into the ceremony, rehearsed and assertion-guarded | Decision 1's literal stop-on-any-conflict | Rejecting leaves the ceremony unable to run at all. The stop condition narrows to "anything beyond the staged conflict" | **Rec.** (architect-blessed) |
| 2 | **KEEP-WITH-ADAPTATION**: the purification commit mechanically de-instances four keep files — restore/delete only, never authored prose | Verbatim keeps + a bug carried forward | Rejecting ships a `main` whose `work/README.md` first screen is dead links, falsifying BASELINE's project-generic claim on day one. **This extends the spec's single named adaptation** — flagged so sign-off covers it | **Rec.** |
| 3 | File the BASELINE post-date-list divergence (3 vs 7) as a bug carrying forward | Edit `BASELINE.md` now | Editing is barred — final `main`-bound text, doctrine authoring limited to decision 6. Consumers inherit the gap, mitigated by the runbook's inline 7-entry list | **Rec.: file** |
| 4 | Two rehearsal runs — a development run and a confirmation run from the true branch tip | One run | A single run's green predates the runbook/bug/order commits, so the ceremony's actual strip list would never have been exercised. Cost: minutes | **Rec.: two** |

### Out of scope — seeds, not residuals

Template-genericity audit of the remaining `work/` scaffold prose (dated correction citations, the rename mapping's era pointers) — phase 3's residue list seeds it, same neighborhood as the footgun-index drift bug. Post-fold `git gc` (924 loose objects, never collected) — safe operator hygiene, deliberately outside the ceremony. `commit-guard`'s TTY arm is untestable from a script; the rehearsal exercises the stdin arms and records the adaptation.

---

## Blessing

> architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # blessing

**BLESSED** — round 1, no critique directives. Consult-fork of the standing milestone-architect handle (the author of the phase doc this order was seeded from), flagship per the always-flagship rule. Four judgments requested, all returned as interpretation of the spec's own intent:

1. **KEEP-WITH-ADAPTATION is within remit, not an operator escalation.** The phase doc's single named adaptation (the machine registry) "was an instance of a principle, not the boundary of one" — BASELINE's step-2 claim that the scaffold READMEs are project-generic is *part of what the fold delivers*, and a `main` that dead-links on `work/README.md`'s first screen falsifies it on day one. **The restore-or-delete constraint is what keeps this interpretation rather than design — hold that line hard.** If any of the four files turns out to need a *composed* sentence rather than a restore or a deletion, **that single file escalates; the class does not.**
2. **The two-run rehearsal is what the acceptance meant.** A single development run's green predates this phase's own artifacts, "so it validates a strip list the ceremony will never face — that green would satisfy the acceptance's letter and miss its point."
3. **The pause placement is right.** The runbook is shape-dependent through and through; splitting its authorship across the pause "would invite exactly the divergence the *runbook transcribes the proven recipe* rule exists to prevent."
4. **File the 3-vs-7 divergence, do not fix it here — confirmed.** Decision 6's exception is scoped to one hazard *append* to a register built for appends; reconciling BASELINE's post-date list would reopen phase 2's closed, `main`-bound acceptance surface un-blessed. Harm is real but bounded and recoverable; the runbook's inline 7-entry list mitigates the ceremony itself.

**Human-decision flag: nothing new.** The only genuine operator fork in this order remains the one already routed — the D4 tree-vs-history purity question, if the merge-vs-squash deliberation turns on it.

---

## Amendment to Confirmed decision 1 (26-07-24)

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Decision 1's text above stands as written and is **not** rewritten; this amendment records what execution found and how the standing handle ruled.

**What changed.** Decision 1 asserted `git merge-tree --write-tree 26-07-23 main` exits 0 with three clean auto-merges, and mandated a pre-flight re-run because phase 2 would edit `BASELINE.md` again. The re-run was performed. **It exits 1** — a real content conflict in `.claude/BASELINE.md`. Decision 1's own caution was right; its factual premise is dead.

**How it is resolved** (consult `architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2`, recorded in § Research Findings): the resolution is **staged here, not improvised there**. Decision 1's "any conflict stops the ceremony, never an ad-hoc resolution inside it" guards the *operator's execution window*; staging is the reassessment it demands.

**Decision 1's stop condition survives, narrowed to:**

> Any conflict at ceremony pre-flight **beyond the single staged `.claude/BASELINE.md` conflict for which the recipe carries a verified resolution** stops the ceremony for reassessment. Drift after staging is exactly what this now catches.

---

## Amendment to Confirmed decision 7 (26-07-24)

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Decision 7 expects **26 handles / 13 UUIDs**. Re-derived live: **27 handles / 13 UUIDs**. The delta is a `reviewer@play/2bd6ea6d-…` stamp that landed during phase 2, after phase 1 took its count. The UUID count — the one that matters — is unchanged.

The decomposition, which the runbook carries so the operator can verify rather than trust:

`13 = 9 archived + 3 LOST (pre-existing, expected) + 1 collected at SessionEnd`

- **9 archived**: `1db4a079`, `2f2dc1c5`, `34b58558`, `6a0035c9`, `6b086890`, `76ba13c6`, `7f0fbcc4`, `a52d1822`, `a9ddd6b3`
- **3 permanently LOST**: `86ade408…` and `1063f991…` (stamped inside the founding series, predating archive-on-stamp — the retention rot that convention was adopted to stop) and `3816fb23…` (relayed from another repo; never ran with this repo as `cwd`)
- **1 pending**: `2bd6ea6d…`, the live Executor session — what the post-close re-sweep confirms landed

**The lesson, written into the runbook**: this number has now drifted **twice** mid-campaign (24 → 26 → 27). The runbook states the counts as *derived*, gives the literal sweep command, and notes that a **higher** count is expected as the campaign stamps its own close-out — a **lower** one is the anomaly worth stopping for.

---

## The purification manifest

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Re-verified against the **live** tree at `26-07-23` (`2bf162e`), not planning's snapshot.

**188 tracked paths — 62 KEEP, 126 STRIP, zero unclassified.** Verified by construction: KEEP is a finite explicit list; STRIP is its complement over `git ls-tree -r --name-only 26-07-23`. Nothing can fall between the two.

### The KEEP column (62)

| Group | Count | Paths |
| --- | --- | --- |
| Root | 4 | `CLAUDE.md`, `README.md`, `.gitignore`, `.cbmignore` |
| Cold storage | 1 | `.archive/README.md` — inspected: a 190-byte generic convention statement, zero project facts. **Confirmed KEEP** |
| Product | 38 | `.claude/` **whole** — matches BASELINE's manifest 1:1, no gaps, no strays |
| Docs / src | 3 | `docs/README.md`, `docs/research/README.md`, `src/README.md` |
| `work/` scaffold | 16 | the 12 directory READMEs + 4 already-tracked `.gitkeep`s (`todos/_cancelled/`, `todos/bugs/`, `proposals/_deferred/`, `proposals/_rejected/`) |

### Two `.gitkeep` files are **added**, not merely preserved

Per confirmed decision 2. `work/todos/_done/` and `work/proposals/_enacted/` are non-empty today and therefore carry no keeper; after the strip they would vanish from git entirely.

- `work/todos/_done/.gitkeep` — **add**
- `work/proposals/_enacted/.gitkeep` — **add**

Every other keep-column directory retains its own README and never reaches zero tracked files. Checked exhaustively — no third case exists.

### Set equality against BASELINE — the hard-green condition

BASELINE step 2 derives an **18-path** expected `work/` listing. KEEP-under-`work/` (16) ∪ the two added `.gitkeep`s = **18**, matching exactly, in both directions. The purified tree is therefore **64 files** (62 KEEP + 2 added).

### KEEP-WITH-ADAPTATION — the class a path-level manifest cannot see

Four keep-column files carry instance content *in their text*, which `git rm` structurally cannot reach. **Restore-or-delete only — never newly authored prose. Any file needing a composed sentence escalates individually; the class does not** (architect-blessed on exactly that constraint).

| File | Adaptation | Kind |
| --- | --- | --- |
| `work/milestones/README.md` | restore `main`'s own copy (`git checkout main -- …`) — verified to be the generic conventions stub from founding commit `5adad6a`; the branch turned it into the v1 roadmap index | **restore** |
| `work/README.md` | delete `:5` (determinations + design-corpus links), `:54` (work-queue-tier `_deferred/` pointer), `:103` (cross-LAN proposal pointer); reset the machine registry per BASELINE's stated adaptation | **delete** + the one pre-sanctioned adaptation |
| `work/proposals/README.md` | delete `:17` (founding-series pointer) | **delete** |
| `work/runbooks/README.md` | delete the "Held here" table's data rows, leaving the empty table | **delete** — *the one to watch: if an empty table proves unacceptable and a replacement sentence is wanted, this file escalates rather than being authored* |

### The staged `.claude/BASELINE.md` resolution — **proven, not asserted**

Resolution: **take the branch side wholesale** — `git checkout 26-07-23 -- .claude/BASELINE.md` at the conflict. Never a hand-authored merge: that would be a third `BASELINE` variant existing on neither branch, novel content entering `main` unreviewed and unblessed.

`b332083` touches `BASELINE.md` in **exactly 3 hunks**, all three analyzed, all three subsumed by the branch text. Assertions run against `git show 26-07-23:.claude/BASELINE.md`, all passing today:

| # | Assertion | Result |
| --- | --- | --- |
| A1 | zero instance-history paths remain — **run the literal command below, not a reinvented grep** | **0 hits — PASS** |
| A2 | `src/_archive/` sanctioned as created-on-first-retirement | **3 hits**, incl. `:40` and the `:49` post-date block — **PASS** |
| A3 | the Retention line de-instanced (third hunk; both sides made the identical deletion) | **PASS** |

**A1's literal command** — the anchoring is load-bearing, because A1 runs at ceremony pre-flight under "any deviation stops the ceremony":

```sh
A1RE='work/todos/_done/[A-Za-z0-9]|agentic-workflow|_enacted/[A-Za-z0-9]|[0-9]{2}-[0-9]{2}-[0-9]{2}\.'
git show <branch>:.claude/BASELINE.md | grep -cE "$A1RE"      # expect 0
```

The `[A-Za-z0-9]` anchors are not decoration. A bare `_enacted` token matches BASELINE line 39's scaffold glob `proposals/{README.md,_deferred/,_rejected/,_enacted/}` — a **generic directory name, not instance history** — and an operator who reinvents the grep without the anchor gets **1 hit and a false red on a stop gate**. The runbook's § 3d carries this anchored form; this table previously described the assertion loosely, which is the gap.

**Contingent escalation, written in**: if any assertion fails at rehearsal, or `b332083` content outside these three hunks is found missing from the branch text, then *which text `main` gets* becomes a genuine design decision → escalate, do not patch locally.

### Prospective paths — the manifest accounts for its own artifacts

Classified before they exist, so the ceremony's strip list is complete rather than merely current:

- `work/todos/26-07-24.fold-preparation.md` (this order — untracked today) → **STRIP**
- `work/runbooks/fold-and-close.md` (this phase authors it) → **STRIP**
- `work/todos/_done/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md` (post-move) → **STRIP**
- `work/todos/bugs/26-07-24.bug.baseline-postdate-list-divergence.md` (filed below) → **STRIP**
- `work/sessions/play/**` — **volatile**: a directory-level rule, not an enumeration. This campaign's own sessions land here at each `SessionEnd`, including the Executor's. Concretized to enumerated paths at execution time.

### The zero-unclassified assertion, runnable

```sh
# every tracked path is KEEP or STRIP, and the purified tree is exactly the 64
comm -23 <(git ls-tree -r --name-only <branch> | sort) keep.txt   # → the strip list
git ls-files | sort | diff - expected-tree.txt                    # → empty, post-purification
```

Re-run at ceremony pre-flight: it is what catches artifacts committed between staging and execution.

---

## The Executor-held decision — asked and answered

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

The campaign's criterion-3 upward leg. The full exchange is the Executor's to record in the campaign log; this section is the resumed continuation's working record.

**Asked** (Manager → Executor): merge vs squash, with the corrected facts (61 commits, not 49; 6,045,725 B / 71 blobs, not 3.7 MB / 37), and the material new input — confirmed decision 1 falsified, so true merge now carries one staged conflict-resolution step while squash-as-tree-adoption sidesteps it. Relayed with the weight stated honestly (a solved one-step cost, not an open risk) and **no recommendation**. The D4 escalation trigger was assessed and explicitly *not* invoked: the tree-level outcome is byte-identical under both shapes (the same 64 files), so tree-purity does not discriminate and the conflict finding is mechanical, not charter-level.

**Answered** (Executor): **true merge — `git merge --no-ff 26-07-23`.**

The deciding fact was one neither party held: **`origin` carries only `main`** — the mission branch has never been pushed and there are **no tags on the remote at all**. Since the continuation protocol requires the tag be pushed and never deleted, and pushing `v1.subagent-boundary-closed` carries all the mission's objects to origin regardless of shape (a default `git clone` fetches all tags), **squash does not produce a lighter clone — only a cosmetically cleaner `git log main`.** The trade reduces to honest ancestry plus `main`-reachable objects versus a tidier log, at identical clone weight. Merge also fails safer: if the tag is ever lost, the objects stay reachable rather than becoming orphans awaiting `gc`.

The Executor concurred on not escalating, and recorded *why* rather than leaving it implicit: the deliberation could not turn on tree-vs-history purity because **squash cannot deliver history purity here anyway** while the protocol demands a pushed tag — a charter question needs the operator only when the answer would change the outcome.

**Staged conflict resolution accepted**, with one tightening: the three assertions run in the ceremony's **pre-flight**, and **any deviation stops the ceremony** — no ad-hoc resolution inside it.

**Binding additions to the recipe and runbook:**

1. The conflict was **created by phase 2's own `BASELINE.md` edits** and did not exist at reconnaissance. The pre-flight re-run is what caught it — the recipe says so explicitly, because that is the amendment doing its job and it justifies keeping the assertion for future missions.
2. `origin/main` is **4 commits behind** local `main` (`fa6d173`); the fold's push publishes that backlog too. State it so the operator is not surprised by a push carrying more than the fold.
3. **Push the tag — mandatory**, it is the continuation protocol's anchor. Also push the mission branch ref: it costs nothing (the objects travel with the tag regardless) and makes the mission navigable by name rather than by tag archaeology.
4. Post-fold verification confirms the tag string matches the ADR's provenance pointers **verbatim** (decision 8). `.git`'s 924 never-collected loose objects are **optional** post-fold housekeeping — mentioned, not gated.

**Attestation**: the operator panel attestation slot remains open. The Executor owns it and is raising it with the operator. It does not block this phase; it blocks the ceremony, which is downstream of this verdict.

---

## The rehearsal — GREEN

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Three runs in a disposable worktree (`fold-rehearsal` off `main` @ `b332083`), command-driven throughout. **No `claude` invocation of any kind occurred inside the worktree** — confirmed decision 4 held; `session-archive` was exercised no-arg only.

### Green on every condition

| Condition | Result |
| --- | --- |
| **Hard green** — `work/` listing vs BASELINE's 18-path enumeration | `IDENTICAL — 18`, both directions |
| Whole-tree listing vs expected | `IDENTICAL — 64` |
| Zero-unclassified | `tree=194  keep=62  expected=64  strip=132  keep-paths-missing-from-tree=0` → OK. **The 194 is run 3's simulated archive**: the then-190 tree plus 4 staged `work/missions/v1.subagent-boundary/` files. Run 2, without the simulation, read `tree=190 … strip=128` |
| **Merge proof** — `git diff --stat 26-07-23 HEAD` after resolution | **empty** — the merged tree is byte-identical to the branch tree; `b332083` contributed nothing the branch had not superseded |
| Assertions A1 / A2 / A3 on the *resolved* file | `0` / `3` / `0` — pass; zero conflict markers under `.claude/` |
| Reference resolution | 30 paths extracted, **7 unresolved, every one sanctioned**. The `git show <ref>:<path>` alternative consumed the ADR's 10 provenance strings whole, as designed |
| Tooling smoke | full pass (4/4 exits correct) |
| `/initialize` | 7 green; checks 2 and 5 expected-red per the table |
| Idempotency | re-derive + re-purify on a purified tree → `strip=0`, `git status` empty |

**No red on `work/PROJECT.md` beyond its sanctioned absence — no phase-2 escalation.** All four KEEP-WITH-ADAPTATION edits proved **pure restore-or-delete**; no file needed a composed sentence, so nothing escalated under the architect's constraint.

Two adaptations recorded rather than worked around: `commit-guard`'s TTY arm is untestable from a script (a permanent property of the check, not a rehearsal shortcut), and `sha256sum -c MANIFEST` is N/A on the purified tree because the fold strips `MANIFEST` — it belongs to `/archive`, upstream.

### The red run — a recipe fault that the rehearsal could not have caught by running

Run 1 went green on every visible check while carrying a latent defect. The keep list derived the `work/` scaffold by grepping `git ls-tree … -- work` for `(^|/)(README\.md|\.gitkeep)$`. That pattern keeps **any** `README.md` under `work/` — including the ones inside `work/missions/v1.subagent-boundary/`, the mission archive that `/close` `git mv`s in **upstream of the fold**. It would have classified the archive's own READMEs as KEEP and **silently smuggled mission content into the purified `main`** — the exact outcome D4 exists to prevent.

The rehearsal ran against a tree where the archive did not yet exist, so **no amount of running would have surfaced it**. It was caught by reasoning about the ceremony-time tree, then proven by simulating the archive move in run 3 (a staged `work/missions/v1.subagent-boundary/` with its own README copies: every archived path stripped, `work/missions/README.md` survived, tree still exactly 64).

**Fix**: the `work/` keep set is now **transcribed from BASELINE step 2's enumeration, never grepped** — which is what the acceptance demanded all along ("must equal BASELINE step 2's 18-path derivation exactly"). The hazard is commented in `derive.sh`.

A second, smaller recipe fault fixed in the same pass: strip was `tree − keep`, so a re-run would `git rm` the two `.gitkeep`s it had just added. Strip is now `tree − expected-tree(64)`, genuinely idempotent; zero-unclassified is still proven via the complementary `keep ⊆ tree` assertion.

**Both faults were in the recipe, not the manifest.** The manifest's 62 KEEP / 2 added `.gitkeep` / 18-path `work/` set / four adaptations / staged `BASELINE.md` resolution all held exactly as written.

### Teardown

`git worktree list` clean (one entry), `fold-rehearsal` branch deleted, `git tag -l` **empty**, `git log -1 main` still `b332083` — unchanged. Nothing pushed, mission archive not moved. The only artifacts produced live in the scratchpad and are reproduced verbatim inside the runbook.

### Residue observed, recorded, not acted on

Six references in keep-column files to stripped or instance-specific material that the four adaptations do not cover — two genuine dead links (`src/README.md:7`, `work/README.md:81`), three prose-level instance facts, one set of dated examples in a format specimen. The reference gate cannot see them: it greps `.claude/` only. Same neighborhood as the footgun-index drift bug; seeds for a future template-genericity audit, carried into the runbook's triage dispositions.

---

## The confirmation rehearsal — GREEN (the run of record)

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Run from the **true branch tip** (`f4edcce`) after every phase artifact was committed. This is the run the acceptance records, because it is the only one whose strip list contains the runbook, this order, and the bug moves — the list the ceremony will actually face. Worktree `fold-confirm` off `main`, command-driven, no `claude` invocation.

| Step | Result |
| --- | --- |
| Merge (`git merge --no-ff 26-07-23`) | conflict in `.claude/BASELINE.md` **only** — exactly the staged surface |
| Resolution (`git checkout 26-07-23 -- .claude/BASELINE.md`) | unmerged set empty; **0 conflict markers** under `.claude/` |
| Assertions A1 / A2 / A3 | `0` / `3` / `0` — pass |
| **Merge proof** (`git diff --stat 26-07-23 HEAD`) | **empty** — merged tree byte-identical to the branch tree |
| Derivation | `tree=191  keep=62  expected=64  strip=129  keep-paths-missing=0` → zero-unclassified OK |
| Purified tree | **64 files** |
| Whole-tree listing vs expected | `IDENTICAL — 64` |
| **`work/` vs BASELINE step 2** | `IDENTICAL — 18` (hard green) |
| Reference resolution | 30 extracted, **7 unresolved, all sanctioned** |
| Tooling smoke | 4/4 correct exits |
| `.gitkeep` count | 6 (4 pre-existing + 2 added) |
| Adaptations | only added line across all four files is the machine-registry placeholder — BASELINE's sanctioned adaptation. `work/milestones/README.md` verified **identical to `main`'s stub** |
| `/initialize` spot checks | `.claude/` 38 files; depth cap `"2"`; `CLAUDE.md` present; `work/PROJECT.md` **absent — expected-red, check 5** |

The arithmetic moved as predicted and the manifest absorbed it without amendment: `tree` 188 → 191, `strip` 126 → 129, keep and expected unchanged. The three additions are precisely the prospective paths the manifest classified before they existed.

**Teardown**: worktree removed, `fold-confirm` deleted, `git worktree list` one entry, `git tag -l` **empty**, `main` still `b332083`, working tree clean.

### A stronger merge proof than the one recorded at staging

Staging proved `b332083`'s three `BASELINE.md` hunks subsumed. Checking the other two files in the merge surface closed the remaining gap: `b332083` also stripped instance-history paths from `.claude/commands/archive.md` (the durable-resumability proposal) and `.claude/commands/initialize.md` (the configure-command filing). **Both refs now carry zero occurrences of either path** — the branch removed them independently during phase 2's coherence work.

So `b332083` is subsumed on **all three** files it touched, not just the conflicted one. "Take the branch side wholesale" is safe across the entire merge surface, and the empty `git diff --stat` is explained rather than merely observed.

---

## Progress

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

- 14:32 two researchers dispatched (manifest inventory; recipe + runbook source material)
- 14:47 finding: `merge-tree` exits 1 — confirmed decision 1 falsified; verified directly
- 14:51 architect consult returned: stage the resolution, branch side wholesale, two assertions
- 14:52 planner dispatched (fable, `difficulty: high`)
- 14:58 findings + plan sections written; new finding — instance content inside four keep-column files
- 15:02 blessing verdict **BLESSED**, round 1, no critique directives
- 15:04 phase 2 landed — ADR standing-hazard entry, 4 lines, gate clean (ADR 73 → 77)
- 15:11 phase 1 landed — manifest section, decision-1 amendment, divergence bug filed
- 15:12 **PAUSED** — merge-vs-squash question to the Executor (criterion-3 upward leg)
- 15:14 resumed: Executor answered **true merge** (`git merge --no-ff`), decided on remote topology
- 15:17 phase 3 landed — recipe authored, rehearsal **GREEN** in 3 runs, worktree removed
- 15:18 phase 4 dispatched — runbook, continued from the implementer holding the proven recipe
- 15:21 correction sent: handle count is **27/13**, not decision 7's 26/13 (a `reviewer@` stamp landed in phase 2)
- 15:26 **verdict-absence stop** — parked on the runbook child; Executor nudge resumed it (3rd instance)
- 15:31 phase 4 landed — `work/runbooks/fold-and-close.md`, 723 lines, 14 sections + README row
- 15:34 quality gate green; runbook + bug closure committed (`f4edcce`)
- 15:38 **confirmation rehearsal GREEN** from the true branch tip; worktree removed; invariants hold
- 15:40 Acceptance Verdict written

---

## Code Review

> reviewer@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Fresh reviewer over `295749e`, `dd4ac33`, `c741d9d` — dispatched fresh by policy, never continued from a build context. **Nothing blocking.** Seven findings, all non-blocking and all **mechanical**; none reopened a design decision, so no pause was warranted. Six resolved directly, one accepted as-is.

**All four independently-verified claims held.** The reviewer recomputed rather than trusting: 188 tracked / 62 KEEP / 126 STRIP; BASELINE step 2 enumerated to 18; set equality both directions; purified tree 64. It also ran its own throwaway-worktree merge and confirmed `git diff --cached 26-07-23` empty — reaching the *same* extended merge proof independently, that `b332083` contributes nothing on any of its three files. The ADR entry: exactly one entry, generic, and **provably gate-neutral** — the extraction set on `295749e^` and `295749e` is byte-identical at 6 paths.

| # | Finding | Resolution |
| --- | --- | --- |
| F1 | A1's *stated wording* disagreed with its result — the loose token list matches BASELINE's generic `_enacted/` scaffold glob, so an operator reinventing the grep gets **1 hit and a false red on a stop gate** | **Fixed** — A1's literal anchored command written into the manifest, with the reason the anchors are load-bearing. The runbook § 3d already carried the correct form; only the order's prose was loose |
| F2 | Decisions 1 and 7 carried no in-place amendment marker — a reader acting on decision 1 halts a ceremony engineered to hit exactly one staged conflict | **Fixed** — marker line on each, pointing at its amendment. Additive, so the never-rewrite convention holds |
| F3 | Decision 3's three-path parenthetical reads as the whole sanctioned list, not BASELINE's half of seven | **Fixed** — qualified in place |
| F4 | Three unreconciled tree counts; the first rehearsal's `194` was never explained | **Fixed** — annotated as run 3's simulated archive (190 + 4 staged) |
| F5 | § The Executor-held item still carries 49 / 3.7 MB / 37 blobs | **Fixed** — superseded-marker added. Nothing depended on them, but the section is the shape a future mission copies |
| F6 | Footgun absorption is faithful on rule, lossy on detail — `consult.md`'s five layers were named, the ADR says "five-layer"; the audited CLI version defers to a script comment | **Accepted, consciously.** Correct compression for a register, and both files stay restorable from the tag. Recording it so the strip is a decision, not a side effect |
| F7 | The ADR entry is the register's longest (135 words vs 106/63/57) | **Accepted as written.** The rule and the observable are both load-bearing; only the middle exposition is compressible. Correct, generic, gate-clean — trimming is taste, and taste is not worth an edit to `main`-bound doctrine at this hour |

**One pre-existing convention gap surfaced, correctly routed onward.** `sessions:` frontmatter is **not** fully derivable from body stamps: seeded filing sections and bug filings carry no stamps, so `architect@…76ba13c6` and `executor@…2bd6ea6d` exist only in frontmatter. This contradicts `work/README.md:91` ("the stamps are the source of truth"), but it is present in **every phase order and bug filing in the repo** — not introduced here. Closing it is a **design** call (either seeded filings get stamps, or the derivability claim narrows to "appended sections") and belongs in the template-genericity pass alongside R1–R6. This order is in fact *stricter* than its sibling: its `## Progress` is stamped where `26-07-24.doctrine-and-adr.md`'s is not.

**The reviewer hit the hazard this phase codified**, and handled it right: the work order grew 412 → 445 lines under it mid-review because I was editing it concurrently. It read only, and said so. Its verification of the three named commits is unaffected — but it is a fourth in-anger instance of the concurrent-access hazard, and worth the ADR entry existing.

---

## Acceptance Verdict

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

**PASS**, with two recorded residuals and one process finding the Executor should carry to close.

### Acceptance walk

| Criterion | Verdict |
| --- | --- |
| Manifest stands; every tracked path classified; `.archive` dispositioned; zero unclassified | **PASS** — 188 → 191 paths, 62 KEEP / 129 STRIP, zero-unclassified proven *by construction* and re-proven at the confirmation run. `.archive/` holds exactly one tracked path, inspected, KEEP |
| Rehearsal green; `work/` matches BASELINE exactly; reference resolution, tooling smoke, `/initialize` all pass; output recorded; worktree removed | **PASS** — green **twice**. `IDENTICAL — 18` on the hard-green condition, `IDENTICAL — 64` whole-tree, 7 sanctioned unresolved, 4/4 tooling, `/initialize` green-or-expected-red. `git worktree list` one entry |
| Upward pause-and-ask happened and is recorded; decision made by the Executor | **PASS** — asked with no recommendation, answered `true merge`, resumed in place. Recorded above; the campaign log carries the criterion-3 ledger |
| *(operator-gated)* Panel attestation recorded | **PASS** — landed at `833377e`, authored unprompted during this phase's live run. Criterion 3 is complete |
| `work/runbooks/fold-and-close.md` exists with all elements; executable by a context-free reader | **PASS** — 723 lines, 14 sections, both helper scripts verbatim, the `/initialize` expected-outcomes table, the 7-entry sanctioned-absence list inline |
| No fold outside the rehearsal: `main` untouched, no tag, archive not moved | **PASS** — `main` still `b332083`, `git tag -l` empty, `work/missions/` holds only `README.md`, tree clean |

Also landed: the one sanctioned doctrine edit (`.claude/ADR.md` § Standing hazards, 4 lines, generic, gate-clean); `bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md` closed to `_done/`; `bugs/26-07-24.bug.baseline-postdate-list-divergence.md` filed and carried.

**Commits**: `295749e`, `dd4ac33`, `c741d9d`, `f4edcce`.

### The finding worth more than the artifact — what a rehearsal structurally cannot see

Run 1 of the first rehearsal **passed every visible check** — 64/64, 18/18, gate green — while carrying a defect that would have put `work/missions/v1.subagent-boundary/**` into a purified `main`. The keep list derived the `work/` scaffold by grepping for `README.md` under `work/`. That pattern matches the mission archive's own README copies. The archive is `git mv`'d in by `/close`, **upstream of the fold**, so it did not exist in any tree the rehearsal could run against.

**No amount of running the rehearsal would have caught it.** It was caught by reasoning about the ceremony-time tree, and only then proven by *simulating* the archive move.

The generalizable lesson, which is why this belongs in the verdict and not just the ledger: **a rehearsal validates the recipe against the tree it can see, and the ceremony runs against a tree that does not exist yet.** Every step upstream of the fold — the archive move, the close-out commits, the session that lands at `SessionEnd` — mutates the input the recipe will actually face. The fix generalizes past this one case:

> **Derive keep-column membership from the specification, never from the tree.** The `work/` keep set is now transcribed from BASELINE step 2's enumeration. A tree-derived pattern answers "what looks like scaffold *here, now*"; a specification-derived list answers "what scaffold *is*" — and only the second is stable under a tree you cannot predict.

The same principle is why the strip list is **derived, never transcribed** (it must absorb what arrives late), and why the manifest classified its own artifacts prospectively. The confirmation run is the empirical check on all of it: `tree` moved 188 → 191 and `strip` 126 → 129 with keep and expected unchanged, exactly as predicted.

A future mission re-running this runbook against a tree we cannot foresee is protected by that asymmetry, not by the rehearsal's green.

### Residuals

1. **~~The independent review pass did not return.~~ Corrected — it did.** I misread a stalled output-file size as a stalled agent and recorded the review as a gap. It completed: **nothing blocking**, seven non-blocking mechanical findings, six resolved directly and one accepted with reasons (§ Code Review). The correction is left visible rather than silently overwritten, because a verdict that under-claims and then corrects is recoverable, whereas one that over-claims is not — and the failure mode worth recording is that *file-size polling is not a liveness check*.
2. **Residue R1–R6, deferred by design.** Six references in keep-column files to stripped or instance-specific material that the four sanctioned adaptations do not reach — two genuine dead links (`src/README.md:7`, `work/README.md:81`), three prose-level instance facts, one set of dated examples. The reference gate cannot see them (it greps `.claude/` only). Fixing them would have required composed prose, which the architect's restore-or-delete constraint forbids here. Carried into the runbook's triage dispositions.

3. **`sessions:` is not derivable from body stamps** — a pre-existing convention gap in every phase order and bug filing in the repo, surfaced by review, not introduced here. It contradicts `work/README.md:91`. Closing it is a **design** call and routes to the template-genericity pass with R1–R6.

### Process finding for close — the third verdict-absence stop

This phase stopped without a verdict once, parked on the runbook child. That is the **third instance across two campaigns** (M2 phase 2, M3 phase 2, M3 phase 3). Three is no longer anecdote.

The trigger is consistent across all three: **a Manager dispatches a child near the end of its work and treats the dispatch as completion.** Mine had an aggravating detail worth recording — I had genuinely useful blocking reasons to wait (the runbook was another agent's file and editing it would have violated the very ownership rule this phase was codifying), and *the correctness of the reason to wait made the parking feel like discipline rather than a stop*. The remedy that worked was the same one both prior instances needed: a SendMessage nudge, clean auto-resume from transcript, context intact.

Not fixed here and not expanded into scope — recorded factually for disposition at close. The nudge mechanism has now demonstrated in anger three times.
