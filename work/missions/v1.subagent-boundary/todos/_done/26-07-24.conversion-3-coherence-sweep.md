---
type: chore
difficulty: low
depends-on: [2]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect (consult handle)
  - executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 # M2 campaign executor (seeded this order)
---

# M2 · Phase 3 — coherence-sweep (work order)

> Seeded by executor@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19 from the phase spec [`work/milestones/2.conversion/3.coherence-sweep.md`](../../milestones/2.conversion/3.coherence-sweep.md) + phase-3 reconnaissance + phase-2's recorded end-state (fallback range 28–49) and its deferred-residue handoff. **The phase doc stays the spec/record; this is the build surface.**

**Automatic mode.** [work-order workflow](../../../.claude/agents/manager/workflows/execute-work-order.md) + [automatic-mode overrides](../../../.claude/agents/manager/workflows/execute-work-order-automatically.md): plan → implement → review → resolve to Acceptance, inner budget 3, converging to a `## Acceptance Verdict` here. Documentation-only; no front-end (`work/PROJECT.md`) → no inspector. Consult the Architect for doctrinal questions: `bun .claude/bin/consult "architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2" "<question>"`.

## Objective

After phase 2's rewrite: no live-canon reference to the retired apparatus remains outside marked fallback text; sibling docs that *describe* the Manager mechanism agree with the new boundary; the deferred work-queue-tier proposal takes its reconciliation note; the full quality gate passes end-to-end. Mechanical by design.

**Phase 2 is landed (cite as fact).** The six canon files are in converted end-state. The **fallback subsection** is `.claude/agents/executor/CLAUDE.md` **lines 28–49** (`### Headless fallback — deliberate, non-default`) — the token-grep's only sanctioned survivor range. `work/README.md:90` is already corrected.

## Scope

### 1. Token sweep — the grep recipe (rerun at close, output pasted into the verdict)

**Pinned invocation (recon footgun — this env's `grep`/`ugrep` shell functions mis-split a multi-file arg list under zsh and return zero hits with exit 2, silently; do NOT trust a naive `grep "$FILES"`):** sweep **directories**, not an expanded file list —

```sh
grep -rn -- 'tmp/accomplish\|accomplish-live-feed\|stream-json\|mtime\|tail -c\|tail -f' \
  .claude work/README.md work/todos/README.md work/PROJECT.md work/footguns work/runbooks docs \
  CLAUDE.md README.md
# and, separately, Manager-dispatch-shaped claude -p:
grep -rn -- 'claude -p' .claude work/README.md work/todos/README.md work/PROJECT.md work/footguns work/runbooks docs CLAUDE.md README.md
```

**Classify every hit** (no silent skips) into one of these labels — **seven** (recon added the last two):
- `live-canon-clean` — already fixed by phase 2, now reads clean (re-verify).
- `fallback-subsection-exclude` — inside `executor/CLAUDE.md` 28–49.
- `consult-apparatus-exclude` — `.claude/bin/consult`, the callback grammar / `--fork-session` text in `work/README.md`, `work/footguns/consult.md`. A **surviving** mechanism; `--resume`/`claude -p` there are legitimate.
- `historical-exclude` — `work/todos/_done/`, `_cancelled/`, `work/proposals/` (all statuses, **including** the two loose top-level files `26-07-22.lan-intercom.md` and `26-07-23.subagent-manager-boundary.md`), `work/sessions/`, `work/missions/`. Never rewritten.
- `runbook-probe4-exclude` — `work/runbooks/probe-battery.md` `--resume` hits (probe-4 procedure, bare `claude --resume`).
- `MISSION.md-exclude` — `work/MISSION.md` is the fixed charter (its failure-criteria clause names the fallback deliberately). It is **out of scope** for edits; if a hit surfaces, classify it here — do not edit it.
- **`phase-3-own-fix`** — this phase's own residue targets (item 2 below), fixed here, not merely re-verified.

Acceptance = **zero unclassified hits.**

### 2. Semantic residue — the flagged items the token-grep will NOT catch (phase-2 handoff + recon)

These carry no banned token, so grep misses them — fix/adjudicate each explicitly:

- **`work/README.md:48`** (Executor role-ladder row): "one Manager **child-session** per phase" → mechanism-neutral wording that agrees with the converted boundary (a subagent Manager per phase; the role-ladder should not invoke the retired session-dispatch mechanism). **Fix.** Keep it work-system-generic and minimal.
- **`.claude/agents/architect/CLAUDE.md:39`** and **`.claude/agents/architect/workflows/plan-milestone.md:16`**: "the literal convergence target of a **headless Manager session**" → mechanism-neutral (an *autonomous* Manager session — the criterion is **headless-verifiability of the acceptance block**, not the dispatch mechanism; preserve that meaning + the operator-gated-items flag). **Fix.**
- **`.claude/agents/manager/CLAUDE.md:114`** ("findings travel to **child Managers** via documents") and **`.claude/agents/executor/workflows/execute-milestone.md:12`** ("so **child Managers** can consult the planning session"): "child Manager" is genealogically accurate under the new model (a subagent *is* a child of its Executor); phase-2 reviewer judged not-wrong. **Coherence-review** — keep unless the phrasing actively implies the retired *session* mechanism; if kept, note why in the verdict.
- **`work/todos/26-07-22.lan-intercom.md`** consult-site anchors drift — informational only (halted order; consult apparatus, not retired dispatch). **No edit.**

### 3. Reconciliation note

Append to [`work/proposals/_deferred/26-07-11.work-queue-tier.md`](../../proposals/_deferred/26-07-11.work-queue-tier.md), **after its final section**, house style (five prior `## Reconciliation note (…)` precedents exist), append-only (diff shows pure addition, nothing else in the file moves):

`## Reconciliation note (26-07-24, subagent-manager-boundary conversion — "never a subagent" clause obsoleted)`

Content: the file's retirement-rationale clause (a) (L10–13, "it dispatches orchestrators as Agent-tool subagents, which the session-nesting lesson rules out … so it must be a `claude -p` child session, never a subagent") rested on the session-nesting lesson; M1 certified nested dispatch on the pinned CLI and M2 converted the boundary, so a future work-queue tier dispatches Managers as subagents per the current executor canon.

### 4. Alignment + full quality gate

`BASELINE.md` ↔ `/initialize` ↔ live tree agree on the phase-1 additions; then the full quality gate (`work/PROJECT.md` § All roles):
1. Reference-resolution across `.claude/` (every referenced path resolves; sanctioned lazy-creates excepted, incl. `tmp/accomplish` inside the fallback subsection).
2. Tooling smoke: `bun .claude/bin/session-archive` and `consult` no-args → usage + exit 1; `commit-guard` TTY contract; `cd work/sessions && sha256sum -c MANIFEST`.
3. `/initialize` end-to-end (incl. the new depth-cap check #4).

## Acceptance

*(carried verbatim from [the phase doc](../../milestones/2.conversion/3.coherence-sweep.md))*

- The work order carries the grep recipe verbatim and its final run's output: **zero unclassified hits** — every hit either fixed in this phase or matched to a written exclusion class above.
- The two architect docs read mechanism-neutral while preserving the headless-verifiability criterion for acceptance blocks.
- The reconciliation note stands in `_deferred/26-07-11.work-queue-tier.md`, house style, append-only (diff shows pure addition).
- Quality gate passes in full: every `.claude/`-referenced path resolves; `bun .claude/bin/session-archive` and `consult` no-arg usage/exit-1; `commit-guard` TTY contract; `cd work/sessions && sha256sum -c MANIFEST`; `/initialize` audit green including the new depth-cap line.
- Diff scope: no edits under the historical paths (verifiable via `git diff --stat` against the exclusion list).

**Executor-added (from the phase-2 handoff):**
- `work/README.md:48` reads mechanism-neutral (no "child-session"); the four semantic-residue items above are each fixed or adjudicated-with-reason in the verdict.

## Depends on

Phase 2 (landed) — the sweep greps against final canon text with the fallback subsection delimited (28–49). Downstream premise it must leave true: **M3's validation campaign launches from coherent canon** — a fresh Executor session following only the docs dispatches a subagent Manager, and the milestone-level acceptance (`MILESTONE.md`) is checkable without this planning session's context.

---

## Progress

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

- 02:24 baseline grep sweep run (both greps clean of unclassified; all hits map to known classes); edit targets read
- 02:25 blessing: consulted milestone architect → BLESSED round 0, no critique, no human decision
- 02:25 planner skipped (deliberate): scope fully enumerated by the milestone-architect phase spec — the plan; difficulty:low mechanical sweep
- 02:26 implementation started (direct tool calls, final-mile mandate)
- 02:27 5 edits landed: 3 canon rewordings + reconciliation note; 2 "child Managers" adjudicated-keep
- 02:28 grep sweep rerun: 13 hits total, all classified, ZERO unclassified
- 02:29 review pass 1 (fresh reviewer, independent): no findings, all 3 edits PASS, both keep-rulings hold → converged
- 02:30 quality gate: refs resolve (src/_archive sanctioned lazy-create), tooling smoke green, MANIFEST OK, /initialize green incl. depth-cap check #4
- 02:31 commits landed: canon rewordings (Update:), reconciliation note (Document:)
- 02:32 Acceptance Verdict written

## Blessing

> architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # blessing

**Verdict: `BLESSED`** (round 0, no refinement rounds needed) — consulted the standing milestone-architect handle via `bun .claude/bin/consult`.

The Architect endorsed every planned edit against the phase spec's intent:
- `work/README.md:48` fix endorsed — "one Manager child-session per phase" still invokes the retired mechanism as doctrine; "one Manager subagent per phase" is the correct sibling-doc coherence. Confirmed `work/README.md:90` was already correctly converted by phase 2.
- Architect-doc rewording endorsed as specified — "autonomous Manager session" with the trailing "write criteria a headless session can verify" clause and operator-gated flag preserved; the load-bearing criterion is headless-**verifiability** of the acceptance block, not the dispatch mechanism.
- Both "child Managers" keep-rulings confirmed — a subagent Manager *is* a child of its Executor; the retired term of art was "child *session*", which bare "child" does not invoke. Keep both, note reason in verdict.
- Reconciliation note endorsed — heading, placement, append-only discipline, and content (scoping obsolescence to retirement-clause (a) only; (b)/(c) stand) all match house style.
- Grep taxonomy + gate endorsed — the seven-class taxonomy faithfully refines the spec's five exclusions; zero-unclassified + full gate carry verbatim.

**No human decision required** — every edit descends from enacted determinations, no new design decision. Boundary reminder carried into execution: `MISSION.md-exclude` is classify-only; touching the charter's failure-criteria clause would be an escalation, not an edit.

---

## Acceptance Verdict

> manager@play/2f2dc1c5-64e6-4673-8e24-4309de22fb19

**`PASS`** — every acceptance criterion met; convergence in one review pass (fresh, independent reviewer: no findings). No escalation; the Architect blessed the order with no human decision required.

### Final grep run (pasted verbatim — zero unclassified hits)

```
=== GREP 1: banned tokens ===
.claude/agents/executor/CLAUDE.md:35:mkdir -p tmp/accomplish
.claude/agents/executor/CLAUDE.md:39:  --output-format stream-json --verbose \
.claude/agents/executor/CLAUDE.md:40:  > tmp/accomplish/<phase-slug>.jsonl 2>&1
.claude/agents/executor/CLAUDE.md:46:- **Introspection.** ... tail -c 20000 ... tail -f ...
.claude/agents/executor/CLAUDE.md:47:- **Stall rule (this path only).** If the `.jsonl` mtime has not advanced ...

=== GREP 2: claude -p ===
work/footguns/consult.md:7        (consult forks a session — apparatus)
.claude/agents/manager/CLAUDE.md:123  (Planner Callback — warns against raw claude -p; apparatus)
.claude/bin/consult:209           (consult script source)
.claude/bin/consult:228           (consult script source)
work/README.md:85                 (callback grammar — --fork-session; apparatus)
work/README.md:93                 (consult read-only by construction; apparatus)
.claude/agents/executor/CLAUDE.md:30  (fallback subsection, L28–49)
.claude/agents/executor/CLAUDE.md:36  (fallback recipe, L28–49)
```

**Classification of all 13 hits (no silent skips):**

| Hit | Class |
| --- | --- |
| executor/CLAUDE.md:30, 35, 36, 39, 40, 46, 47 | `fallback-subsection-exclude` (all within the delimited `### Headless fallback` range L28–49) |
| work/footguns/consult.md:7 | `consult-apparatus-exclude` (surviving mechanism) |
| manager/CLAUDE.md:123 | `consult-apparatus-exclude` (Planner Callback text warning against the raw recipe) |
| .claude/bin/consult:209, 228 | `consult-apparatus-exclude` (the consult script itself) |
| work/README.md:85, 93 | `consult-apparatus-exclude` (callback grammar / read-only-by-construction) |

**Zero `live-canon-clean` residue, zero `phase-3-own-fix` residue remaining, zero unclassified.** The three live-canon semantic-residue targets carried no banned token (grep never saw them); they were fixed by direct edit and re-verified by the reviewer, so they do not appear as grep hits.

### Per-criterion

1. **Grep recipe + final output, zero unclassified** — PASS. Recipe carried verbatim (Scope §1); final run above; all 13 hits matched to a written exclusion class.
2. **Two architect docs mechanism-neutral, headless-verifiability preserved** — PASS. `architect/CLAUDE.md:39` and `plan-milestone.md:16`: "headless Manager session" → "autonomous Manager session"; trailing "write criteria a headless session can verify" clause + operator-gated-items flag intact verbatim. Reviewer confirmed the verifiability criterion survives and "autonomous" is the term already in live use for the subagent model (executor agent frontmatter).
3. **Reconciliation note — house style, append-only, pure addition** — PASS. Appended after the proposal's final section; `git diff --stat` shows `4 ++++` (0 deletions) for that file; heading + prose match precedents; content scopes obsolescence to retirement-clause (a) only, leaving (b)/(c) standing. Reviewer verified pure addition and clause-untouched.
4. **Quality gate passes in full** — PASS.
   - Reference resolution: every `.claude/`-referenced path resolves; the lone flag `src/_archive/` is a BASELINE-sanctioned convention path ("created on first retirement — reference gates must sanction its absence", BASELINE.md:38), not a broken reference.
   - Tooling smoke: `session-archive` no-args → usage + exit 1; `consult` no-args → usage + exit 1; `commit-guard` empty-stdin backstop → exit 0 silent (the interactive-TTY usage branch is not scriptable in this non-TTY env — operator-verifiable); `sha256sum -c MANIFEST` → all OK, exit 0.
   - `/initialize`: green — baseline integrity (0 missing), hooks resolve (both targets ran), **spawn-depth cap == "2" (check #4)**, keystone docs, scaffold, toolchain all pass. One pre-existing, out-of-scope hygiene note (below).
5. **Diff scope — no historical-path edits** — PASS. `git diff` for this phase touched exactly: `work/README.md`, `.claude/agents/architect/CLAUDE.md`, `.claude/agents/architect/workflows/plan-milestone.md`, `work/proposals/_deferred/26-07-11.work-queue-tier.md` (the sanctioned reconciliation-note exception), and this work doc. No edits under `_done/`, `_cancelled/`, `work/sessions/`, `work/missions/`, `work/MISSION.md`, or `work/proposals/` beyond the one append.
6. **Executor-added (README:48 + four semantic-residue items)** — PASS.
   - `work/README.md:48` reads mechanism-neutral ("one Manager subagent per phase"). **Fixed.**
   - `architect/CLAUDE.md:39` + `plan-milestone.md:16`. **Fixed** (item 2).
   - **Adjudication — the two "child Managers" phrasings, KEPT:** `manager/CLAUDE.md:114` ("findings travel to child Managers via documents — subagents don't cross the session boundary") and `execute-milestone.md:12` ("so child Managers can consult the planning session"). Both are genealogically accurate under the converted model — a subagent Manager *is* a child of its Executor. The retired term of art was "child *session*"; bare "child" does not invoke it. `manager/CLAUDE.md:114` is self-reinforcing ("subagents don't cross the session boundary" in the same clause frames the Manager as a subagent); `execute-milestone.md:12` sits two lines above step 7.1's explicit "Each phase Manager is an Agent-tool subagent." Both the Architect (blessing) and the independent reviewer confirmed neither implies the retired mechanism. **No edit.**
   - `work/todos/26-07-22.lan-intercom.md` consult-site drift — informational only, halted order, consult apparatus. **No edit** (as specified).

### Notes / residuals (non-blocking, out of scope)

- **Pre-existing hygiene, for a separate filing:** `.gitignore` carries no explicit `.env`/secret pattern. This is the workflow bootstrap's own instance repo (no application secrets), and it is unrelated to the retired-apparatus conversion — surfaced by `/initialize` check #7, not a regression from this phase. Recommend a hygiene follow-up (add `.env*` + key/secret globs to `.gitignore`), or a genericity review of the shipped `.gitignore` template.
- **Milestone-doc + roadmap update deliberately not done here** — that is the Executor's outer-loop close-out (execute-milestone step 7.3, on ultimate-acceptance PASS). The work doc is left in `work/todos/` (not moved to `_done/`) as the caller's handback surface.
