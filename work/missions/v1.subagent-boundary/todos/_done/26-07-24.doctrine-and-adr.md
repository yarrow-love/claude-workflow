---
type: feature
difficulty: high
status: open
scope: [.claude, docs]
sessions:
  - architect@play/76ba13c6-e6f5-4933-9e6c-7ef3fbdd6b7d # mission architect (stub)
  - architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # milestone architect — consult handle
  - executor@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # campaign executor
  - manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2 # phase-2 manager (subagent, anchored to the hosting Executor)
---

# M3 phase 2 — doctrine and ADR

Seeded from [`work/milestones/3.validation-and-fold/2.doctrine-and-adr.md`](../milestones/3.validation-and-fold/2.doctrine-and-adr.md) by the campaign Executor. The phase doc stays the spec/record; **this** document is the build surface. Do **not** write to the campaign log [`26-07-24.execute-validation-and-fold.md`](26-07-24.execute-validation-and-fold.md) — it is the Executor's alone.

## Objective

Make the workflow's architecture durable on the product side of the seam: rewrite the root dual-doctrine per D4 (product-on-`main`, instance-on-mission-branches) and author the new `.claude/ADR.md` — the terse decision register future agents read instead of excavating archived branches. **Everything authored here folds to `main` unchanged; genericity is the acceptance test for every line.**

Phase 1 is closed: the proposal now sits at `work/proposals/_enacted/26-07-23.subagent-manager-boundary.md` — cite it at that path.

## Scope

**1. Root `CLAUDE.md` rewrite (D4).**

- The two-bullet product/instance doctrine paragraph → the new model: `main` is the pure project-generic template (product only: `.claude/` manifest, scaffold, canon READMEs, `src/README.md`, `.cbmignore`); the instance — the workflow's own development — lives on **mission branches**, each closed branch tagged (`<v>-closed`) and carrying its mission archive; design history is reachable through `.claude/ADR.md` and the tags, not through in-tree instance files.
- The footgun-index table (rows point at `work/footguns/*`, which archives with the branch) → replaced by a pointer to ADR § Standing hazards.
- Conventions section survives with the genericity rule intact; "BASELINE tracks the manifest" gains nothing new (ADR.md rides the same rule).

**2. Root `README.md` rewrite (D4).** The header's "both source of truth and live instance" sentence; the "Bootstrap a new project" section (its selective-copy warning presumes a non-purified `main` — post-fold the instruction is **clone `main`**, and the wholesale-copy trap is dead at the root, per D4's stated consequence); the "Developing the workflow" section → the continuation protocol (branch from `main` per mission, work under the workflow on the branch, close + fold per the ceremony); the "Provenance" section → points at the mission tags and ADR instead of in-tree paths (the founding consult handle moves to the ADR with its restore path noted).

**3. `.claude/ADR.md` — new, terse, BASELINE-manifested.** A decision register, not an essay — one compact entry per decision: number, date, one-line decision, one-to-three-line rationale, provenance pointer (`git show <tag>:<path>` form). Integrates:

- **The founding determinations** (from `work/todos/_done/agentic-workflow/1.determinations.md`, locked 26-07-11 + the 26-07-12 `/design` amendment): callback grammar; the role ladder; the command family; the portability seam (`.claude/` + BASELINE vs `work/PROJECT.md`); MCP posture; durability/archive-on-stamp; mission close; the `work/` layout.
- **Mission v1 (subagent-boundary, 26-07-23/24)**: D1 proposal-binds; D2 *as amended* — depth is configured state; the spawn-depth key is a **pin + cap** against a default-deep platform (the enabling premise inverted on v2.1.218; capability flipped twice in a year — never assume); D3 whole-tree archives (Manager below the durable tier, stamps anchor to the Executor session, `session-archive` sweeps `subagents/`); D4 template purification (this doctrine itself); D5 consult integrity. Plus the boundary conversion's operational rules worth one line each: pause-and-ask protocol, verdict-absence detection, parent-mode permission dominance, headless-as-documented-fallback.
- **§ Standing hazards** (absorbing the product footguns): the consult DENY-list is name-deny-only for the deferred/built-in tool class — re-enumerate and reconcile on every harness upgrade; consults are repo-bound (exit 4 on foreign handles); `.claude/bin/` JSDoc headers must never contain `*/` (globs break the bun parse).
- **§ Continuation protocol**: how the next workflow mission runs — branch from `main`, `/plan` the mission (charter recreates `work/MISSION.md`), author `work/PROJECT.md` at branch open, close + fold per the staged ceremony; the mission-tag ledger (v1 → tag, one line, append per mission).

Each mission entry cites its tag; the register is append-per-mission. **Genericity check**: the ADR documents the *product's* design rationale — it must read correctly inside any consuming repo; only the continuation protocol is explicitly marked as for-the-template-repo.

**4. BASELINE reconciliation.** `ADR.md` added to the manifest. Scaffold-enumeration gaps closed (see decision 1 below — there are four, not two). `work/sessions/MANIFEST` and `work/PROJECT.md` explicitly noted as bootstrap-authored/lazy-created.

**5. Routed bug — fix it here.** [`bugs/26-07-24.bug.initialize-command-genericity-leak.md`](bugs/26-07-24.bug.initialize-command-genericity-leak.md): `.claude/commands/initialize.md:13` carries project facts from a *different* project ("this repo: ephemeris data, catalogs, vendored clones"). This is a genericity violation in the product by this repo's own stated acceptance test, and it survived M2's coherence sweep because that sweep hunted dispatch tokens, not project facts. You are already editing product docs — fix it, close the bug order to `_done/`, and **sweep for siblings**: grep the whole of `.claude/` for other foreign-project facts of the same shape (a check nobody has yet run). Report what the sweep found, including "nothing else."

## Acceptance

Carried verbatim from the phase doc, then amended by the gate corrections below — **the amendments govern where they conflict**.

- Root `CLAUDE.md` and `README.md`: no remaining passage presumes instance content on `main` — headlessly checkable: `grep -n 'todos/_done/agentic-workflow\|proposals/_enacted/26-07-05\|proposals/26-07-05\|work/footguns/\|work/sessions/' CLAUDE.md README.md` returns zero hits (the ADR and tags carry those pointers now); the bootstrap section says clone `main`; the genericity rule and BASELINE-manifest rule survive.
- `.claude/ADR.md` exists; covers the founding determinations, mission v1's decisions as amended, standing hazards (all three), and the continuation protocol; every mission entry carries a provenance pointer; length in the spirit of "terse" — a register, not a narrative (reviewer judgment; target well under 150 lines).
- `BASELINE.md` manifests `ADR.md`; the scaffold enumeration reflects the reconciled list; the same-commit rule held (manifest updated in the commit that adds ADR.md).
- Genericity review recorded in the work order: the ADR and rewritten root docs read correctly for a consuming repo; project-specific residue (machine names, operator paths) absent from ADR.md — `grep -n 'yarrow\|/home/' .claude/ADR.md` empty.
- Reference resolution passes for all edited files against the *current branch* tree (the bare-template resolution check is phase 3's rehearsal — root docs may still cite `work/MISSION.md` etc. as conventions, but only as creatable-by-convention paths, matching BASELINE's enumeration).
- Historical files untouched: the determinations record and founding corpus are sources read, never edited.

## Confirmed decisions (Reconnaissance gate, 26-07-24)

Milestone-architect confirmed. These **govern** the acceptance list above.

1. **The BASELINE gaps are four, not two — under a set-equality invariant.** Step 2's brace expansion omits `sessions/` **entirely** (directory *and* README), and lists `missions/`, `notes/`, `runbooks/` bare, so `missions/README.md`, `notes/README.md`, `runbooks/README.md` are unenumerated though all three are tracked. Scope item 4's original list was illustrative, not exhaustive. State the governing invariant explicitly in the reconciliation: **BASELINE step 2's enumeration and phase 3's purification keep-column must be identical sets.** Close everything that invariant surfaces — including anything beyond these four.

2. **`.archive/README.md` is an unmanifested product orphan — manifest it.** 190 bytes, generic convention statement ("repo-local cold storage… not part of the bootstrap"), added in the founding commit `5adad6a`, never touched since, zero project facts. It is product, it survives the fold, and it appears nowhere in `.claude/BASELINE.md`.

3. **Sanctioned-absence additions land in *two* places, not one — this is the phase's most consequential mechanical item.** `work/PROJECT.md` is referenced by **17 files** under `.claude/` and is stripped by purification; `work/sessions/MANIFEST` is referenced by `initialize.md` (which treats it conditionally, but the gate's blunt grep does not). Both must be added to the sanctioned-absence list in **`work/PROJECT.md` § All roles step 1** (this branch's live gate, which phase 3's rehearsal runs) *and* noted in **BASELINE** as bootstrap-authored/lazy-created. **Landing it in BASELINE alone leaves phase 3's rehearsal red, and that is a phase 2 acceptance failure, not a rehearsal tolerance.** Also add `src/_archive/` while you are there — phase 1 filed [`bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md`](bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md) showing `BASELINE.md:38` mandates gates sanction it but `PROJECT.md:9` omits it. Fix the `PROJECT.md` half here; note in the bug order that the runbook half is phase 3's, and leave the order open for phase 3 to close.

4. **The acceptance grep flags one passage that is not a genericity violation.** `README.md:10` uses `work/sessions/` to describe the callback-grammar archive mechanism as *architecture*. Rephrase to point at `work/README.md` § The callback grammar — satisfying a literal-string check that cannot distinguish convention from instance. Do not mistake this for evidence the bullet was wrong.

5. **The continuation-protocol exemplar must be prospective.** `git tag -l` is empty; `v1.subagent-boundary-closed` does not exist until the ceremony that follows M3's close. Phrase as "at close the mission tags as `<v>-closed`; the next mission restores its supplement via `git show <tag>:work/PROJECT.md`" — never as a runnable example asserting a live tag.

6. **Mark the two-audience boundary structurally.** The ADR body must read correctly inside any consuming repo; § Continuation protocol is template-repo-only. Carry that as an explicit sub-heading boundary in the file itself, not merely a planning-time distinction — a single file mixing audiences invites future edits that blur it.

## Reconnaissance findings — source pointers (saves re-excavation)

- **Founding determinations 1–8**: `work/todos/_done/agentic-workflow/1.determinations.md:20-31`; the 26-07-12 `/design` amendment (family grows to ten) at `:50-55`.
- **Mission v1 D1–D5**: `work/MISSION.md:29-40`, with D2's premise-inversion amendment note at `:34`.
- **Operational rules**: pause-and-ask and headless-as-fallback at `work/proposals/_enacted/26-07-23.subagent-manager-boundary.md:33,38`; permission dominance at `:23,38`; verdict-absence detection at `work/milestones/2.conversion/2.canon-rewrite.md:33`.
- **The three standing hazards are confirmed complete** — `work/footguns/bin-scripts.md` has exactly one entry, `work/footguns/consult.md` exactly two; there is no fourth.
- **House-format precedents**: the determinations record's numbered table (register body); `MISSION.md:30-40`'s bold-lead-plus-inline-amendment (mission entries); the footgun ledgers' `##`-heading-per-hazard (§ Standing hazards). Match one of these rather than inventing a format.
- **The acceptance grep's current output** is 6 hits: `CLAUDE.md:6,22,26,27` and `README.md:10,29`. Semantic presumptions the grep misses, which must also go: `CLAUDE.md:3` ("Two things live here and must not blur"), `README.md:3` ("both source of truth and live instance"), `README.md:16` (the selective-copy warning), `README.md:23-25` ("Developing the workflow"), `README.md:27-29` (Provenance).
- **Load-bearing passages that must survive**: `CLAUDE.md:14` (genericity is the acceptance test), `:15` (BASELINE same-commit rule), `:16` (historical documents never rewritten), `:17` (no packaging leaks to root), `:18` (commit conventions).

## Notes for the run

- **Downstream premise this phase must leave true**: phase 3's purification manifest and rehearsal treat the rewritten root docs + ADR as the **final `main`-bound text** — the fold carries them verbatim, and no doctrine authoring remains in phase 3.
- The consult handle for planning intent is `architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2`, via `bun .claude/bin/consult "<handle>" "<question>"`.
- Quality gate per `work/PROJECT.md` § All roles. **Known pre-existing environment noise** at `/initialize` check 2: `chrome-devtools` MCP not connecting (`-32000`), `codebase-memory-mcp` not installed. Neither is yours; do not chase them.
- Commits: enumerate paths, never `git add -A`. The ADR and its BASELINE manifest row land in the **same commit** (the same-commit rule is itself an acceptance criterion).

---

## Plan

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Six phases; A–D own disjoint file sets and run in parallel, E serializes after A+D (file contention), F verifies.

**A — `.claude/ADR.md` + BASELINE reconciliation** (one commit; the same-commit rule is an acceptance criterion, so they cannot split across agents). ADR format is a **hybrid of the three house precedents** the reconnaissance named, one per block: numbered table for the founding determinations (original numbering 1–8 preserved so existing "det. 2" citations stay live), bold-lead-plus-inline-amendment for mission D1–D5, heading-per-hazard for § Standing hazards. Target 85–100 lines. The two-audience boundary is a single trailing `## Template repository only` super-section holding continuation protocol + mission ledger + the founding consult handle. BASELINE gains `ADR.md` in `### This file`, the reconciled step-2 enumeration, and decision 3's lazy-create note; `BASELINE.md:5` ("design rationale under `work/`") also repoints at the ADR — a semantic instance-presumption the acceptance grep does not catch but phase 3's premise does.

**B — root `CLAUDE.md` + `README.md`.** D4 doctrine header; conventions survive verbatim; footgun table → ADR § Standing hazards pointer phrased to avoid the literal banned string; README bootstrap becomes clone-`main`; Provenance shrinks to the tags + ADR; `README.md:10` rephrased per Confirmed decision 4.

**C — `work/PROJECT.md` sanctioned absences** (+ the phase-3 note on the still-open bug order). **D — `initialize.md` leak fix** (+ bug closed to `_done/`). **E — sweep absorption. F — reviewer verification** against the literal acceptance greps.

**Set-equality invariant (Confirmed decision 1) — the gaps are seven paths, not four.** The planner verified BASELINE step 2 against `git ls-files` and phase 3's keep column (`3.fold-preparation.md:19`): the four known (`sessions/README.md`, `missions/README.md`, `notes/README.md`, `runbooks/README.md`), plus `.archive/README.md` (Confirmed decision 2), plus **`docs/README.md` and `docs/research/README.md`** — both tracked, both in phase 3's keep column, both absent from step 2. `.gitignore` is in the keep column but stays out of step 2 under an explicit carve-out sentence (root keystones `CLAUDE.md`/`README.md`/`.gitignore` are template-carried but consuming-repo-re-authored); the carve-out is stated in BASELINE so the invariant reads as exact rather than approximate.

**Where Confirmed decisions overrode Acceptance**, as applied: `README.md:10` is rephrased, not deleted (decision 4); the gap count is invariant-driven, not fixed at four (decision 1); the sanctioned-absence fix lands in both places, PROJECT.md as its own phase (decision 3); § Continuation protocol prose is prospective, while the mission entries' provenance pointers do cite the literal tag string — sanctioned, since phase 3 amendment 6 verifies those very strings before push (decision 5).

### Decision points

- **ADR format family** — hybrid of the three house precedents *(recommended)* vs. one uniform 5-column table. Rejected alternative is cramped for D2's amendment nuance and invents a shape no house doc uses.
- **Two-audience boundary** — single trailing `## Template repository only` super-section *(recommended)* vs. per-section audience badges. Badges blur under future edits, which is the failure Confirmed decision 6 exists to prevent.
- **Founding consult handle** — keep it, inside the template-repo-only section with its restore-from-tag path *(recommended)* vs. drop it. Dropping makes the founding session reachable only by archaeology.
- **Bootstrap section** — full clone-based rewrite *(recommended)* vs. minimal edits keeping copy-the-manifest framing. The latter preserves a procedure D4 made obsolete.
- **`.gitignore` and the invariant** — carve out with the root keystones *(recommended)* vs. enumerate it in step 2, which would have the template dictate consuming repos' ignore files.
- **`docs/` READMEs joining step 2** — *not freely reversible*: the invariant plus the never-edit-historical-phase-docs constraint force it. Flagged because it widens the published template surface by two files; reversing would require the operator amending phase 3's keep column through its own process.

---

## Blessing

> architect@play/6a0035c9-cd57-4573-8e3a-b22dc4cba6c2 # blessing

**BLESSED**, round 1, no critique directives. Consult-forked the standing milestone-architect handle rather than dispatching a fresh architect: the spec's author judges whether the order serves its intent. Five points submitted for judgment, all resolved as interpretation of the milestone spec — the architect stated explicitly that none is a new design decision and none needs the operator.

1. **Seven-path gap set confirmed, `docs/` READMEs included.** The invariant is the point of decision 1 and was applied correctly. Crucially this does *not* widen what `main` carries — both `docs/` READMEs are already tracked, already template-pure, already in the operator-approved keep column. What widens is BASELINE's *honesty* about a surface that already exists. Added rationale the plan lacked: `/research` writes into `docs/research/`, so a consuming repo genuinely needs that scaffold — its absence is a latent reference-gate failure in every bootstrap. Reconciliation, not a new surface decision.
2. **`.gitignore` carve-out acceptable and structurally consistent.** BASELINE step 3 already carves out root `CLAUDE.md` as consuming-repo-authored, so the root-keystone carve-out extends an existing seam rather than inventing one. The invariant is not hollowed *because the carve-out is enumerated in BASELINE itself*: "step 2 ∪ the three named keystones = keep column, exactly" remains an exact auditable equation. An *unstated* exception would hollow it; this is not one. Enumerating `.gitignore` in step 2 would be the worse error — the template dictating consuming repos' ignore files is the overreach the seam exists to prevent.
3. **Prospective prose, literal provenance pointers — the intended split.** The continuation protocol is timeless product prose and must never assert a live tag; the mission entries' pointers are *data* — the tag name was pinned at planning, the ADR reaches `main` only through a fold that postdates the tag, and phase 3 verifies the literal strings pre-push. Data verified before it goes live; prose that never goes stale.
4. **Founding handle inside `## Template repository only` — confirmed, and more than stylistic.** Consults are repo-bound (foreign handles refuse, exit 4 — the standing hazard the ADR itself records). A machine-qualified handle in the generic body would be an instruction a consuming repo *cannot follow*; the placement makes the audience boundary and the consult semantics agree.
5. **`BASELINE.md:5` repoint — in scope, do it.** The phase's downstream premise is that phase 3 inherits final `main`-bound text with no doctrine authoring left. An instance-presuming pointer in a product file that folds to `main` is exactly this phase's remit; leaving it would either fold an orphan pointer or push doctrine work into phase 3 against the premise. The acceptance bullets enumerate the known surface — they were never a ceiling on removing instance-presumptions from `main`-bound files.

---

## Progress

- 14:05 genericity sweep dispatched (scope item 5 — researcher, fresh)
- 14:06 planner dispatched (step 5 — fable, `difficulty: high`)
- 14:08 genericity sweep returned: 1 new sibling leak (`close.md:17`), 2 adjudicated no-change
- 14:14 blessing BLESSED (round 1, no directives) — consult-fork of the milestone-architect handle
- 14:15 implementation started — phases A/B/C/D dispatched in parallel (disjoint file sets)
- 14:17 phase C landed (PROJECT.md sanctioned absences + bug-order phase-3 note)
- 14:18 phase B landed (root docs; acceptance grep 6 hits → 0)
- 14:19 phase D landed (initialize.md + close.md leaks; bug closed to `_done/`)
- 14:22 phase A landed (ADR.md, 73 lines; BASELINE reconciled, 7 gaps closed)
- 14:24 novelty surfaced + architect-consulted: gate extracts tag-qualified provenance paths as tree refs — ruled in-scope for phase 2, option (a); phase C continued to amend the recipe
- 14:38 gate recipe amended + verified (33 → 30 extracted paths, removals only)
- 14:52 review pass 1: 1 blocking (reclassified, filed), 2 non-blocking (both resolved)
- 14:56 quality gate green (items 1–3; two known host-environment reds excluded)
- 14:58 commits landed: 668e78e, cba0711, d914146, 6ef32bb
- 15:00 Acceptance Verdict written

---

## Genericity sweep (scope item 5)

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

**Coverage**: every tracked file under `.claude/` — 24 files: all agent `CLAUDE.md` and workflow docs, all ten commands, `BASELINE.md`, both settings files, all three `bin/` scripts. Patterns: foreign-project domain nouns, `work/` instance-artifact references (distinguishing *convention* pointers from *instance* ones), machine/operator paths and the machine literal, live session UUIDs, and stack assumptions stated as fact.

**One sibling found and fixed.** `.claude/commands/close.md:17` hardcoded `git grep -h '@play/'` — this repo's actual machine name embedded in a recipe as though generic, where every other callback reference in the product correctly uses `<machine>`. Fixed to the placeholder, plus an explicit "every machine in the registry, not just this host" clause: the implementer's reasoning was that a self-deriving `$(hostname -s)` form would silently narrow a sweep whose whole obligation is to span machines, and that the machine-partitioning that *does* belong to this flow already lives downstream in `/archive`.

**Adjudicated no-change**, recorded so a future sweep does not re-flag them blind:

- `.claude/ADR.md` § Founding provenance carries `architect@play/a9ddd6b3-…`. Architect-confirmed (Blessing point 4): it is a *concrete handle to a real archived session*, not a stand-in — genericizing destroys the information — and it is quarantined below the template-repo-only boundary precisely because consults are repo-bound.
- `.claude/agents/manager/CLAUDE.md:98` "settled 26-07-11" — a provenance citation, not a fact a fresh repo must satisfy. Nothing dereferences it.

**One judgment call split, and half of it fixed.** The sweep flagged `BASELINE.md:3`'s canonical-home line whole; the reviewer correctly separated it. The GitHub URL is deliberate and load-bearing (it names the one upstream a clone re-syncs from, and D4 makes that *more* important). The parenthetical local path `~/dev/claude-workflow` is a machine-specific path in the one file copied byte-identical into every consuming repo — removed.

**Negative result, stated plainly**: beyond `close.md:17`, the sweep found no other foreign-project or machine-specific fact of the `initialize.md` shape anywhere in `.claude/`. All `work/…` references in the product are convention pointers, not instance artifacts; the `bin/` scripts derive machine and repo root dynamically; no stack assumptions are stated as fact. **The product docs are clean for the fold.**

---

## Code Review

> reviewer@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Fresh dispatch over the full working-tree diff. All seven literal acceptance checks pass; the reviewer additionally verified set-equality against `git ls-files`, the prospective/literal tag split, two-audience leakage in both directions, ADR coverage against every cited source, and that historical files are untouched.

**Resolved without escalation (mechanical):**

1. *The `git show <ref>:` exclusion only catches fully-written pointers.* An elided `` `…:work/path.md` `` form slips through — and `ADR.md:34` used exactly that form until it was rewritten mid-flight, so the gate was passing by timing rather than construction. Resolved by stating the requirement where the format is defined (`ADR.md:3`): write every pointer in full, use a tag or SHA, never a branch name. Chose stating over widening the regex because the failure mode is loud — the gate goes red — so a documented convention is proportionate, and it simultaneously closes the reviewer's separate finding that the ref character class excludes `/` and would silently fall through on `origin/main`-shaped refs.
2. *Pre-existing markdown defect in the gate recipe.* The command was a single-backtick span containing a backtick, so under CommonMark it closed early and the recipe rendered truncated mid-pipeline — an agent reading the rendered supplement got a broken gate command. Delimiter-only fix to a double-backtick span. This travels: `work/PROJECT.md` is the shape exemplar every consuming repo copies.
3. *`BASELINE.md:3` local path* — removed, per the sweep split above.

**Reclassified and filed, not fixed:** the reviewer flagged as *blocking* that eight product/scaffold files still instruct roles to reconcile "the CLAUDE.md index", which this phase's rewrite replaced with an ADR pointer. On inspection those instructions remain **correct as generic convention** — a consuming repo authors its own root `CLAUDE.md` at bootstrap step 3 and may index its ledgers there; `work/footguns/README.md` reads correctly for that audience. The drift is confined to this template's own root doc. That makes it real but out-of-scope (scope items 1–5 never named the agent-role docs) and design-flavored — the open question is whether a purified template should still model the ledger-index convention at all. Both resolutions touch agent policy prose across eight files, which wants its own blessed order rather than an unchartered end-of-phase edit. Filed as [`bugs/26-07-24.bug.footgun-index-convention-drift.md`](bugs/26-07-24.bug.footgun-index-convention-drift.md), with an explicit note that phase 3 will *not* surface it (that rehearsal checks purification, not prose coherence).

---

## Summary

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

Four commits: **668e78e** (`.claude/ADR.md` + BASELINE, same commit), **cba0711** (root doctrine), **d914146** (reference gate), **6ef32bb** (genericity leaks).

`.claude/ADR.md` is 73 lines — under the 85–100 target and far under the 150 ceiling. The implementer declined to pad to hit a line count, correctly: terseness was the instruction. Format is the blessed hybrid of three house precedents, one per block. The two-audience boundary is structural — a single `## Template repository only` super-section — and the reviewer checked leakage in both directions.

**Design decisions made during implementation:**

- **The set-equality invariant surfaced seven gaps, not four.** `docs/README.md` and `docs/research/README.md` were tracked, in phase 3's keep column, and absent from BASELINE step 2. Architect-confirmed as reconciliation rather than surface-widening: both were already template-pure, and `/research` writes into `docs/research/`, so its absence was a latent reference-gate failure in *every* bootstrap.
- **`.gitignore` carved out rather than enumerated**, with the carve-out stated in BASELINE so the invariant remains an exact auditable equation. Enumerating it would have the template dictate consuming repos' ignore files — the overreach the seam exists to prevent.
- **The gate learned a distinction it did not have.** Discovered mid-implementation, escalated, and ruled in-scope: tag-qualified provenance paths are data, not tree references. Implemented syntactically (consume the `git show <ref>:<path>` expression) rather than as per-path exemptions, which rot.

**Deferred**, each with enough detail to seed a todo:

- `bugs/26-07-24.bug.footgun-index-convention-drift.md` — **new**, filed this phase. Eight files; a genuine design question about the purified template.
- `bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md` — **deliberately left open** per Confirmed decision 3. Its `work/PROJECT.md` half landed here; the runbook half (writing the sanctioned-absence list *into* the fold runbook, rather than assuming it resident in a file purification strips) is phase 3's, which closes the order.

**Diverged from the plan**: phase E (sweep absorption) collapsed into phase D, since the sweep returned before the builds finished and its one finding sat in a file phase D already owned.

**A hazard about this workflow's own parallel-dispatch discipline, recorded rather than smoothed over.** I dispatched A–D in parallel on file sets I asserted were disjoint, then — under the Executor's final-mile instruction — edited `work/PROJECT.md` directly while a continued phase-C implementer was still live on that same file. Two writers, one file. It surfaced only because phase C's `Edit` failed on a stale `old_string`; a lost update was possible and would have been silent. The disjointness held *between specialists* and broke at the **manager/specialist boundary**, which is exactly the seam a manager is least likely to check: I stopped treating a continued agent as an owner the moment I picked up the tool myself. The correct discipline is that dispatching an agent onto a file transfers ownership until it reports, and the final-mile exception does not implicitly revoke it — either wait, or cancel the continuation explicitly before editing. Phase C recovered well (it verified the surviving recipe against its own independently-derived form, diffed both extraction sets to byte-identical, and kept the stronger one — the surviving form anchors its qualifier alternative at `git`, strictly left of the path, so leftmost-match alone decides it, where the alternative leaned on GNU grep preferring the longer alternative at equal start position). That it ended well was luck plus a good specialist, not design.

---

## Acceptance Verdict

> manager@play/2bd6ea6d-d5b5-43cb-bfcc-743b1494caa2

**PASS.** Every acceptance criterion met, as amended by the Confirmed decisions. Blessed round 1 with no directives; one mid-flight novelty escalated to the architect and resolved as interpretation; review converged in a single pass.

| Criterion | Result |
| --- | --- |
| Root docs: acceptance grep zero hits | PASS — was 6 (`CLAUDE.md:6,22,26,27`, `README.md:10,29`), now 0 |
| Bootstrap section says clone `main` | PASS |
| Genericity + BASELINE-manifest rules survive | PASS — all five load-bearing conventions verbatim |
| `.claude/ADR.md` exists, covers all required content | PASS — founding 1–8 + `/design` amendment, D1–D5 with D2 as amended, four operational rules, exactly three hazards, continuation protocol, mission ledger, founding provenance |
| Every mission entry carries a provenance pointer | PASS |
| Terse — a register, not a narrative | PASS — 73 lines |
| BASELINE manifests `ADR.md`; same-commit rule held | PASS — 668e78e contains both files and nothing else |
| Scaffold enumeration reconciled | PASS — seven gaps closed; invariant + carve-out stated in-file; verified against `git ls-files` |
| `grep -n 'yarrow\|/home/' .claude/ADR.md` empty | PASS |
| Genericity review recorded | PASS — Code Review + Genericity sweep sections above |
| Reference resolution passes on this branch | PASS — only misses are the four sanctioned lazy-creates |
| Historical files untouched | PASS — only change under `_done/` is the moved bug order |

**Decision 3 — the criterion that could fail phase 3 — is closed in both places.** `work/PROJECT.md` § All roles step 1 now lists `src/_archive/`, `work/PROJECT.md`, and `work/sessions/MANIFEST`; BASELINE independently notes the same three as post-scaffold paths. Phase 3's rehearsal gains a third protection nobody planned for: the reference gate no longer mistakes the ADR's tag-qualified provenance for live tree references, which would otherwise have gone red on three paths that never exist on `main`.

**Downstream premise left true**: the rewritten root docs and the ADR are final `main`-bound text. No doctrine authoring remains for phase 3.

**Residuals — neither blocking, both filed, neither requires action before the fold:**

1. `bugs/26-07-24.bug.footgun-index-convention-drift.md` — out-of-scope design question about whether a purified template models the ledger-index convention. Phase 3 will not surface it.
2. `bugs/26-07-24.bug.project-md-sanctioned-absence-list-incomplete.md` — intentionally open; phase 3 owns the runbook half and closes the order.

**Not operator-gated on anything.** No deploy, runtime evidence, or measurement is required; `interface` scope does not occur in this repo, so no inspector was dispatched. The two known host-environment reds at `/initialize` check 2 (`chrome-devtools` MCP `-32000`, `codebase-memory-mcp` absent) were excluded as pre-existing and not this phase's, per the work order.
