# missions/

Closed missions, archived whole and in-repo — `<v>.<slug>/` with `MISSION.md`, `closing.md`, and the mission's work system. This README becomes the mission ledger (one row per mission) at the first `/close`. None closed yet.
